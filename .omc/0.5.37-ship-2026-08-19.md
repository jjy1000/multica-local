# 0.5.37 Ship Log — 2026-08-19

**Version**: 0.5.36 → 0.5.37 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Time**: 14:06–14:13 local (06:06–06:13 UTC)
**Goal**: client-log audit close-out (4 fixes) + MUL-6140 UX port

## Ship steps (all PASS)

| Step | Result |
|---|---|
| Pre-ship liveness (PG:5432 + server:8090 + /health) | PASS |
| pnpm typecheck | 6/6 PASS (full turbo) |
| go build ./... | clean |
| go test ./internal/... ./pkg/agent/... | PASS except documented pre-existing mythos panic (0.5.34 baseline byte-identical) |
| pre-update snapshot | PASS (app .bak + PG backup `pre-update-20260819-140841`) |
| migrate up | 0 new migrations, all skip |
| bundle-cli | PASS (server/migrate/multica + pythia + openscience prompts + claude-science + stubs) |
| electron-vite build | PASS 1.43s (renderer 12.6MB) |
| electron-builder --dir | PASS (no deadlock this time; ad-hoc sign + skipped notarization) |
| 4a/7 renderer integrity | rawRequest 155 in asar |
| cp -R → /Applications | PASS |
| desktop-sign-nested-binaries.sh | PASS (3 nested binaries signed, multica exit 0) |
| verify-desktop-cold-start.sh | PASS: server 0.5.37 PID 98708, ~4s, row parity psql-unavailable (non-blocker) |

## Commits shipped

```
55114e921  docs(audit): client log audit 2026-08-19
ef5a7f615  fix(desktop): 50MB log rotation
572eeaba5  feat(issues): scroll to newly-posted comment (MUL-6140)
ef4484fa5  fix(desktop): renderer console sink + daemon spawn mutex
e5e0c8b40  fix(invitations): dead code removal
```

## Risk vs outcome

- **Risk**: none of the 4 fixes touch server data paths or migrations; all TS/Electron-main only. MUL-6140 is a pure UI additive.
- **Outcome**: log noise -166 WRN/day (invitations), daemon spawn race eliminated (10 EADDRINUSE/13h → 0), server.log auto-rotates at 50MB, renderer crashes now leave a breadcrumb, and posting a comment auto-scrolls to it.
- **Regression check**: cold start ~4s (0.5.36 was 4s — same), server 0.5.37 healthy.

## Known stability surfaces touched (re-read before next touch)

- `server-manager.ts` log open — now preceded by `rotateLogIfNeeded` (L505)
- `daemon-manager.ts` `startDaemon` — now wrapped by `startInFlight` single-flight (L910-925)
- `renderer-log.ts` (new) — path pinned in `ensureActiveProfile`

## Post-ship

- Release note `.omc/release-notes-0.5.37.md`
- Memory `0.5.36-client-log-audit-2026-08-19.md` (written pre-ship, still accurate)
