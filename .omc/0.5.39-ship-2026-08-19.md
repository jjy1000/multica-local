# 0.5.39 Ship Log — 2026-08-19

**Version**: 0.5.38 → 0.5.39 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Time**: 15:14–16:28 local (≈ 1h 14m: investigation + parallel agent batch + ship chain)
**Goal**: close dormant-GC bug + integrate 4 upstream ports + ship chain / MUL-6350 3/4 watch verdict

## Ship steps (all PASS)

| Step | Result |
|---|---|
| Pre-ship liveness (PG:5432 + server:8090) | PASS (PG 38085, server 38136 = 0.5.38 install, health 200) |
| pnpm typecheck | 6/6 PASS (views/web/desktop/core/ui/docs) |
| go test ./internal/... ./pkg/agent/... | PASS (33 packages, only failure: documented pre-existing mythos panic, unrelated) |
| pre-update snapshot | PASS (app .bak + PG backup written to ~/.multica/backups/) |
| migrate up | 2 new migrations applied (259 + 260 from MUL-6324 port); schema_migrations + pg_indexes verified |
| bundle-cli | PASS (server/migrate/multica + PG manifest + experimental manifests + Pythia + OpenScience + ClaudeScience) |
| electron-vite build | PASS 1.48s; renderer index 714 bytes, well-formed |
| electron-builder --dir | PASS (ad-hoc sign + skipped notarization, no deadlock) |
| 4a/7 renderer integrity | rawRequest 155 in asar (fresh renderer, content count stable) |
| cp -R → /Applications | PASS |
| desktop-sign-nested-binaries.sh | PASS (app + 3 nested binaries signed; nested `multica --help` exit 0, NOT 137) |
| verify-desktop-cold-start.sh | PASS: server 0.5.39 PID 56449, cold start ~2s, health ok |
| Post-ship row parity | workspace=7 / issue=346 / comment=2396 / agent=119 (vs pre-ship 7/345/2359/119 — +1 issue / +37 comment is natural agent activity during the ~14s cold-start window) |

## Commits shipped (7)

```
1629e1a26  fix(experimental): keep swarm_gc loop alive past router boot (dormant GC)
8bcb06f79  feat(server):     expose Go pprof on loopback port                       (MUL-6390)
a05b4f75f  fix(handler):    scrub NUL bytes on terminal task callbacks              (MUL-6310 gap)
57c84462b  feat(agent):     surface every in-flight built-in Hermes tool call       (MUL-5879 + BuiltinRuntime fence)
c42ae76ef  fix(chat):       guard chat rename during IME composition                (MUL-6387)
0f2abf2e4  chore(release):  bump 0.5.38 → 0.5.39 + release notes + CLAUDE.md sync    (pre-ship)
e9839c18b  feat(server):    reduce chat resume query load                            (MUL-6324 + migrations 259/260)
```

Net: 12 files committed across the functional batch (+ a single chore(release) sync commit). Migrations applied: 259 + 260 (MUL-6324 indexes; fork renumbered upstream 349/350).

## SKIPs (added to CLAUDE.md deferred list)

- `728ba9c58` OpenClaw discovery cache — SKIP-DEAD-CASE (fork missing `daemon/execenv/openclaw_config_cache.go`; related deferred MUL-6321)
- `24d8c521a` MUL-6364 LLM client retry budget — SKIP-DEAD-CASE (fork has no `pkg/llm/client.go`; LLM calls go through `RunProviderLLM` directly)
- `a35a75ab1` MUL-6341 fail-open entitlement policy provider — SKIP-DEAD-CASE (fork has no `server/internal/entitlement/` subsystem; fork has deleted all cloud / entitlement / billing surfaces)
- MUL-6350 plugin rebuild 1/4 / 2/4 / **3/4 hook engine branch** (`upstream/mul-6350/plugins-3-hook-engine`) — SKIP-DEAD-CASE for the whole series (~30k LOC wholesale; fork's user-plugin model is local subprocess + sandbox-isolated, zero overlap with upstream's 3rd-party iframe + HMAC-outbound architecture; 4/4 unmerged). Existing "watch series" deferral in CLAUDE.md stays as-is.

## Risk vs outcome

- **swarm_gc dormant GC** (commit 1): the most consequential — Contract #9 swarm cleanup never ran since 0.5.21. Same class as the 0.5.25 runtime_gc fix; CLAUDE.md "verify the sweep branch is actually reachable with a test, not by reading the code" lesson is now load-bearing for the second time.
- **MUL-5879 BuiltinRuntime fence** (commit 4): fork-local addition — hermes backend can be built-in or `protocol_family: hermes` custom profile (deferred MUL-5991 jcode path), so upstream's monolithic gate needed a runtime-type fence. Documents the pattern for future hermes work.
- **MUL-6324 fork deviation** (commit 7): MUL-5305 block (GetLatestChatTaskRolloutMissing query + PriorSessionResumeUnavailable field) intentionally omitted — fork has neither; multi-file port stays as future work. labels.go kept fork's alphabetical order; avoided upstream's wholesale reorg.
- **No 0.5.39 batch regression**: full pnpm typecheck 6/6, full go test 33 packages, all schema migrations applied, asar rawRequest count stable.

## Deferred to future batches

- MUL-6243 frontend UI close-out (1484 — separate audit, fork delta too large to bundle)
- #7038 revision-aware concurrency guard (35-file frontend concurrency layer; fork issue paths diverge)
- MUL-6273 MiniMax Code ACP runtime (decides whether to lift MUL-6327 mcode logo SKIP)
- MUL-6350 plugin rebuild 4/4 (when merged upstream + fork has a concrete outbound need)
- upstream new-commit scan beyond `aa6d0dadc` (last scan: 57 new commits through `9d6c0c81e` baseline)