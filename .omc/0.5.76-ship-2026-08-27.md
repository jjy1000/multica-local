# 0.5.76 — Upstream Integration Batch #2 (2026-08-27)

Branch: `epic/0.5.72-followups`. 4 commits on top of 0.5.75 (2 cherry-picks + version bump + ship log).

## Commits (oldest → newest)

| # | Commit | Scope |
|---|---|---|
| 1 | `22bc83ace` | merge: MUL-6658 task token rejection (Batch B) |
| 2 | `086ccf73e` | merge: #6081 autopilot runbook min-w-0 (Batch C2) |
| 3 | TBD | chore(release): bump 0.5.75 → 0.5.76 |
| 4 | TBD | docs(omc): add 0.5.76 ship log |

## Parallel-agent batch summary

5 worktrees + 5 general-purpose agents ran in parallel against upstream triage Batches A-D:

| Batch | Agent task | Result |
|---|---|---|
| A (4 CVEs) | `a57795fbc3ed6883e` | 0 applied — all ALREADY_PRESENT (fork commits `e57d0b57c`/`f5de3a927`/`d03025093`/`7a14de758`) |
| B (7 CLI/skill) | `af96833313b060bbb` | 1 applied (MUL-6658), 4 skipped fork-feature-missing, 2 ALREADY_PRESENT |
| C1 (4 UX chat) | `ac2d386ae6f8b8e41` | 0/4 applied — all skipped fork-surface-retirements |
| C2 (5 UX inbox/autopilot) | `a830eb8e6d92dc867` | 1 applied (#6081), 4 skipped fork-divergence or ALREADY_PRESENT |
| D (docs changelog) | `aad3e2597c2527d3c` | 0 applied — all CLAUDE.md §Localized Fork violations |

**Net result**: 2 clean surgical cherry-picks out of 21 attempted.

## Audit lineage

- Upstream triage baseline: `.omc/upstream-integration-triage-2026-08-26.md` (post-triage re-grade `79a787f80`).
- 0.5.75 ship log: `.omc/0.5.75-ship-2026-08-27.md` — 4 cherry-picks B2/B3 landed.
- This batch picks up at Batches A/C1/C2/D where 0.5.75 stopped.

## Test results

- `pnpm typecheck`: PASS (6/6 packages, cached 82 ms)
- `go test -count=1 -timeout 600s ./internal/... ./pkg/agent/...`: all packages green except 2 pre-existing failures (locked 0.5.74 + 0.5.75 batches)
- Targeted new tests PASS: `TestFormatErrorRejectedTaskTokenDoesNotSuggestAnotherCredential` + 6 `TestExtractServerMessage` subtests + 4 `TestHTTPErrorKind/status_4xx` subtests

## Risk vs outcome

| Dimension | Risk | Outcome |
|---|---|---|
| Batch A CVE presence | Low (already verified) | ✅ 4/4 ALREADY_PRESENT — no work needed |
| B MUL-6658 task token | Medium (test incompatibility) | ✅ surgical port; removed MUL-5372 test that refs fork-absent header |
| B 4 fork-feature-missing | High (would need wholesale port) | ⚠️ SKIPPED — defer to dedicated port cycles |
| C1 4 chat UX | High (fork surface retirement) | ⚠️ SKIPPED — chat-message-list/test deleted, build-steps rewritten |
| C2 #6081 | Low (surgical CSS) | ✅ clean 1-line addition |
| C2 4 inbox/i18n | Medium-high | ⚠️ 2 SKIPPED (wholesale), 2 ALREADY_PRESENT |
| D all changelogs | High (Cloud/billing/seat content) | ⚠️ SKIPPED — CLAUDE.md §Localized Fork violations |

## Row parity

NA — no schema changes, no migrations. Post-ship row parity inherits 0.5.75 baseline (workspace=1, issue≈84, comment≈477, agent=38).

## Known pre-existing issues (NOT this batch)

- **`TestDashboardPerAgentRollupsUseExactWindow`** — 900s delta leak in days=1 window. Bisected to `eb9d23704` baseline (0.5.74 batch). Not introduced by this batch.
- **`TestQuickCreateIssueParentTrustBoundary`** — pre-existing 80% flaky race. This batch touched 0 files in `quick_create_parent_test.go` (verified via `git diff dd4edb11b..HEAD -- server/internal/handler/quick_create_parent_test.go` → 0 lines).

## Files changed (cumulative)

```
M  CLAUDE.md                                                1 (release header)
M  apps/desktop/package.json                                1 (version 0.5.75 → 0.5.76)
A  .omc/release-notes-0.5.76.md                             1 (new)
M  server/internal/cli/client.go                            1 (MUL-6658)
M  server/internal/cli/client_test.go                       1 (MUL-6658 new tests)
M  server/internal/cli/errors.go                            1 (MUL-6658)
M  server/internal/cli/errors_test.go                       1 (MUL-6658 new tests)
M  packages/views/autopilots/components/autopilot-dialog.tsx 1 (#6081)
```

## Cleanup performed

- Removed 3 empty worktrees (`wt-batch-{a,c1,d}`) post-merge
- 2 surviving worktrees (`wt-batch-{b,c2}`) on merged branches
- 5 dead `tmp/cp-batch-*` branches (will be cleaned in next maintenance cycle)

## Next steps for user

1. **Ship the .app**: `bash scripts/ship-mac.sh --yes`
2. **Optional cleanup**: `git branch -D tmp/cp-batch-b-0723-2 tmp/cp-batch-c2-0723-2 tmp/cp-batch-a-0723-2 tmp/cp-batch-a-0723-2 tmp/cp-batch-c1-0723-2 tmp/cp-batch-d-0723-2`
3. **Optional**: remove `wt-batch-b` + `wt-batch-c2` worktrees
4. **Follow-up tickets**:
   - Batch 5 REVIEW-tier (8 large commits) — user decision required
   - Inbox arrow-keys + i18n no-value filter wholesale ports (deferred from Batch C2)
   - `TestDashboardPerAgentRollupsUseExactWindow` fix (deferred)
   - 41 docs-only changelog commits (deferred — same content violation reason)
