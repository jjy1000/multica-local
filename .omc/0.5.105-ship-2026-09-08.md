# 0.5.105 Ship Notes — 2026-09-08

**实验室审计修复批次**：外部 AI 对实验室功能的审计（H1-H3 高优 / M1-M6 中优 / 低优）逐条实证后修复。H3 处置经用户裁决 =「删除运行路径」。上一个版本 0.5.104（同日发）。

## 审计结论 vs 实证差异（比审计更重的）

- **H1**：不仅缺 flag 闸——`/api/experimental/semantica/decisions` 挂在认证组**外**的裸 router 上，`requestUserID` 裸读 `X-User-ID` 头，未认证调用方可伪造成员身份。审计判断「有成员 ACL 兜底，不是泄露」不成立：ACL 的输入本身就是可伪造头。
- **H3**：service/swarm 实测 1933 LOC（审计记 1425）；「manifest installable:null」不准确——10 份 manifest 均无该字段。
- **M6**：`multica-working-on-issues` 的 property 功能比审计说的更死——不仅 CLI 动词不存在，server handler / core API / `/api/properties` 路由全都不存在（纯上游功能），source-map 把 7 行 pin 到不存在的文件上。

## 修复清单

| # | 修复 | 关键文件 |
|---|---|---|
| H1 | semantica decisions 端点移入认证组 + `RequireExperimentalFlag("semantica")`（flag-off → 404）+ 回归钉 | `router.go`、`semantica_decisions_test.go` |
| H3 | swarm_topology 运行链路删除：service/swarm(1933)、swarm_run/swarm_routes/product_swarm_coordinator、`cmd_swarm`、桌面路由/视图/图组件、web 页、swarm.json ×4、FLAG_ROUTE_SUFFIX 行、leader 映射两侧、boot 供给；swarm_gc 通用半边重构为 `ResourceGC`；catalog 留 Frozen 墓碑；DB 表不动 | 见 commit `fix(experimental)` + `refactor(labs)` |
| H3 闸 | `experimental.IsFrozen` + `frozenLabSourceBindError`：新绑定 Create/Update 400、Batch per-issue skip | `catalog.go:727`、`handler.go`、`issue.go` ×3 闸点 |
| H2 | Go `TestCatalogFlagViewParity`（10 键钉住）+ TS FLAG_ROUTE_SUFFIX 精确键集断言；web LABS 补 causal-graph 卡片 | `catalog_view_parity_test.go`、`issue-labs-section.test.tsx`、web `experimental/page.tsx` |
| M1 | plugin-shell-view(~20 处) + issue-labs-section(4 处) 中文进 locale ×4 语言；`toLocaleString("zh-CN")` → 运行时 locale；沉睡的 `toast.*` 键启用（delete_success 带 reclaim 计数） | 两 tsx + experimental/issues.json ×4 |
| M2 | user_plugin PUT status 进程内生效：仅 active 行重注册，disabled/deleted 即时注销 | `user_plugins.go:459-479` |
| M6 | multica-pythia SKILL.md 10→4 动词（7 幻影）；working-on-issues property 节改写 upstream-only + source-map 行退役 | 两个 SKILL.md + source-map |
| M5 | lifecycle map 2026-09-08 附注（catalog 8→10、handler 5→7、ProxyRoutes 2→4、MythosService.Stop 有调用者、迁移过 164 等）；server/CLAUDE.md AutoDispatch 三件套修正；根双文件 dbstartup 条款替换（实测 `go build ./cmd/server/...` BUILD_OK）；Active Contract #5 改写为退役通告 | CLAUDE.md / AGENTS.md / server 双文件 / `.omc/labs-runtime-lifecycle-map.md` |
| M5 门 | docs-sync 新增第 6 类检查：两根文件 Current release 版本行必须一致——上线即抓到 0.5.104 vs 0.5.102 真实漂移 | `scripts/check-agents-docs-sync.mjs` |
| 低优 | lab-i18n-keys 侧边栏钉住 7/9 → 9/9（补 timesfm/causal_graph）；resources/pythia/engine/server.py 漏提交半边收尾 | 两 test/chore |

## 行为变化（用户可见）

- swarm_topology：侧边栏无入口（本来就无）、`/experimental/swarm-topology` 404、`multica swarm` 消失、API 路由 404、旧绑定 issue 不再显示状态 pill / 进度卡 / 深链；LabPicker 永不提供（HideFromIssueLabPicker + 退役闸双保险）。
- semantica decisions 端点：flag 关闭时 404（此前永远 200）；且必须携带有效 JWT。
- user plugin 停用：保存后立即从注册表消失（此前活到重启）。
- 插件管理 UI 在非中文 locale 下显示对应语言。

## 迁移 / 数据安全

- **零新迁移**。swarm 四张表（149/156×2 相关）原地保留；`experimental_resource_lock` 里的历史 `swarm_topology` 行继续被 ResourceGC orphan 清扫覆盖。

## 门禁与验证

- `pnpm typecheck`（全 turbo）：PASS（第一轮抓出 `res` 未赋值 + resources-types.ts swarm.json 残留 import 两处，修复后绿）。
- `pnpm test`（全量 vitest）：PASS。
- `go test -count=1 ./internal/... ./pkg/agent/...`：0 FAIL（DB 真跑）。
- `go build ./cmd/server/...`：BUILD_OK（M5 的 dbstartup 断言依据）。
- `node scripts/check-agents-docs-sync.mjs`：PASS。
- ship chain（snapshot → bundle-cli → build → package → 签名 → 冷启动 verify FATAL）：见下方结果行。
- Ship 结果: **PASS** — `bash scripts/ship-mac.sh --yes` 7/7 步全绿；冷启动 ~4s；安装版本（Info.plist）0.5.105；belt-and-suspenders: server PID 5357 绑定 :8090、`/health`=ok；asar 含 196 处 rawRequest（替换确认）；快照 `.omc/backups/2026-09-08-1150/0.5.105-ship`；整包回滚 `.bak` 已移 `~/.multica/backups/Multica.app.0.5.104.pre-update-20260908.bak`。

## Commit 序列

1. `chore(pythia)` — resources 引擎副本收尾（与 vendor 逐字节相同）。
2. `fix(experimental)` — H1 + H3 服务端 + H2 Go 钉 + M2（注：M6 的两个 SKILL.md 修复因同目录被这次 `git add server/` 带入）。
3. `refactor(labs)` — H3 TS 侧 + H2 卡片/TS 钉 + M1 i18n。
4. `docs` — M5 全部 + docs-sync 检查。
5. release prep + bump 0.5.105。

## 遗留（不阻塞）

- 审计低优「迁移 156 重名」保留现状（lifecycle map 已记录；golang-migrate 语义下两文件同名版本，重放环境需注意）。
- 8 个无 source-map 的 skill 未补（审计只要求修两个幻影技能）。
- Pythia lab 页无 issue 侧进行中态（0.5.104 已记录的 0.5.10x candidate）。
- claude-lab-view.tsx（desktop-only）仍有少量中文 fallback 字符串，危害低一档，未在本批处理。
