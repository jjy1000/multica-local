# 0.5.38 Ship Log — 2026-08-19

**Version**: 0.5.37 → 0.5.38 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Time**: 14:29–14:37 local (06:29–06:37 UTC)
**Goal**: fix agent-completed child issues not notifying their parent agent

## Ship steps (all PASS)

| Step | Result |
|---|---|
| Pre-ship liveness (PG:5432 + server:8090) | PASS (PG 98616, server 98708 = 0.5.37 install) |
| pnpm typecheck | 6/6 PASS (21.6s, 5 cached) |
| go test ./internal/... ./pkg/agent/... | PASS except documented pre-existing mythos panic (`TestTickSupervision_CompletionByFinalIssueStatus`, db.New(nil) stack — byte-identical at 0.5.34 baseline) |
| pre-update snapshot | PASS (app .bak `0.5.37.pre-update-20260819-143304.bak` + PG backup `pre-update-20260819-143304`) |
| migrate up | 0 new migrations, all skip |
| bundle-cli | PASS (server/migrate/multica + resources) |
| electron-vite build | PASS 1.49s |
| electron-builder --dir | PASS (ad-hoc sign + skipped notarization, no deadlock) |
| 4a/7 renderer integrity | rawRequest 155 in asar (same as 0.5.37 — no renderer change) |
| cp -R → /Applications | PASS |
| desktop-sign-nested-binaries.sh | PASS (3 nested binaries signed, multica exit 0) |
| verify-desktop-cold-start.sh | PASS: server 0.5.38 PID 38136, ~2s, row parity psql-unavailable (non-blocker) |

## Commits shipped

```
5a57a89a1  fix(daemon): notify parent when agent-completed child flips to done (+111, 2 files)
```

## Root cause (triple evidence)

- **Log**: JYF-352 workflow 12:21-12:28 — main agent polled child completion itself ("Both sub-issues are done"), expected re-trigger that never came.
- **DB**: children JYF-353/354 (parent set, stage barrier closed, squad assignee) completed via daemon path — parent got ZERO system comments, ZERO wake tasks 12:01→12:20.
- **Code**: `notifyParentOfChildDone` only wired into HTTP UpdateIssue / BatchUpdateIssues / GH-merge paths; daemon CompleteTask flips issue.status→done via direct SQL, bypassing it. Same gap upstream (fork-local fix).

## Risk vs outcome

- **Risk**: 2-file Go change in CompleteTask completion path; zero migrations, zero schema, zero TS. All guards of notifyParentOfChildDone unchanged (stage barrier, backlog park, idempotency, anti-loop).
- **Outcome**: agent-completed child issues now post the parent system comment + wake the parent assignee — the "必须人工对话拉取" gap is closed for the standard agent sub-task flow.

## Known boundary (documented, not fixed)

mythos lab auto-completion (service/mythos/{runner,supervise}.go direct-SQL flips) has the same gap; service layer can't call handler methods. Rare combination (lab issue as sub-task). Tracked in release note.

## Post-ship

- Release note `.omc/release-notes-0.5.38.md`
- Memory update: extend `0.5.36-client-log-audit-2026-08-19.md` or new memory — new session should read this ship log + the fix commit.
