---
name: 0.5.0-wave1-ship
created: 2026-07-31
updated: 2026-07-31
type: project
status: complete
---

# 0.5.0 wave 1 ship — 2026-07-31

One-line: schema-first wave 1 of 0.5.0 upstream integration — `client_usage_daily`
heartbeat + `task_usage` authoritative cost split — ports 6 forward-only
migrations and 4 query edits into the fork on a new integration branch
(`wave1-client-usage-2026-07-31`), 0 application-layer UI changes, 0 dashboard
wiring changes, 0 desktop rebuild.

## What shipped (in git, on `wave1-client-usage-2026-07-31`)

| Module | Commit | Files | Migration | Query | Handler / UI |
|---|---|---|---|---|---|
| **M1 client_usage_daily** | `461f052` | 21 (+744 / -98) | 5 (207-211) | 1 new | 1 new (UpsertClientUsage) + 1 route + 1 test |
| **M2 task_usage_authoritative_cost** | `4b5e77d` | 5 (+470 / -26) | 1 (213) | 4 edit | 0 |
| **Total** | 2 commits | 26 files | 6 migrations | 5 query changes | 1 new handler + 1 route |

### Latent sqlc regen (M1, picked up because sqlc must run end-to-end after adding schema files)

- `agent.sql.go`: reflects `disabled_runtime_skills` column from wave 2 migration 223
- `autopilot.sql.go`, `chat.sql.go`, `chat_input_ownership.sql.go`, `models.go`,
  `runtime.sql.go`: reflect the wave 2 schema (218-227) columns that have
  been live in production since 0.4.0 but were never re-generated (the
  0.4.0 release was tagged without sqlc regen; M1 closes that gap as a
  side effect)

## Schema-only ship (no handler UI for either module)

- **M1**: `client_usage_daily` handler exists and is wired at
  `POST /api/client-usage` (RequireHumanActor). It records the heartbeat
  but no client (web/desktop) is calling it yet — the renderer team
  ships the `client-usage` caller in a follow-up PR.
- **M2**: query interface change is live (5 new columns in the row
  structs). No dashboard handler reads them yet — `dashboard.go` is
  forked at 294 lines vs upstream's 624; the 330-line gap includes
  cost wiring + `foldRestrictedAgents` privacy fix + 2 new
  `failures/*` endpoints. Tracked as a separate deferred PR (see
  "Deferred to follow-up PRs" below).

## Pre-flight (per `conventions.mdx` + build sanity)

- `pnpm install` 干净 (16.8s, no postinstall script issues)
- `pnpm typecheck` **6/6 PASS** (16.0s; 5 cached + 1 fresh)
- `pnpm test` **8/8 PASS** (5.0s; 1282 + 321 desktop tests all green)
- `cd server && go build ./...` **PASS** (0 output, exit 0)
- `cd server && go test -count=1 -timeout 180s ./internal/...` **PASS**
  (handler 11.0 s, experimental 0.3 s, taskusagebackfill 0.6 s, etc.)
- `cd server && go run ./cmd/migrate up` **PASS**
  (207-211 all 'up', 213 'up', 218-227 all 'skip (already applied)')

## Production DB verify (after both migrations applied)

```
client_usage_daily|task_usage_hourly|t|228
```

- `client_usage_daily` table present, 0 rows (handler not yet called by any
  client — expected, the renderer caller is a follow-up PR).
- `task_usage_hourly` table present, 681 rows.
- `task_usage.cost_usd_ticks` is **all NULL** (1735/1735) — every
  pre-existing row is NULL per migration design; rows that report an
  authoritative cost in the future will set it.
- `task_usage_hourly.uncosted_input_tokens` is NULL on **228 rows**
  (those buckets haven't been recomputed since the split existed —
  the other 453 buckets were rolled up after the split and got
  concrete uncosted values).

### Row parity (golden baseline from 0.3.69 ship, identical to 0.4.0 ship)

- `workspace=1` / `issue=244` / `comment=1510` / `agent=96` /
  `user_plugin=0` — unchanged.
- `client_usage_daily=0` (new) — table present, empty by design.
- `task_usage=1735` / `task_usage_hourly=681` — task_usage row count
  unchanged; task_usage_hourly grew from 677 → 681 (+4 buckets) because
  the rollup that ran during this session re-aggregated four dirty
  buckets that had been written since the previous rollup, in line
  with the unique-key idempotency contract.

## Cloud boundary contract — preserved

All grep checks after this ship still show only the existing neutralization
harness hits; no new cloud-keyword leaks introduced by the wave 1 port:

```
$ grep -rE 'posthog|electron-updater|cloud-billing|cloud-runtime|contact-sales|GoogleLogin|autoUpdate' \
    server packages apps | grep -v node_modules
```

Hits remain in (a) structural neutralization (autoUpdateEnabled=false,
GoogleLogin returning 410, PosthogKey/Host DTO never read), (b) doc
comments, (c) test pinning the neutralization. No new production code
path now reaches into cloud services because of this port.

## Fork-first deviations from upstream

| Module | Deviation | Why |
|---|---|---|
| M1 | Dropped `queries.LockWorkspaceForChatSessionCreate` from UpsertClientUsage | Upstream uses it to serialize client-usage reports against chat-session delete/create on the same workspace; the fork has no chat-session delete race, so the advisory lock is unnecessary. Workspace existence is still validated by `GetMemberByUserAndWorkspace`. Inline comment in the handler documents the rationale. |
| M2 | Did not port `ListDashboardFailuresDaily` / `ListDashboardFailuresByAgent` queries | No handler caller in the fork; porting queries alone would be dead code. Tracked in deferred follow-up. |
| M2 | Did not port the dashboard.go wire-up of the new cost / uncosted columns | The 330-line diff between fork and upstream includes a `foldRestrictedAgents` privacy fix that depends on `restrictedAgentIDs` query (itself a 0.5.0 wave 3+ candidate). Deferring the wire-up keeps this wave purely additive in the production row format. |

## Deferred to follow-up PRs (track in `.omc/plans/upstream-integration-0.5.0-proposal-rev2.md`)

| Item | Why deferred | Risk if cherry-picked alone |
|---|---|---|
| `dashboard.go` cost column wire-up | `foldRestrictedAgents` privacy fix needs `restrictedAgentIDs` query (wave 3+) | High — public API for `/api/dashboard/*` would change |
| `ListDashboardFailuresDaily` / `ListDashboardFailuresByAgent` queries + `/api/dashboard/failures/*` routes | Need dashboard handler scaffold + new error-rate UI | Medium |
| `client_usage` renderer caller | The web/desktop clients need to POST a heartbeat on activity — needs API design + 4-locale i18n | Low |
| Wave 2 (212 `agent_service_tier`) | Affects chat dispatch path | High — needs feature flag |
| Wave 3-5 (VCS / channel_media / quick_actions) | Cloud dependencies and feature flag work | High — separate plan |
| Wave 6+ (comment + search trgm + chat pinned) | Touches search path | High |

## Risk vs outcome

- **Schema-only** additive migration; no row loss; no schema rewrite.
- **`IF NOT EXISTS` guards** on every ADD COLUMN / CREATE INDEX — re-runnable.
- **NULL semantics** on `cost_usd_ticks` and `uncosted_*_tokens` preserve
  today's behaviour for any code that doesn't read them yet.
- **Dropped `LockWorkspaceForChatSessionCreate`** removes an upstream
  workaround for a fork-irrelevant race; documented inline.
- **No application-layer behavior change** in any module that existed
  before this ship.

## Cold-start three-check (re-verify after the wave 1 dev session)

Not run as part of this ship — this is a **git-only release** (no .app
rebuild, no DMG, no binary swap). The user did not request a 0.5.0
desktop ship. The next ship that rebuilds the .app should run:

```bash
bash ~/.multica/scripts/pre-update-snapshot.sh
cd server && go run ./cmd/migrate up
pnpm --filter @multica/desktop bundle-cli
pnpm --filter @multica/desktop build
pnpm exec electron-builder --mac --dir
bash scripts/desktop-sign-nested-binaries.sh /Applications/Multica.app
bash ~/.multica/scripts/verify-desktop-cold-start.sh
```

## Three-check pass (git-only sanity)

- `git log wave1-client-usage-2026-07-31 ^epic/0.4.0-integration` →
  2 commits (M1 + M2), both authored with Co-Authored-By
- `git diff --stat epic/0.4.0-integration..HEAD` → 26 files changed
- `pnpm exec playwright test` → not run (no UI change in this release;
  the renderer caller is a follow-up PR)

Installed: git branch `wave1-client-usage-2026-07-31` = **0.5.0-wave1** ready
(integration baseline; not yet tagged — see below). Rollback: `git reset
--hard epic/0.4.0-integration` to return to 0.4.0 + the worktree stash
(preserve for review before discarding).
