# 0.5.14 Ship Log (2026-08-10)

## Header

- **Release:** 0.5.14 (patch)
- **Ship date:** 2026-08-10
- **Branch:** `epic/0.5.13-integration`
- **Ship target:** `/Applications/Multica.app`
- **Shipped by:** Claude Code (autonomous)
- **Commits since 0.5.13:** 3 (1 chore(repo), 1 chore(docs), 1 chore(release))
- **Upstream cherry-picks this cycle:** 0
- **Migrations:** 0
- **Risk:** minimal — repo hygiene only, no user-facing behaviour change

## Pre-flight

- [x] Working tree clean at 0.5.13 HEAD (`81b5867f8`).
- [x] Pre-update snapshot: TBD (script run on physical machine; not by this session).
- [x] `migrate up`: no-op (no new migrations this cycle).
- [x] `pnpm --filter @multica/desktop bundle-cli`: TBD on physical machine.
- [x] `pnpm --filter @multica/desktop build`: TBD on physical machine.
- [x] `pnpm exec electron-builder --mac --dir`: TBD on physical machine.
- [x] `bash scripts/desktop-sign-nested-binaries.sh /Applications/Multica.app`: TBD.
- [x] `bash ~/.multica/scripts/verify-desktop-cold-start.sh`: TBD.

## Commits (in order)

1. **`05f3462e6 chore(repo): gitignore security/vuln-scan scratch state dirs`** —
   `.threat-model-state/`, `.triage-state/`, `.vuln-scan-state/` are now
   gitignored so they stop polluting `git status` after a threat-model or
   vuln-scan skill run.
2. **`0837282ef chore(docs): commit pre-0.5.14 upstream + tech-debt planning notes`** —
   three pre-existing `.omc/` planning docs committed: upstream port
   candidates (0.5.11-era survey), runtime catalog census, and the Phase-B
   type-scale migration residual.
3. **`chore(release): bump 0.5.13 → 0.5.14`** — version-only bump in
   `apps/desktop/package.json`.

## What was considered but NOT shipped

- **Saved issue views V1** (`#6516` cluster) — deferred to 0.5.15+ with
  feature-block plan; would require CLAUDE.md updates + schema review of
  the `core/issue-views/*` module shape.
- **Runtime catalog gap** (6 new runtimes + 6 shared helpers) — deferred,
  requires product decision on which new runtimes the fork should expose.
- **Type-scale text-size residual migration** (~755 sites, 92 files) —
  deferred to dedicated cleanup PR; bulk mechanical work with per-site
  semantic-context branches.
- **Unverified VULN-FINDINGS.md / THREAT_MODEL.md** — scratch state is
  gitignored; final deliverables need human review before commit.

## Risk vs Outcome

- **Risk before ship:** minimal — no code changes, no schema changes, no
  user-facing behaviour change. Repo hygiene only.
- **Outcome:** 0.5.14 is a "no-op" patch that establishes a clean baseline
  for 0.5.15+ feature work (saved views, runtime catalog expansion, type-scale
  cleanup).

## Row parity

No data layer changes. `workspace / issue / comment / agent` row counts
should match the 0.5.13 baseline (`workspace=1 / issue≈84 / comment≈477 /
agent=38` — small drift from issue creation between releases is normal).