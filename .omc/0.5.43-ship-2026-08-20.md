# 0.5.43 Ship Log — 2026-08-20

**Version**: 0.5.42 → 0.5.43 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Time**: ~ 2h local (5 cherry-pick commits + 1 chore(release) + 1 post-ship docs + ship chain)
**Goal**: security-first batch — close 3 fork-lagging CVE inheritance gaps + 1 channel supervisor context leak

## Ship chain results (canonical `bash scripts/ship-mac.sh --yes`)

| Step | Result |
|---|---|
| 1. pre-update-snapshot | PASS — `.omc/backups/2026-08-20-1410/0.5.43-ship/` (HEAD=937dd888c, dirty=false, files=0) |
| 2. migrate up | NOT REQUIRED (zero migrations this batch) |
| 3. bundle-cli | PASS (server/multica/migrate + PG manifest + experimental manifests + Pythia + OpenScience + manifests) |
| 4. electron-vite build | PASS; renderer index well-formed (4a/7 check: 714 bytes, starts with `<`) |
| 5. electron-builder --dir | PASS (ad-hoc sign + skipped notarization, no deadlock; projectDir=apps/desktop) |
| 5a/7 renderer integrity | asar OK: 155 rawRequest occurrences (fresh renderer, 12.65 MB main chunk) |
| 6. install /Applications | PASS (cp -R, overwrites 0.5.42) |
| 6a/7 nested-binary re-sign | PASS (app + 3 nested binaries signed; nested `multica --help` exit 0, NOT 137) |
| 6b/7 local backup | PASS — `.omc/backups/2026-08-20-1410/0.5.43-ship/` (HEAD=937dd888c, dirty=false, files=0) |
| 7. cold-start verify | PASS: cold start ~6s, Info.plist = 0.5.43, server PID 93299, /health ok |
| Post-ship row parity | `psql-unavailable` (psql client not in PATH — 0.5.16 established precedent, non-blocker) |

## Installed binary

```
multica 0.5.43 (commit: 937dd888c, built: 2026-08-20T14:09:23Z)
```

## Live process state (post-ship)

| Process | PID | Status |
|---|---|---|
| Multica.app (Electron main) | 92338 | 🟢 alive |
| Multica Helper (GPU) | 92836 | 🟢 alive |
| Multica Helper (Network) | 92837 | 🟢 alive |
| Multica Helper (Renderer) | 92839 | 🟢 alive (`--no-sandbox` for renderer; fork's <webview> tag takes the sandboxed path; renderer sandbox SKIP deferred to 0.5.44 audit) |
| `server` (bundled Go binary) | 93299 | 🟢 LISTENING :8090 |
| `postgres` (native PG 17.4) | 93081 | 🟢 LISTENING :5432 |

## Commits shipped (5 functional + 1 chore(release) + 1 post-ship docs)

```
b1017bdeb chore(docs): consolidate release history 0.5.12-0.5.41 into .omc/_legacy/archive
3c667e68c fix(deps): upgrade builder-util-runtime to 9.7.0 (CVE-2026-54673)
087f9c6f5 fix(deps): upgrade postcss to 8.5.26 (CVE-2026-45623)
7c57432ae fix(deps): upgrade brace-expansion (CVE-2026-69152)
c66c81896 fix(channel): cancel completed supervisor contexts (MUL-6461)
1fd554b13 chore(release): bump 0.5.42 → 0.5.43 + release notes
```

## Skipped from the planned 6-commit batch

| Commit | Reason | Follow-up |
|---|---|---|
| MUL-6406 auth fail-fast (`658b0b7d9`) | Already ported fork-locally — `jwtSecretBootError` + `os.Exit(1)` present in `server/cmd/server/main.go:138-187` (and `server/internal/auth/jwt.go` has `ValidateJWTSecret` + `insecureJWTSecrets` denylist). Fork's `defaultJWTSecret` is `"multica-dev-secret-change-in-production"` (different from upstream's `"change-me-in-production"`). `git log -S 'jwtSecretBootError'` returns no commits because the code was added as a fork-local backport outside the tracked history. | Verify the existing fork implementation matches upstream's threat model; bump `defaultJWTSecret` to a stronger fork-local value if needed. |
| Renderer process sandbox (`8f48c380d`) | `sandbox: false` → `sandbox: true` in fork's `apps/desktop/src/main/index.ts:226`. Conflict: fork's `createRendererWebPreferences` is INLINED inside `createWindow()` (not extracted) and has fork-specific fields (Claude Science `<webview>` tag, PDFium plugin commentary). Upstream's `navigation-guard.ts` doesn't exist in fork (functionality inlined into `index.ts`). | **0.5.44 renderer-sandbox-audit batch** — dedicated forensics pass on the fork's `webPreferences` for Claude Science + `setWindowOpenHandler` chain + `plugins: true` (PDFium) interaction with sandboxed preload. The auto-mergeable `electron.vite.config.ts` change (bundle `@electron-toolkit/preload`) is safe to land standalone. |

## Per-file diff vs upstream stat

| Commit | Upstream stat | Fork port | Notes |
|---|---|---|---|
| 3c667e68c CVE-2026-54673 | 2 files, +11 / -9 | 2 files, +10 / -8 | Clean (single overhead line) |
| 087f9c6f5 CVE-2026-45623 | 2 files, +39 / -32 | 2 files, +37 / -48 | net +37/-48 vs +39/-32: fork's pnpm dep tree had more transitive postcss resolutions to migrate |
| 7c57432ae CVE-2026-69152 | 2 files, +23 / -15 | 2 files, +22 / -14 | Clean (one overhead line) |
| c66c81896 MUL-6461 | 2 files, +70 / -2 | 1 file, +8 / -2 | Dropped upstream test file (`+62` LOC) due to fork's combined `InstallationStore` (lease methods live in `store.AcquireWSLease`); porting test requires splitting store into two interfaces, which is its own PR. The 8 LOC bug fix lands cleanly. |

## Upstream-poll methodology

Cherry-pick candidates identified by `git log --oneline main..upstream/main --grep="MUL-" --no-merges` (459 commits / 367 unique MUL IDs). 6 selected for 0.5.43 batch (3 CVE dep pins + 1 supervisor fix + 1 auth fail-fast + 1 renderer sandbox). 4 of 6 landed (3 CVE + MUL-6461); 2 SKIPPED with documented rationale (MUL-6406 already-ported, renderer sandbox complex port).

## Gates

| Gate | Result |
|---|---|
| `pnpm typecheck` (full turbo) | 6/6 ✅ (38.16s) |
| `go test -count=1` `./internal/... ./pkg/agent/...` | 32/33 ✅ |
| `internal/integrations/channel/engine` (MUL-6461) | PASS (1.069s) |
| `internal/handler` (handler suite) | PASS (14.191s) |
| `pkg/agent` (agent suite) | PASS (10.313s) |
| Pre-existing mythos panic | `TestTickSupervision_CompletionByFinalIssueStatus` (CLAUDE.md documented, unrelated to this batch; stack trace is `mythos/supervise.go:277` calling `GetIssueStatusEntryByKey` with nil inner — pre-existing test fixture issue) |
| `pnpm install` lockfile regen | 5.9–7.6s, 0 errors (peer-dep warnings pre-existing) |
| `scripts/check-agents-docs-sync.mjs` | PASS (5 guarded constraint categories) |

## Security posture change

Before this batch, fork's lockfile had:
- `builder-util-runtime@9.5.1` → vulnerable (CVE-2026-54673, Electron-builder CVE)
- `postcss@8.4.31` → vulnerable (CVE-2026-45623, PostCSS CVE)
- `brace-expansion@1.1.13` / `2.0.3` / `5.0.4` → ALL VULNERABLE (CVE-2026-69152, ReDoS / shell injection)

After this batch:
- `builder-util-runtime@9.7.0` (pnpm.overrides pin)
- `postcss@8.5.26` (pnpm.overrides pin)
- `brace-expansion@1.1.18` / `2.1.4` / `5.0.9` (parent-scoped pnpm.overrides)

Plus the channel supervisor bug fix prevents a small child-context leak per contended lease acquisition.

## Deferred to 0.5.44

- **Renderer sandbox audit** (`8f48c380d`) — independent batch with full forensics pass, including the auto-mergeable `electron.vite.config.ts` change (bundle `@electron-toolkit/preload`) as a separate commit from the `main/index.ts` change.
- **MUL-6406 verification** — confirm the existing fork implementation matches upstream's threat model; if not, bump `defaultJWTSecret` to a stronger fork-local value.
- **Tier 2 candidates** (next cherry-pick batch): MUL-6472 dispatch leak, MUL-6445 skill cache failures, MUL-6310 NUL bytes, MUL-6417 follow-ups, MUL-6461 supervisor (already landed), CJK markdown, MIME revision conflict, MUL-6085 TTL, MUL-6456 last activity index, MUL-6151 comment index, MUL-6451 webhook labels.
