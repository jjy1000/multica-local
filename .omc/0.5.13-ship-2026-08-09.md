# 0.5.13 Ship Log — 2026-08-09

## Summary

Upstream integration ship: re-introduced daemon fail-fast startup errors
(#5674), ported search cancelled-demotion (#6515), added the CLI `--compact`
comment-read flag (#6546), and closed two audit gaps (custom_args writer +
subscriber entity filter). Plus the first backup-slimming snapshot
(`pg_dump -Fc`, 24 MB vs the old ~1.5 GB per-table CSV set). Baseline:
`epic/0.5.12-cherry-pick` (contains #6194 rollup, #5406 ErrNoRows, #6095 WCAG,
#5355 self-heal, #6124 open-tab). `/Applications/Multica.app` = 0.5.13.

## Commits (on `epic/0.5.13-integration`)

```
6439d87f1 fix(agent): close the custom_args writer + subscriber filter audit gaps
d63761264 feat(search): #6515 — demote cancelled issues and projects below live work
aedefd9f4 fix(cli): #5674 — re-introduce daemon fail-fast with actionable startup errors
```

(Baseline `fe154b49d` = 0.5.12 branch head; the two audit-fix files ported
from the 0.5.12 WIP tree are folded into 6439d87f1.)

## Ship steps

1. **pre-update-snapshot.sh** — PASS with the NEW `pg_dump -Fc` step:
   - PG snapshot 24 MB single file (was ~1.5 GB of per-table CSVs, ~62x
     smaller). Archive verified: 691 TOC entries, 91 table-data entries.
   - pg_dump path auto-resolution added: bundled Postgres.app 17.4 bin first
     (PATH's Homebrew pg_dump 14.19 refuses a 17.10 server), fallback to PATH.
   - Retention unchanged (3 pre-update dirs, 2 KB copies, 1 .app.bak); pruned
     old 8/6 dirs per policy.
2. `go run ./cmd/migrate up` — all applied (238 head), no new migrations.
3. bundle-cli — 3 Go binaries + 452 migrations verified + Pythia/OpenScience/
   manifests staged.
4. electron-vite build — ok (1.40 s renderer).
5. electron-builder --mac --dir — ok (no deadlock; ad-hoc signed).
   - asar verify: 125 `rawRequest` ✓, unpacked 3 binaries ✓.
6. cp -R → /Applications + desktop-sign-nested-binaries.sh — app + 3 nested
   binaries signed; `multica --help` exit 0 ✓.
7. Cold start — first `open` attempt did not bring up the app (no crash
   report; likely launch race after `rm -rf`+`cp`), second `open` brought the
   full stack up in ~8 s. server PID 31678, Info.plist 0.5.13 ✓.

## Risk vs outcome

| Risk | Outcome |
|---|---|
| fail-fast re-introduction conflicts with the 0.5.12 revert | Hand-fused onto the pre-revert base; single-sink err-log localization; all 8 fail-fast tests green |
| `--compact` changes agent-visible JSON | Opt-in only; fork templates intentionally NOT reworded (default output byte-identical) |
| search ORDER BY placeholder drift | cancelledRank inserted between WHERE and rankExpr; $N indexing verified by 4 backend tests + 12 helper tests |
| errLogOffset duplicating logOffset (vestigial crash sink) | Retained upstream two-sink struct; fork-localized to one file; test `TestDaemonStartupFailureError` writes separate err.log that production can't produce — documented, accepted |
| audit M1 (server-body reflection in preflight errors) | Fixed: URL userinfo stripped + status-code-only rendering |
| audit High (agent_creation_expert custom_args `{}` re-seeds WARN) | Fixed; grep confirms 0 remaining `{}` writers |
| 2 pre-existing views test failures (issue-detail scroll-to-comment) | Verified failing on clean baseline (stash) — unrelated to this ship |

## Three-check + row parity (20:24 UTC+8)

- 5432 postgres ✓, 8090 server ✓, `/health` → `{"status":"ok"}` ✓
- Row parity: workspace 1 = 1 ✓, agent 105 = 105 ✓, issue 303 / comment 2109
  (only grows since 0.5.12's 300/2090 — expected drift).
- Daemon: cold-start 401 on the stale desktop token matches the 0.5.12 cold
  start pattern (8/6 23:23:15 → recovered after GUI login 23:23:31); the
  desktop daemon runs `--foreground` (runDaemonForeground), which the
  fail-fast change does not touch — no regression. Daemon expected to come up
  after the user signs in.

## Follow-ups

- `.omc/release-notes-0.5.13.md` — full fix notes.
- Backups: new snapshots now 24 MB each; the pre-0.5.13 history (~1.5 GB of
  CSV dirs) can be tar.gz-archived to reclaim space when the user confirms.
- The daemon cold-start auth flow (desktop profile token vs GUI login) is a
  known pre-existing surface (memory `0.2.97-daemon-autostart-regression`).
