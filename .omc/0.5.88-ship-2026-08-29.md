# 0.5.88 — mythos engine unification + labs delegation loop + frozen metadata + user-plugin taxonomy + UX polish

**Ship date**: 2026-08-29
**Branch**: `epic/0.5.72-followups`
**Baseline**: `d0b89de82` (0.5.87+0.5.88 dev-record header refresh)
**Bump**: `81fd1fc8f` (0.5.86 → 0.5.88)
**Pre-ship review fixes**: `c39fbeb2b` (frozen-lab delegate gate) + `41c1f0a30` (leader-lookup deadline)
**Target**: `/Applications/Multica.app` = 0.5.88 ✅ (cold start ~6 s, server PID 83196, server version 0.5.88)

## What shipped

Full inventory in [`.omc/release-notes-0.5.88.md`](release-notes-0.5.88.md).
Commit set `19d4c506f..41c1f0a30` (12 commits):

| # | Commit | Content |
| --- | --- | --- |
| 1 | `19d4c506f` | feat(server): 0.5.87 — mythos async-engine unification (reaper, per-tick timeout, t=0 heartbeat) |
| 2 | `8d0cf35f6` | fix(web)+i18n: 0.5.87 — built-in-lab audit fixes (web stubs, locale keys, picker comment drift) |
| 3 | `b28715c66` | feat(server): 0.5.88 — delegation loop closure (lab delegate --parent, result comment, causal edge, briefing), frozen flags, user-plugin taxonomy |
| 4 | `4250a41a1` | feat(client): 0.5.88 — labs UX/animation polish + user-plugin form interaction-model contract |
| 5 | `b3924668b` | docs: 0.5.88 dev record — container vision gap-closure |
| 6 | `f2467873e` | test(handler): 0.5.88 — determinize quick-create daemon-version gate |
| 7 | `98fd95138` | fix(server): 0.5.88 — briefing/delegate contradiction (skip AutoDispatch=false labs) |
| 8 | `eb48f6a24` | docs: 0.5.88 — full verification chapter |
| 9 | `d0b89de82` | docs: header refresh for 0.5.87 + 0.5.88 dev cycles |
| 10 | `81fd1fc8f` | chore(release): bump 0.5.86 → 0.5.88 |
| 11 | `c39fbeb2b` | fix(server): 0.5.88 — lab delegate rejects frozen labs (never-disagree parity) |
| 12 | `41c1f0a30` | fix(server): 0.5.88 — bound each delegation-brief leader lookup with its own deadline |

## Gates (run pre-ship, serial, quiet env)

- `pnpm typecheck` (full turbo): **6/6** (31.724 s, 5 cached).
- `go test -count=1 -timeout 600s ./internal/... ./pkg/agent/...` with `DATABASE_URL` exported from repo-root `.env`: **exit 0, 38 packages ok, 0 FAIL** (handler DB-backed suite ran live, ~18.8 s).
- Pre-ship review (workflow `wf_f456438f-489`, 4 dimension agents + adversarial verify): **0 ship-blockers**; 3 confirmed fix-after-ship/contract-violation findings → 2 fixed pre-ship (commits #11/#12, each pinned/compiled into the gate), 1 logged to the 0.5.89 ledger:
  - **#1 (deferred) — mythos reaper boot sweep races ResumeSupervision on stale `supervising` rows** (`service/mythos/reaper.go`): at boot, `ResumeSupervision` revives a >1h-stale `supervising` run (fresh goroutine, async t=0 heartbeat persist) while the reaper's immediate boot sweep can condemn the same row (reads the still-stale `last_check_at`). Window is small; impact is a wrong status in the supervise panel / status bounce, no data loss. Fix (reaper skipping rows currently in `superviseSet`, or a synchronous heartbeat stamp before resume returns) lands in 0.5.89 with a dedicated test.
- Targeted after fixes: `go test ./cmd/multica/` (incl. new `TestRunLabDelegateRejectsFrozenLab`) + `./internal/service/causal_graph/ ./internal/experimental/` — all ok.

## Risk vs outcome

- No new schema migrations (only `mythos_run.sql` query additions + sqlc gen) → forward-only contract untouched.
- No flag-key changes; `swarm_topology` stays frozen, `pythia_oracle`/`timesfm` stay `AutoDispatch=false`.
- The 2 pre-ship fixes are pure additive gates on the delegation path (fail-fast + per-lookup deadline) — they ship inside the bundled `multica`/`server` binaries (bundle-cli recompiled with them).
- Known remaining (next cycle 0.5.89 ledger): daemonless lab-install NULL `runtime_id`; `CompleteTask`/`FailTask` on `dispatched` no-op; invisible LabPicker chip; Unknown-Agent assignee chip; plugin `title_en`; `TestInstallTimesfm` scoping; + the reaper/resume race above.

## Row parity + cold-start (post-ship)

- Cold start: **~6 s**; server PID 83196 (started 2026-08-29 22:27:45, etime 00:02); `Info.plist` = 0.5.88 = `package.json`.
- `verify-desktop-cold-start.sh`: **PASS** (reported `row parity: psql-unavailable` — psql was not on the background shell's PATH; parity re-checked manually below).
- Manual row parity (`PGPASSWORD=multica psql -U multica -d multica -h 127.0.0.1 -p 5432`): **workspace=7 / issue=408 / comment=2588 / agent=132** — live accumulated counts since the 0.5.86-era gold baseline (1/84/477/38); 0.5.88 ships ZERO migrations, so no count moved as a result of this release.
- Local backup snapshot: `.omc/backups/2026-08-29-1427/0.5.88-ship/` (manifest + diff.patch + log + status).
