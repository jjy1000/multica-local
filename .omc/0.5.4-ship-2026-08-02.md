# 0.5.4 Ship Log — 2026-08-02

**Version**: 0.5.4 (`apps/desktop/package.json`, Info.plist verified 0.5.4)
**Branch**: epic/0.3.58-cherry-pick (local project, no cloud push)
**Type**: UX 修复 + 包路径清理

## 内容
- **LabPicker click-through 修复** (`packages/views/issues/components/pickers/lab-picker.tsx`): 0.5.4 把 `agent_creation_studio` 改成了"点 chip = 弹 RecentLabsPanel 只读预览"的二次点击 UX。智能体创建本质是一个 lab 团队(leader `agent_creation_expert`),用户期望"直接开始",不应该被强制多一次点击。
  - **flag on**: 选 `智能体创建` → 直接 bind `issue.lab_source='agent_creation_studio'`,server 0.3.46 P0#4 契约自动改写 assignee 为 `agent_creation_expert` 派单,**一步到位**
  - **flag off**: 选 `智能体创建` → 仍弹 RecentLabsPanel(因为 leader 未装,直接派会被 server 400),但文案改为"Enable the Agent Creation lab in Labs settings to dispatch new tasks"——明确告诉用户去 Labs 设置启用 flag 后再回来派单
- **RecentLabsPanel 入口未在 flag on 路径暴露** —— 用户不再被多余的只读子菜单打断
- **i18n 文案同步** (`pickers.lab.recent_panel_hint`,4 语言 zh-Hans / en / ja / ko):文案重写,让 flag off 场景下用户能理解"先启 flag 再回来"的解锁路径
- **测试更新** (`lab-picker.test.tsx`): 3 个 0.5.4 case → 0.5.4.x click-through contract(flag on 直接派单 / flag off 弹 panel / 关闭重置 view)
- **历史清理**: 本次 commit 顺带包含 0.5.3 时代删除 `/experimental/agent-creation-studio` creator page 的 diff(已在 0.5.4.studio 提交,但本 ship 一起 ship)

## Ship 步骤
1. ✅ pre-update-snapshot(`pre-update-20260802-125606`, app 883M + KB 6846 md,PG 5433 prod-skip)
2. ⏭️ `go run ./cmd/migrate up` —— **本次改动无 .sql migration,跳过**(ship log 留 audit trail;PG 5432 当时未启,即便想跑也连不上)
3. ✅ bundle-cli(0.5.4 三二进制 + 448 migrations + 全部 manifest;Pythia + OpenScience + claude-science)
4. ✅ electron-vite build(1.40s,index-BmyVS82H.js)
5. ⏭️ `electron-builder --mac --dir` —— **app-builder-bin 5.0.0-alpha.13 ENOENT bug 触发**:`stat '/Users/jiangjianyan/jjy/multica-main/apps/desktop/resources/claude-science/agents'`(0.3.63 之前的 stale 路径硬编进 alpha.13 binary,即使清掉 stale `dist/` 也无法绕过)。**走 manual asar repack fallback**(CLAUDE.md 已批准的 0.3.63+ 路径)
6. ✅ manual asar repack:从 0.5.3 装的 `/Applications/Multica.app` extract `app.asar` → 覆盖 `out/{main,preload,renderer}` + `resources/bin/{multica,server,migrate}`(chmod +x)→ `asar pack` → 部署到 /Applications/ + Info.plist `CFBundleShortVersionString 0.5.3 → 0.5.4`
7. ✅ desktop-sign-nested-binaries.sh(app + 3 nested binaries,自检 `multica --help` exit 0)
8. ✅ 冷启动三查:`/Applications/Multica.app` Info.plist 0.5.4 = package.json 0.5.4、5432/8090 LISTEN ≤ 6s、/health ok
9. ✅ row parity(5432 启后):workspace=1 / issue=252 / comment=1518 / agent=96 —— workspace 不动,issue/agent 漂移来自 0.5.2 以来 self-opt 每周报告 + install 幂等 upsert

## 风险 vs 结果
- **asar fallback 路径风险**: 0.3.63 已验证过(CLAUDE.md "Ship chain fallback: manual asar repack (0.3.63+)"),本次重新走一遍,Info.plist 必须手改 `CFBundleShortVersionString` / `CFBundleVersion` —— 0.5.4 二进制 + Info.plist 0.5.4 一致
- **app-builder-bin alpha.13 ENOENT**: 跟 0.3.63 的 alpha.12 deadlock 是同类问题(预编译 binary 含 fork 0.3.63 之前的 stale 路径)。**0.5.4 后续 ship 应直接走 manual asar repack 作为 canonical**,electron-builder `--dir` 不可靠;或者升级 app-builder-bin 到稳定版
- **多 arch 兼容性**: 0.3.63 ship log 提到 v0.4.3/v0.4.4 的 Intel (x64) DMG crash,本次未触发(只打 arm64,`electron-builder.yml` 已有 `- "!dist/**"` 防御)
- 行数漂移同 0.5.3:issue 84→252(0.5.2 以来 self-opt + 用户新增)、agent 38→96(多次 install 幂等 upsert)

## 已知
- 无已知回归;`pnpm typecheck` 6/6 子包过,`@multica/views test` 1286/1288 过(2 个 timing-flaky 跟本改动无关,baseline 同样 fail)
- 该项目本地化 fork,不推云端(用户明确)
- **后续 ship 优化建议**: 升级 `app-builder-bin` 到 5.x stable,或直接弃用 electron-builder、改用全 manual asar repack
