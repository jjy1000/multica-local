# mcp_overlay.go — 决策记录 (0.3.3 audit)

## 决策: 删除 dead code

**日期**: 2026-07-12
**触发**: 0.3.3 comprehensive audit 发现 P1-2 (`mcp_overlay.go` 0 caller 0 test)

## 删除原因

1. **本地无 per-task MCP overlay writer** — Composio 被本地化策略剔除 (CLAUDE.md "Localized Fork" + memory `multica-fork-vs-upstream-divergence-map.md` §2.2)
2. **0 caller** — `daemon.go:1303-1321` 仍直传 `agent.McpConfig`, 无 merge 调用
3. **0 test** — 官方 9 测试未移植, 函数实装正确性未验证
4. **文件头注释撒谎** — "handler layer calls mergeMCPOverlay" 但代码无 caller

## 当前状态

- 文件已 rename 到 `mcp_overlay.go.disabled` (178 行)
- 不参与 Go build (`*.disabled` 不被 Go 识别)
- 可逆: `mv .disabled` 即可恢复

## 恢复条件

未来如要恢复, 必须**同时满足**:

1. 出现 per-task MCP overlay writer (如重引入 Composio 或新的 OAuth/MCP 集成)
2. 移植官方 9 个 `mcp_overlay_test.go` 测试 (覆盖合并 / collision / malformed / server-entry 校验)
3. 改 `daemon.go:1303-1321` 调用 `mergeMCPOverlay(agent.McpConfig, task.RuntimeMcpOverlay)`
4. 加 claim 集成测试验证 wire shape

## 关联

- `.omc/incidents/2026-07-12-0.3.3-comprehensive-audit.md` §P1-2
- `memory/0.3.3-audit-and-fixes-2026-07-12.md`
- upstream `Downloads/multica-main/server/internal/handler/mcp_overlay.go` (178 行原文保留作参考)