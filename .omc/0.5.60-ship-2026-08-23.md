---
name: 0.5.60-ship-2026-08-23
created: 2026-08-22T19:50:00Z
updated: 2026-08-22T19:50:00Z
---

# 0.5.60 ship — 2026-08-23

Labs × issue 集成审计批处理(6 并行审计 agent + DB ground truth)→ 3 P0 +
5 P1/P2 修复 + ship 完整性 + 文档漂移修正。

## Ship 步骤

1. `scripts/ship-mac.sh --yes` 全程通过(快照 → migrate 274 → bundle-cli →
   electron-vite → electron-builder --dir → cp -R → nested 签名 → cold start)。
   Info.plist 0.5.60,标签漂移(0.5.58)修正。
2. ship 后 API 触发验证抓到 0.5.59 事件真根因:**defer 参数即时求值捕获空
   slice 头** → `6bdf5fd63` 闭包化 + 端到端钉扎测试。
3. 热修部署: 重建 3 个 Go 二进制(bundle-cli)→ 拷入已装 .app asar.unpacked
   → `desktop-sign-nested-binaries.sh`(multica --help exit 0)→ 重启。
4. 再次触发: 3 轮完整消费 → `persist run OK rounds=3` + 行落库;提前断开 →
   部分落库(1 轮),两种路径都验证。
5. semantica wheel 首建(`uvx --from build pyproject-build`,系统 python 被
   PEP 668 管)→ 提交 `8fe19fd10` + 拷入已装 .app。

## Risk vs outcome

- 风险: batch mutex 新分支误伤合法批量操作 → 3 子测试钉扎(含合法路径
  成功用例),全量 handler 测试与基线一致。
- 风险: migration 274 大删除误删活锁 → NOT EXISTS 只删无主行,删后
  lock 312 行全为活资源(0 孤儿),visibility 26 行同理。
- 结果: 全部按预期;两个预先存在失败(非本批)维持记录:
  `TestDashboardPerAgentRollupsUseExactWindow`(时间窗断言,已 bisect 至
  基线)+ mythos supervise panic(0.5.59 起记录)。

## Row parity

workspace=7(未动),issue ≈350(测试 +2,均带清理),
pythia_forecast_run 0→2(验证产物,保留),
experimental_resource_lock 3418→312,visibility 434→26。

## 提交栈 (a410baa00 之上,11 个)

`ab5c9ba97` semantica rawRequest · `ce3ea85df` batch swarm mutex ·
`8d760a296` lab_mode 校验 · `7341a423a` 孤儿清扫 · `9567cc301` stamp 键 ·
`fa178e9e3` swarm 描述符 + mythos picker · `6c117433c` catalog 对齐 ·
`286ca8f71` semantica 资源/mig273 · `8e3330f0a` 文档 · `d582746a4` bump ·
`6bdf5fd63` defer 闭包 · `8fe19fd10` wheel
