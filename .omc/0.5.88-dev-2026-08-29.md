# 0.5.88 dev record — 2026-08-29（分支 epic/0.5.72-followups，NOT packaged）

主题：**实验室 = 容器：委托闭环 + 分类法覆盖外置插件 + 动效/信息架构打磨**。
起点是用户 2026-08-29 的容器愿景陈述（低代码容器容纳内置+外置插件；内置插件独立使用+锁分配者；agent 自动配合（因果）与显式委托（构思推演→结果回传）两种协作；动效集合优化）。代码盘点结论：四支柱中三根已有实装，缺口集中在委托闭环收尾、分类法未覆盖 user plugins、一批动效欠账——本周期全部补齐。

三个工作流由三个并行/接力专家代理实现，文件集互不相交，最终合并树统一过门禁。

## WS1 — 委托闭环（server/CLI）

委托骨架 `multica lab delegate` 已存在（建 lab_source issue → 轮询 task-runs → stdout 输出），但结果只到 stdout、委托 issue 是孤儿、主 agent 不会被唤醒。本周期接上三根线：

1. **`lab delegate --parent [--stage]`**（`server/cmd/multica/cmd_lab.go`）：`--parent` 接受 issue key/UUID/UUID 前缀，解析完全镜像 `issue create` 的 `resolveIssueRef`（一种方言，不造第二套）；`--stage` ≥1 进入 stage 屏障组。委托成功后 **best-effort 把结果评论回传父 issue**（`postDelegateParentComment`，走 `issue comment add` 同一 client 路径，`[lab delegate] <flagKey> · <childKey> <title>` 头 + rune 安全 2000 字截断）；评论失败仅 stderr 警告，退出码仍反映委托结果。plain 模式 stdout 保持纯回复文本，child key 走 stderr。
2. **唤醒免费拿到**：child 带 `parent_issue_id` 后，既有 `handler/issue_child_done.go`「子终态 → 系统评论 mention 父 assignee」管道自动生效——**零守卫改动**，新 pin `TestChildDoneNotifiesParentForDelegatedLabChild` 证明 lab 形状（parent in_progress + agent assignee + lab child 空 assignee）会触发 mention + 恰一个 pending wake task。
3. **因果留痕**（`service/causal_graph/delegate_edge.go` 新文件 + `handler/issue.go` 创建成功路径挂接）：`parent_issue_id + lab_source` 同时存在时记 **parent --depends_on--> child** 边（既有 CHECK 集，无新边类型无迁移）；`Recorder.ensureIssueRootNode`（自 resolveTriggerNode 提取，行为等价重构）保证两端节点存在；any-status `FindCausalEdgeBetween` 幂等探针（0.5.84 ICP-5 never-nag）；provenance `{"source":"issue_delegate"}`；每条错误路径 WRN 返回，**issue 创建永因果写失败**。
4. **委托发现**（`service/causal_graph/delegate_brief.go` 新文件 + `handler/daemon.go` claim 注入）：`## Available Labs (delegation)` 小节列出 workspace 已启用的 assignee 型实验室（跳过 frozen 与 roster 型 mythos——无 leader 只会超时）+ 可复制的 `multica lab delegate --parent <issue-id> <flag-key> "<task>"`。2000 字节上限、200ms 预算、错误路径全返空串（claim_brief 同款隔离）；leader 解析器注入自 handler 既有 `resolveLabLeader`（不复制第三张表）；`context.WithoutCancel` 使查活在 claim 请求取消后仍完成。不挂在 causal_graph 旗下——该节宣传的是委托而非因果，空集即空节。
5. **DB pin**：`TestCreateDelegatedLabChildRecordsCausalDependsOnEdge`（端点/provenance/重放幂等）+ `TestDelegationBriefListsEnabledAssigneeLabs`（空集/列出/frozen 永不出现）。

## WS2 — 冻结元数据（server）

`catalog.go` Flag 增 append-only 字段 `Frozen bool` / `SuccessorKey string`（omitempty，镜像同 struct 惯例）；`swarm_topology` 置 `Frozen:true, SuccessorKey:"mythos_swarm"`——0.5.86 整合从此机器可读：委托 briefing 跳过 frozen lab（WS1 联动），设置页出横幅（WS3 联动），toggle 行为刻意不变（advisory 元数据而非第二道启用闸）。新 helper `FlagByKey`（user-plugin 层优先，镜像 InteractionModelOf 分层）。wire payload `ExperimentalFlagResponse` 两字段映射（内建+user-plugin 双循环）。Pin：`TestCatalogFrozenContract`。

## WS3 — 前端动效与信息架构（packages/views + apps/desktop + core types）

动效惯例：motion/react v12 + `UI_EASE_OUT`/UI_MOTION_DURATION tokens + `useReducedMotion()` 门控（因果画布既有的做法）。

1. **LabProgressCard**（`lab-progress-card.tsx`）：三处轮询 loading `return null`（弹入+布局抖动）→ 紧凑三行 Skeleton（对齐 lab-output-panel 惯例）；状态 dot+label+summary 以 AnimatePresence fade-through 按状态 key 过渡（同状态内 5s 轮询刷新不重播）；reduced-motion 即时切换。
2. **IssueLabsSection / LabLastResultChip**：分区展开收起 `height:0→auto` + opacity 动画（initial=false 不闪首帧）；chip 因 line-clamp 由 CSS 控高，改用同款 fade-through（记录在案的偏离）。
3. **设置页 labs-tab**：interaction_model 徽章（独立工作型紫 / 辅助协作型 sky / 未分类不显示）；`flag.frozen` → 琥珀 Snowflake 横幅「已冻结 — 由 {successor} 接替」，successor 标签经 `resolveSuccessorLabel` 同列表解析（fallback 原始 key）。toggle 不变。core 类型加 `frozen?`/`successor_key?`（zod .loose() 下无服务器也安全）。
4. **因果图**：加载从 spinner 整面替换 → 画布保持挂载 + `GraphLoadingOverlay` 蒙层（空态门在 `!isPending && nodes.length===0`，深度切换不再重挂）；zoom 按钮/wheel/reset 走 ~180ms rAF lerp cubic ease-out（拖拽平移刻意保持 1:1 即时——audit 引用的调用点本就是按钮+滚轮；pollPaused 拖拽语义原样）；NodeDetail 按 node.id fade-through；SuggestedQueue confirm/reject 乐观更新（失败原位回滚）+ sonner 成功/失败 toast。
5. **锁错误 UX**：`matchAssigneeLabLockError`（`assignee-lab-lock.ts`，对服务端三条 verbatim 消息的稳定子串匹配，返回 {labSource, leaderName, leaderInstalled}）+ use-issue-actions / issue-detail 子 issue 路径换定向 toast（标题命名实验室，描述给指引）；其余错误保持原样。
6. **删除死代码**：`LeaderRewriteConfirmDialog` 整体移除（0.3.45.8 遗物，`confirmRewrite` 全仓零调用方；非 Radix、无焦点陷阱，与现行为矛盾——picker 选中 lab 已自动清 assignee）；连带 `experimental.json` 的 `lab_picker.confirm_rewrite_*` 5 键（4 语）。
7. **顺带修复**（触碰文件内的既有 lint error）：`issue-labs-section.tsx` AgentTrustCorrectButton 硬编码「纠正智能体」→ 接 `t` + `issues.json` `lab_section.trust_correct_button`（4 语）。

## WS4 — 分类法覆盖外置插件（server + form）

user plugins（`user_<slug>` manifest 实验室）正式纳入 interaction-model 法律：

1. **契约**（`plugin_scanner.go`）：manifest 增 `interaction_model`（absent→`auxiliary`，行为保持：未分类用户插件从不锁，auxiliary 也不锁）与 `leader_agent`（**仅当** `interaction_model=="assignee"` 时必填非空，create/update 400 拒绝 assignee-without-leader）；legacy `capabilities.leader` 保留为读侧回退（老插件 leader-rewrite 不断）。`ParseUserPluginContract` + `UserPluginInteractionModel`/`UserPluginLeaderAgent` 读侧 helper；`UserPluginToFlag`（scanner 与 handler 两处）stamps `Flag.InteractionModel` → 注册表、`InteractionModelOf`、`FlagByKey`、flags payload 全链路解析。
2. **锁路径**（`handler/issue.go` + `service/issue.go` 双侧同步——0.5.86 双表法律）：user-plugin 分支改读 **DB 行**（`GetUserPluginByFlagKey`，过滤 soft-deleted）解析 model+leader；缺失/已删行 → auxiliary → 不锁（删除的插件永不再锁，比注册表更真实）。内建分支逐字节不动。语义与内建一致：空 assignee 放行→leader-rewrite 填充；手动 assignee 必须= leader **AGENT ROW**；leader 行缺失 → install-first 400。
3. **表单**（`user-plugin-form-dialog.tsx`）：分类选择器（默认辅助协作型）+ 选中独立工作型时必填 leader agent 输入（hint：需 workspace 内存在该 agent 行）；manifest 合并保留其余键（降级 auxiliary 时去除 leader_agent）。`user-plugins-section.tsx` 独立工作型琥珀徽章。core 类型：`UserPluginManifest` 接口替换 `Record<string, unknown>`。
4. **DB pin**：`TestUserPluginInteractionModelContract`（校验 400 create+update 双侧；flags payload 呈现契约；(a) 空 assignee→201+leader-rewrite 填充；(b) 手动非 leader→400 点名 leader；(c) leader 行缺失→install-first 400；leader 本人 allow-path；update 改回 400；降级 auxiliary 释放锁）。scanner 侧 11 例解析测试。共享 dev DB 纪律：key 定向 purge（前后双向）+ t.Cleanup。

## 验证（最终合并树）

- `go build ./... && go vet ./...` 干净；`go test ./internal/handler/ -count=1`（DATABASE_URL 导出，真实 DB 跑 18.7s）**0 FAIL**（TestInstallTimesfm 环境性 flake 本轮未复现）；experimental/service/causal_graph/cmd/multica 定向包全过。
- 遗留 pin 重跑（0.5.86 教训）：TestChildDone|TestStageBarrier|TestStageProgress|TestCausalGraph|TestIssueDependency = 28 PASS 0 FAIL 0 SKIP。
- `pnpm typecheck` 6/6；packages/views 全量 vitest 1806 passed / **42 failed 全部为 HEAD 既有**（stash 对照法证实：纯净 HEAD 同样 42 failed、同样 9 文件、同 2 unhandled errors——inbox-page 29（MUL-6632 上游分歧区）、issue-detail 3（lab-output-panel ClaudePanel）、description-preview 2、math 2、chat-input 2、markdown-paste 1、highlight 1、skills-tab 1、integrations-tab 1）；三个工作流触碰组件的测试全绿；locale 4 语对称。
- eslint：触碰文件 0 error（2 warning 为 HEAD 既有）。

## 已知遗留 / 下周期

- `TestInstallTimesfm` 全局计 `experimental_resource_lock`（无 workspace 过滤）的脆弱性仍未修（本轮未复现）；修法=按 workspace scope。
- views 既有 42 失败（见上，与本周期的改动无关）——inbox 大头挂在 MUL-6632 决策门上。
- `ScanUserPluginDir`（Phase-1 磁盘发现，已被 DB 路径取代）未同步契约解析——如需统一再说。
- 因果图 SuggestedQueue 乐观化已做；suggestion 自动确认/拒绝自动化（historian/verifier）仍在 0.5.87+ 清单。
- 下周期候选：DB-backed `ClaimTaskByRuntime` handler 集成测试（子图路径激活）；Tier C Pythia hypothesis→evidence 闭环；mythos 委托形状的 e2e（CLI→child→wake→回传全链路真跑一轮）。

**本周期的产品含义**：agent 在 briefing 里能看见可委托的实验室并拿到可复制命令；委托出去的子任务在因果图上留 depends_on 痕、完成即回传评论并唤醒主 agent；frozen 实验室对 agent 与人同时不可再委托；用户自建插件与内置插件共用同一套独立/辅助分类与锁语义。容器愿景的三根断线全部接上。
