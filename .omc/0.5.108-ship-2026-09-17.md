# 0.5.108 ship record — 2026-09-17

**发版内容 = 同日第二波上游移植批**（分诊+执行账本：
[upstream-sync-2026-09-17.md](upstream-sync-2026-09-17.md)，9 提交 5 port /
4 skip；用户可见说明：[release-notes-0.5.108.md](release-notes-0.5.108.md)）。

## 提交链（epic/0.5.72-followups，基于 0.5.107 的 `7d188cbdc`）

| 提交 | 内容 |
| --- | --- |
| `bd9f7ce50` | feat(agent): MUL-7358 opencode ≥1.1.54 注册地板（+zh/en 文档行） |
| `837e78f32` | fix(editor): #8517 空 mention picker 放键 |
| `1bd867093` | fix(desktop): MUL-7312 PATH 兜底只追加不前置（path-fallback.ts） |
| `c48608845` | fix(daemon): MUL-7458 drain 排序修复（sealPendingLocked + 新回归测试） |
| `e32ae4e32` | fix(chat): MUL-7458 settled 渲染 canonical 答案（copy-text + chat-message-list + 新测试） |
| `2b5a47fbd` | fix(agent): MUL-7312 codebuddy 前台强制 + 后台事件守卫 |
| `4ca6ff6db` | docs: 上游同步账本（9 提交分诊 + MUL-7409 方案归档 + PG 事故记录） |

## 代码门禁（`4ca6ff6db` 上逐项实测）

- `go test -count=1 ./internal/... ./pkg/agent/...`（DATABASE_URL 已导出）：
  40 包 ok / 0 FAIL / 0 DB 静默跳过。
- `pnpm typecheck` 6/6；`pnpm lint` 8/8。
- views 全量 vitest：1855 passed / 33 skipped（= 既定 parked MUL-6632 家族）；
  desktop 全量 vitest：390 passed / 48 文件。

## 同日运维背景（不在本包内，见账本）

12:26 打包 app 采纳的外部救场 PG 被外部 SIGTERM，app 僵尸 8.5h（/health 恒
ok、daemon 401、snapshot 404，全天 0 个 5xx）；21:05 pg_ctl 恢复，daemon 一个
心跳周期自愈，数据完整（8 workspace / 461 issue）。四个产品缺口待立项：
/health 探 DB、运行中 PG 自恢复、DB 错误映射 5xx、宕机期 renderer 卡死 +
renderer.log 不随窗口重建续写。

## ship 链结果（装后回填）
