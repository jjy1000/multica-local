# 0.5.47 Ship Log — 2026-08-21 (pending ship)

**Version**: 0.5.46 → 0.5.47 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Status**: pre-ship — version bumped + release notes + headers updated, ship chain NOT yet run
**Goal**: schema migration sweep + MUL-6472 landing

## Commits (5 functional + 1 chore release + 1 post-ship docs pending)

```
db1dab786 feat(0.5.47): port upstream autopilot_quota migrations + queries
5acfe7e44 feat(0.5.47): sqlc regen for autopilot_quota migrations
243e45638 feat(0.5.47): port clientErrorMessage helper (MUL-6472 dep)
f4c48a8b4 fix(autopilot): don't leak server 5xx body to run-now toast (MUL-6472)
ba3d96918 feat(0.5.47): add minimal pkg/pluginruntime stub (MUL-6310 unblock)
```

## Schema changes (8 migrations applied)

Migrations 261-268 sequenced to continue from fork's 260. Created 2 new tables:
- `autopilot_quota_period` (with index, primary key)
- `autopilot_quota_reservation` (with indexes, primary key)

Plus 5 supporting indexes (262, 263, 264, 265, 267, 268) + 1 unrelated webhook index (266).

Verified: `go run ./cmd/migrate up` applied all 8 cleanly. `sqlc generate` produced 393 LOC of new sqlc code.

## First user-facing cherry-pick since 0.5.44

**MUL-6472 dispatch error leak** landed via minimal fork-style port. The security fix:
- Server: `slog.Error(...) + writeError(w, ..., "failed to trigger autopilot")` (no `err.Error()` in 5xx body)
- Client: `clientErrorMessage(e) || localized` (no 5xx message in toast)

Fork deviations: skipped upstream's quota branch (fork's quota subsystem is sentinel-only), skipped upstream's tests (entitlement deps + jsdom), applied minimal catch-block change in fork's `autopilot-detail-page.tsx` instead of upstream's `dispatchReasonCode` pattern.

## Gates

| Gate | Result |
|---|---|
| `go run ./cmd/migrate up` | PASS (8 migrations, 260 → 268) |
| `sqlc generate` | PASS (393 LOC new) |
| `go build ./...` | PASS |
| `pnpm typecheck` (full turbo) | 6/6 ✅ |
| `go test -run TestTriggerAutopilot` | PASS |
| Full server test | 32/34 (1 pre-existing flaky dashboard test + 1 pre-existing mythos panic) |
| Pre-existing flaky test | `TestDashboardPerAgentRollupsUseExactWindow` (data integrity issue, unrelated to MUL-6472) |
| `scripts/check-agents-docs-sync.mjs` | PASS (5 guarded categories) |

## Live state (NOT yet 0.5.47)

- Installed: `/Applications/Multica.app` = **0.5.45** (last shipped)
- DB: 268 migrations applied (next cold-start will use 0.5.47 binaries)
- Working tree: 16 untracked migration files in `apps/desktop/resources/server/migrations/` (build artifacts from bundle-cli)

## Deferred to 0.5.48

- **MUL-6310 caller integration** — main handler task.go still has 8+ upstream sqlc queries (GetActiveAutopilotRuleVersionParams, GetWorkspaceAttributionFailClosed, etc.) + `protocol.ChatCancelFinalizedPayload` + `taskfailure.ReasonSkillBundleUnavailable`. Schema work unblocked but more sqlc + protocol additions needed.
- **MUL-6472 quota branch** — the `*service.AutopilotQuotaExceededError` 429 branch is deferred; fork's quota subsystem is sentinel-only.
- **MUL-6417 follow-ups** — daemon workflow refactor + skill prompt changes.
- **CJK markdown integration** — `cjk-emphasis.ts` into fork's `readonly-content.tsx`.
- **Pre-existing flaky test** — `TestDashboardPerAgentRollupsUseExactWindow` audit needed.
