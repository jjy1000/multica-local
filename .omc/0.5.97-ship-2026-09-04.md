# 0.5.97 Ship Log — 2026-09-04

**结果:ship 7/7 一次通过,cold-start PASS ~6s,`/Applications/Multica.app` = 0.5.97。**
提交范围:`ce67ef1c9..909402eb3`(`f3dfedcb8` 功能 + `909402eb3` bump/notes)。
干净发版,无事故;本记录按惯例留档门禁口径与余项。

## 内容

实验室结果渲染全面优化(TS-only:无迁移、无 Go 改动,详见
[`.omc/release-notes-0.5.97.md`](release-notes-0.5.97.md)):
桌面实验室页删「打开对话」+ 新增 LatestResultPanel 实验结果面板(结果优先);
Issue 卡片两动作分工(查看结果渲染=原地展开 / 在实验室查看完整记录=run 级深链);
渲染收敛到共享三模块(`lab-task-result-view` / `interactive-chart-envelope` /
`lab-attachment-sanitize`),顺手关掉 LabOutputPanel 图表退化 JSON 的遗留 TODO。

## 发版过程(干净,要点留档)

- **带 app 运行发版**(0.5.92 铁律):ship 前 0.5.96 packaged app 在跑、
  bundled Postgres(:5432)在线,step-2 migrate no-op 通过(本周期无迁移)。
- 门禁口径(发版前,全 uncached):typecheck 6/6、lint 8/8、
  views vitest 1832 passed / 33 skipped(MUL-6632 停放)。
  本周期无 Go 改动,未跑 Go 套件;5 个相关测试文件 96 用例定点全绿。
- ship 门禁逐项:4a index.html 798B 首字节 `<`;5a asar rawRequest 204 处;
  5b 数据区金丝雀逐字节一致(main/index.js + package.json JSON 有效);
  6a 嵌套 Go 二进制重签后 `multica --help` exit 0(非 137);
  7 冷启动 ~6s,Info.plist 0.5.97 = 源 package.json 0.5.97。
- **冷启动验证器软提示留档**:「Row parity: psql-unavailable」——验证器环境
  PATH 无 psql,行对齐检查跳过,PASS 判定仍由版本 + health 支撑。非缺陷,
  但若未来要恢复行对齐,需在验证器里用
  `/opt/homebrew/opt/postgresql@17/bin/psql`(PATH 上的 pg_dump 是 v14,
  同 [[pgdump-17-binary-path]] 记忆中的版本错配)。
- 回滚副本:`/Applications/Multica.app.0.5.96.pre-update-20260904-192955.bak`
  已按用户偏好移至 `~/.multica/backups/`(/Applications 只留正式版);
  仓库级备份 `.omc/backups/2026-09-04-1134/0.5.97-ship/`(manifest+diff+status)。

## 子域文档同步

`packages/views/CLAUDE.md` 新增 **"Lab run result rendering (0.5.97)"** 一节
(唯一共享渲染器 / interactive-chart 必须走 InteractiveChartEnvelope /
agent 附件必过 sanitize / 双动作契约 / `i18next/no-literal-string` +
`noUncheckedIndexedAccess` 两条 lint 陷阱),AGENTS.md 镜像再生成,
`scripts/check-agents-docs-sync.mjs` ✓。

## 遗留 / 下周期

- `issue-detail.test.tsx` 的 CI 5s 超时 flake(0.5.96 期 run 33608082929 /
  33610251863 假红)仍是待办:给该重型文件提高 `testTimeout`。本地本次
  以 `--testTimeout=30000` 跑绿;仓库默认值尚未改。
- 无其他新增遗留;此前账本(CompleteTask dispatched no-op、mythos reaper
  race、MUL-6632 决策门等)不变。
