# 0.5.93 Ship Log — 2026-08-31

> **Shipped**: `/Applications/Multica.app` = **0.5.93**（MCP 管理提升为侧边栏「配置」组一级入口，Skills 之下）。Chain: `bash scripts/ship-mac.sh --yes` exit 0（app 保持运行 → Postgres 在线，未再触发 0.5.92 首航的 step-2 中止）。Branch: `epic/0.5.72-followups`，commits `d6601de08`（nav promote，13 files）+ `b1ec61ff2`（bump 0.5.92 → 0.5.93）。

## 背景

0.5.92 发布后约一小时，用户看图反馈：MCP 管理藏在「设置 → 工作区 → MCP」太深，"移动到设置下面功能栏 Skills 下面这样好一些"。当日即时迭代。

## 变更

- `NAV_PAGE_REGISTRY` configure 组新增 `mcp` 条目（Skills 与 settings 之间，`Plug` 图标）；`paths.ts` 加 `mcp: () => \`${ws}/mcp\``。
- `mcp-sync-tab.tsx` 从 `packages/views/settings/components/` git-mv 至 `packages/views/mcp/components/`（组件字符串仍在 settings.json 命名空间 `mcp_sync.*`，键已随 0.5.92 发布于四语言，避免再次翻动 locale）；新增 `McpPage`（PageHeader + McpSyncTab，去除 tab 内重复标题）。
- 设置页 `?tab=mcp-sync` 注册移除（移动而非复制）。
- 路由：web `[workspaceSlug]/(dashboard)/mcp/page.tsx`；desktop routes `path: "mcp"`；`packages/views/package.json` exports 加 `./mcp`。
- i18n：四语言 `layout.json` 的 `nav` 组加 `"mcp": "MCP"`（插在 skills 后，镜像注册顺序）。

## 验证

- typecheck 6/6；发版时 app 在线（DB 真跑）。
- 安装包核验（asar 解包/二进制 grep）：desktop 路由 `path: "mcp"` ×1；nav 注册 `key: "mcp"` ×2；四语言 `"mcp": "MCP"` ×16；设置页 `value: "mcp-sync"` ×0（旧 tab 确认移除）。
- cold-start：`PASS: cold start verified, server is 0.5.93`（~6s）。
- `/Applications` 清理：`Multica.app.0.5.92.pre-update-20260831-155856.bak` 已移至 `~/.multica/backups/2026-08-31-1602/`，安装目录只留正式版。

## 遗留

同 0.5.92 ship log（v1 边界：仅 claude provider / user-scope / 无 per-agent 排除开关；views 存量 lint 8 errors；上游移植账本不变）。
