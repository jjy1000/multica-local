---
name: 0.3.65-ship-2026-07-29
created: 2026-07-28T16:14:54Z
updated: 2026-07-28T16:14:54Z
---

# 0.3.65 ship — 2026-07-29 (Pythia PAT fix + lab flow redesign, SHIPPED)

`/Applications/Multica.app` 0.3.65 cold-start verified. Fixes the
Pythia / 科研实验室 direct-Q&A 500 (llm-call rejected the user's PAT) and
redesigns the lab-plugin flow (all labs lock the assignee + show the
lab test agent; 「创建实验室」 disabled while a lab is bound). See
`release-notes-0.3.65.md` for the change set.

## Ship steps (canonical order, all green)

1. `pre-update-snapshot.sh` — PASS (App .bak 774M + PG logical backup
   13 tables + config 80 files + KB 6809 md; tag
   `pre-update-20260729-000800`).
2. `cd server && go run ./cmd/migrate up` — no new migrations (168
   already applied in 0.3.64); ran clean per the mandatory step.
3. `pnpm --filter @multica/desktop bundle-cli` — 3 Go binaries at
   version=0.3.65 + migrations + Pythia/claude-science resources staged.
4. `pnpm --filter @multica/desktop build` — electron-vite OK (one benign
   INEFFECTIVE_DYNAMIC_IMPORT warning on emoji-picker).
5. **From `apps/desktop/`**: `rm -rf dist && pnpm exec electron-builder
   --mac --dir` — exit 0, ad-hoc signed. (First attempt run from the
   repo root packaged the WRONG app — root `dist/`, "index.js not found
   in archive". Re-ran from `apps/desktop/`, the canonical cwd.) Manual
   asar-repack fallback NOT needed.
6. Quit old app (8090 freed; stale pythia venv process killed so it
   respawns with fresh env) → `rm -rf /Applications/Multica.app` +
   `cp -R dist/mac-arm64/Multica.app /Applications/` → codesign app
   (`--force --deep`) + 3 nested binaries (`multica`, `server`,
   `migrate`) individually → `multica --help` exit=0 (NOT 137).

## Risk vs outcome

| Risk | Outcome |
|---|---|
| electron-builder alpha12 deadlock (macOS 27) | Did not occur; clean exit 0 |
| electron-builder from wrong cwd | Hit it (root vs apps/desktop); corrected, no damage |
| Gatekeeper SIGKILL on unpacked Go binaries | Re-signed all 3; `multica --help` exit 0 |
| Stale asar | `grep -a -c rawRequest app.asar` = 119 → fresh renderer |
| Pythia fix not reaching a provider | `claude` on server child PATH; bridge returns real text |

## Cold-start three-check + row parity

1. Listeners: 5432 (postgres) + 8090 (server) both up.
2. `GET /health` → `{"status":"ok"}`; cold start ~12 s to listener-up.
3. **Pythia bridge verified live**: `POST /api/runtime/llm-call` with the
   real profile PAT → `200 {"text":"1+1 等于 2。"}` (was 401 → the /chat 500).
4. Row parity vs the pre-update backup (CSV-parsed, exact):

| table | backup | live | |
|---|---|---|---|
| workspace | 1 | 1 | OK |
| issue | 238 | 238 | OK |
| comment | 1426 | 1426 | OK |
| agent | 92 | 92 | OK |

`user_plugin=0`, `schema_migrations=298`. Zero drift. (issue is 238,
up from 237 at the 0.3.64 ship — the user's Pythia test issue; backup
and live agree, so no drift.)

## Notes

- Version bump 0.3.64 → 0.3.65 (patch; one server fix + one product
  redesign, no schema change).
- Working tree left uncommitted (user did not ask to commit): version
  bump, release notes, ship log, plus the Pythia + lab-flow diff.
