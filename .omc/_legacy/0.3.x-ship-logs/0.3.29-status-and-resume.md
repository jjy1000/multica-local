# 0.3.29 — Labs Closure (resumed, blocked on Token Plan quota)

Status: 2026-07-16T00:11:44Z — three of four sub-agents were killed by
HTTP 429 "已达到 Token Plan 用量上限". Pythia forecast finished;
Claude Lab / Mythos / UI polish worktrees exist but carry no committed
delta yet. We are paused waiting for the user's plan reset or upgrade.

## Goal (unchanged from 0.3.29 goal)

1. Remove the settings page buttons "集成 / Git / 更新".
2. Claude Research Lab dynamic visualization — issue pick → lab agent
   locked → runtime / artifact / skill loop → live render in the lab
   surface. Low-code experiment execution.
3. Forecast engine — same loop on Pythia. Chinese UI, default 1 round,
   additional interactive ops (horizon / persona / regenerate / new
   scenario).
4. Swarm topology — the only lab where the user CAN still pick extra
   agents. Strengthens loop / self-reflection / skill learning. Left
   sidebar shows the swarm-enabled badge.
5. Lab tasks gain a right-side chip in the issue panel pointing at the
   corresponding lab route.
6. Test, ship, package, replace /Applications/Multica.app.

## Sub-agent status

| Branch | Worktree | Status |
|---|---|---|
| `feat/0.3.29-pythia-forecast` | `~/worktrees/0.3.29-pythia-forecast` | **committed `3c765bf`** ✅ |
| `feat/0.3.29-claude-lab` | `~/worktrees/0.3.29-claude-lab` | running tree only, no commit, killed by 429 |
| `feat/0.3.29-mythos-swarm` | (none — agent killed during initialization) | pending |
| `feat/0.3.29-ui-polish` | `~/worktrees/0.3.29-ui-polish` | unchanged (still on main) |

## Pythia forecast — completed delta

**Go**
- `server/internal/handler/forecast_issue.go` — new
  `POST /api/experimental/pythia-oracle/forecast/issue` with SSE per
  issue, default 1 round, max 3.
- `server/internal/handler/forecast_issue_test.go` — envelope contract
  tests (issue_id / scenario_context / lab_source).
- `server/internal/handler/claude_lab_forecast.go` — `forecastEnvelope`
  extended; `syntheticForecast` and `queryOracle` mark `lab_source`.
- `server/cmd/server/router.go` — gates new route on
  `experimental.DefaultFor("pythia_oracle")` (full bypass when off).

**Renderer**
- `apps/desktop/src/renderer/src/components/pythia/pythia-report-surface.tsx`
  — replaces legacy Pythia view; subscribe / horizon / persona /
  regenerate / new scenario form; parses SSE prediction blocks.
- `apps/desktop/src/renderer/src/components/pythia/pythia-report-surface.test.ts`
  — 4 vitest cases.
- `apps/desktop/src/renderer/src/pages/pythia-view.tsx` — lazy loads
  the report surface; flag-off path entirely bypasses.

**Locales**
- `packages/views/locales/{en,zh-Hans,ja,ko}/pythia.json` — 28 new
  keys each (report title, horizon, persona, scenario, lab_source).

**Verification landed on `feat/0.3.29-pythia-forecast`**
- `go build ./...` PASS
- `go vet ./...` 0 new warnings
- `go test ./internal/handler -race -count=1` PASS
- `pnpm vitest locales/parity.test.ts` 163/163 PASS
- `pnpm vitest pythia-report-surface.test.ts` 4/4 PASS
- `pnpm --filter @multica/views tsc --noEmit` 0 new pythia errors

The committed branch is integration-ready: merge into main (or cherry-pick
into the 0.3.29 train) once the other three agents finish.

## Outstanding work

### Claude Lab (worktree `0.3.29-claude-lab`, no commit yet)

When the quota resets, resume with these explicit deliverables:

1. Backend
   - `ListIssuesByLabSource` sqlc query +
     `GET /api/experimental/claude-science-lab/issues?lab=claude_science_lab`.
   - When `experimental_claude_runtime` creates a session, persist
     `issue_id` and `lab_source` on the session row.
   - When a runtime artifact is created, persist `issue_id` (nullable)
     on `experimental_runtime_artifact`.
   - Migration 156: `lab_source TEXT` on runtime session + artifact +
     index where not null. Forward-only.
2. Renderer
   - Replace the six placeholder tabs (`plan/chat/artifact/forecast/code/knowledge`)
     in `apps/desktop/src/renderer/src/pages/claude-lab-view.tsx` with
     real data:
     - `Plan`: list of issues with `lab_source = claude_science_lab`,
       with Run / Open buttons.
     - `Artifact`: mount `ExperimentalArtifactView`, filtered by issue.
     - `Forecast`: real chart (Recharts if available, else SVG) for the
       SSE stream — Issue-scoped.
     - `Code`: list recent runtime sessions' code, scoped to the lab.
     - `Knowledge`: list the Skill references the lab agents cite.
   - Agent selector must be locked while a lab is selected; offer a
     "Run for me" low-code button that calls the runtime execute endpoint.
3. Locales — add the new copy to all four `claude-lab` bundles and
   parity test 163/163.
4. Flag-off path must remain import-free.

### Mythos swarm (no worktree)

1. AppSidebar — when `mythos_swarm` is enabled, show a FlaskConical
   badge in the Labs section: "蜂群拓扑已启用".
2. Agents enhancement
   - Allow user to attach extra agents in addition to the 5 canonical.
   - Per-iteration: coda writes a reflection comment (`self_reflection`).
   - Allow coda loop to pull Skill references via the SkillAdapter.
   - `structured_conclusions JSONB` field on Mythos run (migration 156 / 157).
3. UI exposes: extra-agents multi-select, depth (iterations),
   self-evolution toggle.
4. Locales — four languages parity.

### UI polish (worktree `0.3.29-ui-polish`, still on main)

1. Right panel on Issue detail — if `issue.lab_source` is set, render a
   "Labs" section with an Open button pointing at `/experimental/<suffix>`.
   Suffix table: `claude_science_lab → claude-lab`,
   `pythia_oracle → pythia`, `mythos_swarm → mythos`,
   `claude_science_lab (single) → claude-lab` etc. Build a helper.
2. Issue title area link "进入实验室" duplicates the sidebar section for
   visibility.
3. Settings cleanup: remove `集成` / `Git` / `更新` entries from all four
   settings locales; if any `/settings/integrations`, `/settings/git`,
   `/settings/updates` routes exist, drop them.

## Hard constraints (unchanged)

- Flag off must completely bypass experimental code.
- No reserved workspace for new labs.
- Migrations forward-only.
- i18next selectors must be arrow-expression; no block-body selectors.
- AppSidebar lab components wrapped in ErrorBoundary (existing
  invariant from the 2026-07-14 incident).
- Pre-DMG `bash ~/.multica/scripts/pre-update-snapshot.sh` mandatory.
- Do NOT re-add telemetry / auto-update / Google OAuth / cloud features
  / external support UI.
- Username-only login semantics untouched.

## Resilience wiring kept

- Four `~/Library/LaunchAgents/com.multica.auto-resume-{0130,0300,0540,0600}.plist`
  jobs (last exit 0 in `launchctl print`).
- Each calls `~/.multica/scripts/multica-auto-resume-0.3.29.sh` which
  writes `/Users/jiangjianyan/.multica/auto-resume-marker.json` (last
  wrote at `2026-07-15T22:00:03Z`). Claude Code cannot be relaunched by
  cron, so the marker is read on the user's next manual boot.

## Next-step UX

1. Wait for `Token Plan` quota to reset OR the user to top up credits.
2. Re-launch the three blocked agents with identical worktree paths.
3. After all three land, run `pnpm typecheck` then `pnpm test` in main.
4. Make 0.3.29 release: version bump → snapshot → migrate up →
   bundle-cli → electron-vite build → electron-builder --dir --mac --arm64
   → ad-hoc sign → cp -R into /Applications/Multica.app → cold-start
   verification (5432 LISTEN, 8090 LISTEN, /health OK) → row parity
   check.
5. Write `.omc/release-notes-0.3.29.md` and commit on `feat/0.3.29`.

---
