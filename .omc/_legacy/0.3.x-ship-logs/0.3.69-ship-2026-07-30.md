---
name: 0.3.69-ship
created: 2026-07-30T18:10:16Z
updated: 2026-07-30T18:10:16Z
status: complete
---

# 0.3.69 ship — 2026-07-30 (UTC)

One-line: backend + frontend patch (Labs install-failure toast + real
`runtime_gc` tarGz + installable-sources sync) shipped via manual asar-repack
fallback, **extended to replace the Go binaries** since `server/` changed.

## What shipped

- **Backend (new Go binaries, 0.3.69)**:
  - `experimental_flags.go`: `UpdateExperimentalFlag` returns `200 +
    {install_error}` on install failure (the toast contract).
  - `runtime_gc.go`: real atomic `tarGz` (replaces the 0.3.19 placeholder stub
    that silently destroyed archived session data — root CLAUDE.md "Known
    Stability Surfaces").
  - `experimental_resources.go`: `installableSources` += pythia_oracle,
    code_canvas.
  - labs fixes in `skill.go` / `squad.go` / `registry.go` /
    `experimental_mythos_run.go` / `mythos/supervise.go` / `cmd/server/main.go`.
- **Frontend (new renderer)**: `labs-tab.tsx` install-failure warning toast,
  `create-issue.tsx`, `issue-labs-section.tsx`, `app-sidebar.tsx`,
  `packages/core/api/client.ts` (parses `install_error`).
- **i18n**: `labs.toast_install_warning` (en / zh-Hans / ja / ko).
- **Not in the .app**: sub-domain CLAUDE.md/AGENTS.md mirrors, docs-sync
  script, ci.yml, eslint boundaries.

## Pre-flight (per `conventions.mdx` + build sanity)

- `go build ./...` PASS; `pnpm typecheck` 6/6 PASS (FULL TURBO).
- No new migrations (latest = 168; `migrate up` all already-applied).
- Conventions check: new i18n key passes (naming / arrow selector / voice).
  Half-width comma in zh-Hans matches existing `settings.json` house style
  (pre-existing doc-vs-practice gap, not a regression).

## Ship path (manual asar-repack fallback + Go binary swap)

1. Bumped `apps/desktop/package.json` 0.3.68 → 0.3.69.
2. `pre-update-snapshot.sh` PASS — app `.bak` 882M, PG logical dump (13
   tables), config 80 files, KB 6828 .md, git tag `pre-update-20260731-020202`.
3. `migrate up` — no pending.
4. `bundle-cli` — rebuilt `multica`/`server`/`migrate` at **version=0.3.69**,
   410 migrations staged.
5. `electron-vite build` — new renderer (`index-DgBowe9B.js`), `install_error`
   + toast string present.
6. Extracted installed 0.3.68 asar (616M) → replaced `out/{main,preload,
   renderer}` → repacked (513M). Verified: `rawRequest=119`, `install_error=91`.
7. Stopped app + daemon; copied new asar over `app.asar`; copied fresh Go
   binaries over `app.asar.unpacked/resources/bin/{multica,server,migrate}`.
8. Info.plist → 0.3.69.
9. `desktop-sign-nested-binaries.sh` PASS — app + 3 binaries signed,
   `multica --help` exit 0 (NOT 137).
10. `verify-desktop-cold-start.sh` PASS in ~4s, server 0.3.69.

## Risk vs outcome

- **Backend changed → Go binaries replaced** (the key difference from the
  0.3.68 renderer-only ship). Re-signing covered the swapped binaries; the
  self-verifying `multica --help` exit 0 confirms Gatekeeper is satisfied.
- **electron-builder network block**: avoided via fallback (no electron
  re-download). No resource-path additions/removals/renames → fallback
  contract holds.
- **Row parity**: workspace=1 / issue=244 / comment=1510 / agent=96 /
  user_plugin=0 — identical to 0.3.68 (this ship creates no data);
  workspace + agent invariants held.

## Three-check pass

1. Ports: 5432 (bundled PG) + 8090 (server PID 24889). ✓
2. `/health` → server 0.3.69 (etime 00:01). ✓
3. Row parity via bundled psql: workspace=1 / agent=96 invariants held. ✓

Installed: `/Applications/Multica.app` = **0.3.69**, cold-start verified.
Rollback: `/Applications/Multica.app.0.3.68.pre-update-20260731-020202.bak`
+ PG logical dump at `~/.multica/backups/pre-update-20260731-020202`.
