# 0.3.66 ship — 2026-07-29

One-line: tech-debt + stability audit fixes — 2 rules-of-hooks crash fixes
(ChatWindow + Claude Lab), root ErrorBoundary, LabContext schema, enforced
ship chain, i18n 57→0. Runtime fixes required a reinstall → shipped.

## Ship steps (canonical chain, run manually for electron-builder control)

1. `pre-update-snapshot.sh` → PASS. Backed up running 0.3.65 app (773M),
   13-table logical PG dump, config (80 files), KB (6809 .md), git tag
   `pre-update-20260729-075131`. Pruned the 0.3.64 .bak.
2. `migrate up` (DATABASE_URL from .env) → **no pending** (168 is latest; this
   round adds NO SQL — all changes are TS/scripts/docs).
3. `bundle-cli` → 3 Go binaries at version=0.3.66; post-copy integrity check
   ran: "verified 410 migration files copied intact" (the fix-#2 self-check).
4. `electron-vite build` → ✓ built (renderer compiled; typecheck already green).
5. `electron-builder --mac --dir` (cwd=apps/desktop) → **completed, NO M2
   deadlock this run** (electron 39.8.7 downloaded, ad-hoc signed, notarization
   skipped as expected for the fork). The intermittent alpha.12 deadlock did not
   fire; manual asar-repack fallback was NOT needed.
6. asar verification (grep -a): rawRequest=119, client_render_error=1 (P1-2),
   no_workspace=33 (C1/H1), lab_row_bound_tooltip=10 (H1);
   CFBundleShortVersionString=0.3.66. All round changes present in the package.
7. `cp -R dist/mac-arm64/Multica.app /Applications/` (user-confirmed destructive
   step) → installed version reads 0.3.66.
8. `desktop-sign-nested-binaries.sh /Applications/Multica.app` → signed app + 3
   nested binaries; self-verify `multica --help` exit 0 (NOT 137). First real
   caller of the sign script (P1-1 closed).
9. `verify-desktop-cold-start.sh` → PASS: cold start ~6s, server 0.3.66.

## Risk vs outcome

- **M2 electron-builder deadlock (main risk):** did NOT occur this run.
  `app-builder-bin@5.0.0-alpha.12` still unpinned (see release notes); the
  deadlock is intermittent. Fallback documented but unused.
- **Row parity:** workspace=1 (invariant held), agent=92 (stable vs 0.3.56's
  ~90 band — this round touches neither), issue=238 / comment=1426 (drift
  expected from issue creation between releases), user_plugin=0. No data loss.

## Three-check pass

1. Ports: 8090 (server PID 47634) + 5432 (postgres) both listening. ✓
2. `/health` → `{"status":"ok"}`. ✓
3. Row parity: workspace=1 / issue=238 / comment=1426 / agent=92 /
   user_plugin=0 — workspace + agent invariants held. ✓

Installed: `/Applications/Multica.app` = **0.3.66**, cold-start verified.
Rollback point: `/Applications/Multica.app.0.3.65.pre-update-20260729-075131.bak`
+ PG logical dump at `~/.multica/backups/pre-update-20260729-075131`.
