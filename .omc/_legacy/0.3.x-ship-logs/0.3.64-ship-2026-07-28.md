---
name: 0.3.64-ship-2026-07-28
created: 2026-07-28T15:32:04Z
updated: 2026-07-28T15:32:04Z
---

# 0.3.64 ship — 2026-07-28 (labs audit fixes, SHIPPED)

`/Applications/Multica.app` 0.3.64 cold-start verified. Working tree
labs-audit fixes (9 high-severity items) + migration 168 shipped via
the canonical chain. See `release-notes-0.3.64.md` for the change set.

## Ship steps (canonical order, all green)

1. `pre-update-snapshot.sh` — PASS (App .bak 881M + PG logical backup
   13 tables + config 80 files + KB 6809 md; tag
   `pre-update-20260728-231947`).
2. `cd server && go run ./cmd/migrate up` — applied
   **168_user_plugin_slug_partial_unique** (partial unique indexes
   `idx_user_plugin_slug_live` / `idx_user_plugin_flag_key_live`
   confirmed in live DB).
3. `pnpm --filter @multica/desktop bundle-cli` — 3 Go binaries at
   version=0.3.64 (git-describe fallback to package.json worked) +
   migrations + Pythia/claude-science/manifest resources staged.
4. `pnpm --filter @multica/desktop build` — electron-vite OK (one
   benign INEFFECTIVE_DYNAMIC_IMPORT warning on emoji-picker).
5. `rm -rf dist && pnpm exec electron-builder --mac --dir` —
   **no deadlock this round** (electron 39.8.7 download 2m30s, then
   packaging + ad-hoc sign, exit 0). Manual asar-repack fallback NOT
   needed.
6. Quit old app (8090 freed in 2s) → `rm -rf /Applications/Multica.app`
   + `cp -R dist/mac-arm64/Multica.app /Applications/` → codesign app
   (`--force --deep`) + 3 nested binaries (`multica`, `server`,
   `migrate`) individually → `multica --help` exit=0 (NOT 137).

## Risk vs outcome

| Risk | Outcome |
|---|---|
| electron-builder alpha12 deadlock (macOS 27) | Did not occur; clean exit 0 |
| Gatekeeper SIGKILL on unpacked Go binaries (0.3.62 blocker) | Re-signed all 3; `multica --help` exit 0 |
| Stale asar (0.3.30.1 incident: pnpm build alone ≠ replace) | `grep -a -c rawRequest app.asar` = 119 → fresh renderer |
| Migration 168 SQL error at first launch | Applied at build time, indexes present in live DB |

## Cold-start three-check + row parity

1. Listeners: 5432 (postgres) + 8090 (server) both up.
2. `GET /health` → `{"status":"ok"}`; cold start ~2 s; server PID
   reports 0.3.64.
3. Row parity vs the pre-update backup (CSV-parsed, exact):

| table | backup | live | |
|---|---|---|---|
| workspace | 1 | 1 | OK |
| issue | 237 | 237 | OK |
| comment | 1426 | 1426 | OK |
| agent | 92 | 92 | OK |

`user_plugin=0`, `schema_migrations=298`. Zero drift.

## Notes

- Version bump 0.3.62 → 0.3.64 (0.3.63 shipped via manual asar repack
  without a package.json bump; 0.3.64 is the first bump since, and the
  schema change warranted it per the release-notes checklist).
- Working tree left uncommitted (user did not ask to commit): version
  bump, ship log, release-notes status flip, plus the pre-existing
  audit-fix diff.
