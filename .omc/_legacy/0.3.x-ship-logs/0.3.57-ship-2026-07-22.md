---
name: 0.3.57-ship-2026-07-22
created: 2026-07-22T10:48:00Z
updated: 2026-07-22T10:48:00Z
status: complete
---

# Multica 0.3.57 ship — 2026-07-22T18:50+0800

`/Applications/Multica.app` 升级到 0.3.57 (`constitution_agent` lab retired)。
替代原 0.3.56 安装版。

## 起点

接续 0.3.56 ship (`.omc/0.3.56-ship-2026-07-22.md`),HEAD detached on
`033b036`。0.3.56 release-notes + ship log 已就位。pre-update snapshot
`pre-update-20260722-174244` 仍保留为回滚 anchor (指向 0.3.55)。

新需求: 实验室功能「宪法宪章守护」(`constitution_agent` lab) 不要了。
采取 A 方案 (全删 L1+L2+L3+L4 + new migration 165 forward-only drop)。

## 工作

委托 executor agent (subagent_type=executor) 跨 4 层删除,完成后:

| 层级 | 改动 |
|------|------|
| L1 资源 | 删除 6 文件: manifest.json / SKILL.md × 2 / builtin skill dir / 153 migration × 2 |
| L2 server | 16 Go 文件 (catalog Flag literal / visibility / lock / 7 handler 分支 / 4 service 分支 / 2 测试 / 1 install handler 整个删除) |
| L3 desktop | 6 文件 (1 view 整个删除 + 3 引用清理 + manager-factory + routes) |
| L4 packages | 10 文件 (4 语言 layout.json + zh-Hans experimental.json + sidebar + 2 components + create-issue + agent.ts) |

新 migration 165 (forward-only drop):
- DELETE visibility rows
- UPDATE agent SET archived_at = now() WHERE name = '宪法智能体'
- UPDATE autopilot SET status = 'archived' WHERE title IN (CTR / CSIL / TAOL)

## 验证

- go build ./... → 0 errors
- pnpm typecheck (node + web) → 0 errors
- grep constitution_agent / ConstitutionAgent / constitution-agent → 0 active code paths
- migration 165 up applied cleanly (DELETE 4 + UPDATE 0 agent + UPDATE 0 autopilot, 因为 0.3.20 已经 archived 过 autopilots,agent 在 fresh workspace 是新加的)

## Ship 链

| 步骤 | 结果 |
|------|------|
| 1. pre-update-snapshot (reused) | `pre-update-20260722-174244` (4h 前,未重跑) |
| 2. migrate up | ✓ mig 165 applied |
| 3. bundle-cli | ✓ 3 Go 二进制 version=0.3.57;resources 114M,**无 constitution_agent 实验目录** |
| 4. electron-vite build | ✓ out/main 164K + out/renderer 29M, 2.23s |
| 5. electron-builder --mac --dir | ✓ dist/mac-arm64/Multica.app 786M,ad-hoc signed |
| 6. pkill + cp -R 替换 | ✓ /Applications/Multica.app → 0.3.57 |

## Cold-start 三检查 + row parity

启动后 10s 内:

| 检查 | 结果 |
|------|------|
| lsof -nP -iTCP:5432 -sTCP:LISTEN | ✓ `postgres 69693` LISTEN |
| lsof -nP -iTCP:8090 -sTCP:LISTEN | ✓ `server 69773` LISTEN |
| curl /health | ✓ `{"status":"ok"}` |

Row parity:

```
workspace=1            (严格匹配 0.3.56 baseline)
issue=220              (一致)
comment=1346           (一致)
agent=92               (比 0.3.56 baseline 91 多 1,符合 mig 165 把 '宪法智能体' 移到 archived 的预期)
agent_archived=14      (比 0.3.56 baseline 13 多 1,匹配同一 archive 动作)
```

## 与 0.3.56 baseline 对比

| | 0.3.56 安装版 | 0.3.57 新安装版 |
|---|---|---|
| 版本号 | 0.3.56 | 0.3.57 |
| `constitution_agent` lab sidebar 入口 | 显示 | 移除 |
| `宪法智能体` agent | active | archived (mig 165) |
| 3 个宪章 autopilots (CTR/CSIL/TAOL) | archived | archived (重设 status 防 fresh workspace 漂移) |
| 资源目录 `resources/experiments/constitution_agent/` | 存在 | 删除 |
| install handler `install_constitution_agent` | 注册 | 移除 |
| `agent_creation_studio` 的 CompatToggle (constitution 系统 key) | placeholder | 移除 |

## Pre-update snapshot 路径 (回滚 anchor)

| 类型 | 路径 |
|------|------|
| App (0.3.56 → 0.3.57 之前) | `/Applications/Multica.app.0.3.55.pre-update-20260722-174244.bak` (0.3.55) |
| App (0.3.57 之前) | 新装 0.3.56 已被 cp -R 覆盖,无独立 backup;需要回滚时用 0.3.55 backup + 重 ship 0.3.56 |
| PG | `~/.multica/backups/pre-update-20260722-174244/` (备份在 0.3.55 时刻) |
| Git | tag `pre-update-20260722-174244` + tag `pre-update-20260720-*` (历史) |

## 已知风险

- HEAD 0.3.57 是 detached commit,未新建 branch。如果之后还有改动,需要在 epic/upstream-0.4.0 上重新基于 033b036 建新分支。
- `loadSystemPromptBinding` 的 `case "constitution_agent_v1"` 桩保留(用户可能在该 key 下有老 agent 数据),`return ("", false)` 让用户 agent 不再加载 charter prompt;UI 不显示该 option。
- `system_key` 文档现在指向 0.3.51 column shipped,constitution_agent_v1 已 retire。
- 工作树当前 47 文件未 commit 改动 (32 modified + 7 deleted + 3 untracked + 5 docs).后续可逐 PR commit / 暂存。

## 后续会话起点

- 想 ship 0.3.58: 需先 commit 当前 47 文件改动到 epic/upstream-0.4.0 分支,再跑 ship 链
- 想 cherry-pick 0.4.0 上游修复: 看 3 个 cherry-pick 报告
  - `.omc/cherry-pick-ui-0.4.0.html` (23 候选)
  - `.omc/cherry-pick-bug-0.4.0.html` (16 候选)
  - `.omc/cherry-pick-perf-0.4.0.html` (0 候选,已同步)
- 0.4.0 上游半合并改动 (670 文件) 在 `stash@{0}` (epic/upstream-0.4.0 分支)