---
name: 0.3.68-ship
created: 2026-07-30T14:49:56Z
updated: 2026-07-30T14:49:56Z
status: complete
---

# 0.3.68 ship — 2026-07-30

One-line: renderer-only patch (issue detail full-width layout) shipped via
manual asar-repack fallback — same recovery path as 0.3.67, chosen because
electron-builder's electron download is still blocked by proxy ECONNRESET and
this patch adds/removes/renames no resource paths.

## What shipped

- **User-visible**: issue detail page body switched from centered
  `mx-auto max-w-4xl` to full-width (`packages/views/issues/components/issue-detail.tsx`,
  2 sites: loading skeleton + live content container).
- **Not in the .app**: sub-domain `CLAUDE.md`/`AGENTS.md` mirrors,
  `check-agents-docs-sync.mjs` (+115), `ci.yml`, eslint boundaries — repo
  infra/docs only, no binary change.

## Ship path (manual asar repack fallback)

1. Bumped `apps/desktop/package.json` 0.3.67 → 0.3.68.
2. `pre-update-snapshot.sh` PASS — app `.bak` 882M, PG logical dump (13 tables),
   config 80 files, KB 6827 .md, git tag `pre-update-20260730-224656`.
3. `pnpm --filter @multica/desktop build` (electron-vite) PASS.
4. Extracted installed 0.3.67 `app.asar` (616M) → replaced
   `out/{main,preload,renderer}` with fresh build → repacked (527M).
   Verified new asar: `rawRequest=119`, full-width string present.
5. Stopped app + daemon, copied new asar over
   `/Applications/Multica.app/Contents/Resources/app.asar`.
6. Info.plist `CFBundleShortVersionString`/`CFBundleVersion` → 0.3.68.
7. `desktop-sign-nested-binaries.sh` PASS — app + 3 nested binaries signed,
   `multica --help` exit 0 (NOT 137).
8. `verify-desktop-cold-start.sh` PASS in ~4s, server 0.3.68.

**Go binaries NOT replaced**: `server/` had no code changes (only a new
`AGENTS.md` doc), so the signed 0.3.67 binaries in `app.asar.unpacked/` are
byte-identical and kept their signatures. Repack only touched `app.asar`.

## Risk vs outcome

- **electron-builder network block** (expected): avoided entirely by the
  fallback — no electron re-download. Identical to the 0.3.63 / 0.3.67 path;
  no functional compromise.
- **M2 app-builder-bin pin** still pending in the lockfile (pnpm install
  deferred, npmmirror ECONNRESET). Irrelevant to this fallback ship; takes
  effect at the next canonical `electron-builder` run.
- **Row parity**: workspace=1 / issue=244 / comment=1510 / agent=96 /
  user_plugin=0. workspace + agent invariants unchanged from 0.3.67
  (242→244 issues, 1449→1510 comments = normal growth).

## Three-check pass

1. Ports: 5432 (bundled PG, psql reachable) + 8090 (server PID 35301). ✓
2. `/health` → cold-start script verified server 0.3.68 (etime 00:00). ✓
3. Row parity via bundled psql: workspace=1 / agent=96 invariants held. ✓

Installed: `/Applications/Multica.app` = **0.3.68**, cold-start verified.
Rollback: `/Applications/Multica.app.0.3.67.pre-update-20260730-224656.bak`
+ PG logical dump at `~/.multica/backups/pre-update-20260730-224656`.
