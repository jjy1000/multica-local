{
  "version": "0.3.42",
  "goal": "Ship hardened server binary + renderer to /Applications/Multica.app, cold-start verify",
  "phases": [
    {"step": "Probe PG", "tool": "pg-bootstrap.ts probeMulticaPg", "note": "PG was down last session — likely just needs initdb + start via bundled pg_ctl 17.4"},
    {"step": "Rebuild .app from clean dist", "tool": "rm -rf dist + pnpm package -- --mac --dir", "note": "Wipe stale dist to avoid asar integrity check failure. Use pnpm package wrapper (scripts/package.mjs) which correctly sets extraMetadata.version from git describe or package.json fallback"},
    {"step": "Verify Info.plist", "tool": "defaults read CFBundleShortVersionString", "expect": "0.3.42"},
    {"step": "Resign fresh .app", "tool": "xattr -cr + codesign --force --deep --sign -", "note": "Fresh dist build should NOT have the 'unsealed contents' issue — that was from a cp -R of a stale dist from prior session"},
    {"step": "Kill any prior server/daemon/PG", "tool": "pkill Multica + pkill multica daemon + pg_ctl stop"},
    {"step": "cp -R to /Applications", "tool": "cp -R", "note": "Not via open. Use bare binary launch: /Applications/Multica.app/Contents/MacOS/Multica"},
    {"step": "Cold-start probe: 5432 + 8090 + /health", "tool": "lsof + curl", "timeout": "20s"},
    {"step": "Row parity: workspace/issue/comment/agent", "tool": "psql", "expect": "1/200/1022/86 (zero drift)"},
    {"step": "Lab-context e2e: auth → GET /lab-context → structured fields", "tool": "curl", "note": "Verify 0.3.42 hardening: kind allowlist active, error messages generic, GetChatSessionInWorkspace wired"},
    {"step": "Write release notes", "tool": ".omc/release-notes-0.3.42.md", "note": "Already created in prior session — just update with final verification results"}
  ],
  "known_risks": [
    "macOS 27 code-signing might still block open(). Fallback: launch binary directly: /Applications/Multica.app/Contents/MacOS/Multica & (known working from 0.3.41 ship)",
    "PG bootstrap might fail if initdb was run against Homebrew PG 14 instead of bundled PG 17.4. Fallback: export PATH to bundled pg bin + initdb fresh"
  ]
}
