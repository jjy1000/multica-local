---
name: 0.5.80-ship-2026-08-27
created: 2026-08-27T15:10:00+08:00
---

# 0.5.80 Ship Log — 2026-08-27

`bash scripts/ship-mac.sh --yes --skip-snapshot` → 7/7 PASS, `/Applications/Multica.app` = **0.5.80**, cold start ~4s.

## What shipped

Labs fullscreen-takeover fix: one-shot release-suppression handshake (`platform/workspace-singleton-release-guard.ts`) keeps the workspace singleton alive across in-tab navigations into `/experimental/*`, so `{slug && <AppSidebar/>}` / `{slug && <WindowToolbar/>}` chrome stays mounted over labs + plugin shells. `ExperimentalViewShell`'s fixed Back pill removed (was the workaround). Version bump 0.5.79 → 0.5.80. Full detail: [`.omc/release-notes-0.5.80.md`](release-notes-0.5.80.md).

## Chain of events

1. First ship attempt aborted at step 2/7 (`go run ./cmd/migrate up`): DATABASE_URL (localhost:5432) refused — **the app's bundled Postgres (`~/Library/Application Support/Multica/pg/17.4`) was not running** (last clean shutdown log entry dated 2026-07-20; snapshot script also warned "PG 5433 not listening"). No Docker container exists on this machine — the bundled PG is the only instance.
2. Recovery: started bundled PG manually —
   `pg_ctl -D ~/Library/Application\ Support/Multica/pgdata -l ~/Library/Application\ Support/Multica/pg/17.4/pg.log start`
   Verified via `pg_isready` (accepting, port=5432 from pgdata's postgresql.conf; `listen_addresses='127.0.0.1'`), auth with `.env` creds (multica/multica), and table listing (schema intact).
3. Attempt #1 had already produced a complete pre-update snapshot before dying at migrate, so attempt #2 ran with `--skip-snapshot`. Rollback point: `/Applications/Multica.app.0.5.79.pre-update-20260827-142934.bak` (~813M) + config/backup dir `~/.multica/backups/pre-update-20260827-142934`.

## Gates

- Snapshot: PASS (attempt #1, full 7-step pre-update)
- Migrations: PASS on rerun (post PG start)
- electron-builder bundle + asar verify: PASS
- Nested-binary re-sign (`multica` / `server` / `migrate`): PASS — not SIGKILL'd (exit ≠ 137)
- Backup: see rollback point above
- Install to /Applications: PASS (`CFBundleShortVersionString` = 0.5.80 read back independently)
- Cold-start verify: PASS ~4s ("PASS: cold start verified, server is 0.5.80"); note `Row parity: psql-unavailable` is a benign sub-check (script couldn't find psql on PATH; DB connectivity confirmed manually)

## Gates BEFORE ship (dev)

- `pnpm typecheck`: 6/6 green
- Desktop vitest: 45 files / 362 tests all passed (incl. new guard suite + workspace-route-layout labs regression case)

## Ops lesson (this machine)

Bundled Postgres does NOT auto-start for headless ship runs — the ship script assumes the app (or a prior manual start) left it listening. If `migrate up` refuses with ECONNREFUSED at step 2/7, start PG per recovery above instead of suspecting migrations.
