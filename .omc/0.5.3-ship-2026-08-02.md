# 0.5.3 Ship Log — 2026-08-02

**Version**: 0.5.3 (`apps/desktop/package.json`, Info.plist verified 0.5.3)
**Branch**: epic/0.3.58-cherry-pick (local project, no cloud push)
**Type**: 功能 + 迁移 + 修复

## 内容
- **功能一**: 自进化循环泛化四对象(智能体/技能/团队/自动化)—— trust ≥8 保留、<7 强制优化、增量验证打分、低信任优先、deferral 联动
- **功能二**: agent_creation_studio 升级为可分配 lab(agent_creation_expert leader + install + leader 映射)
- **迁移**: 232(agent_opt_edit 泛化)+ 233(修复 0.5.2 updated_at drift)
- **edits.go**: 人工 apply/revert 泛化四对象(含 snapshot bug 修复)

## Ship 步骤
1. ✅ pre-update-snapshot(`pre-update-20260802-015503`, 13 tables + KB 6846 md)
2. ✅ `go run ./cmd/migrate up`(232/233 本地先应用)
3. ✅ bundle-cli(0.5.3 三二进制 + 444 migrations + manifests,232/233 进 bundle)
4. ✅ electron-vite build(1.44s, index-D9D6MTEM.js)
5. ✅ electron-builder --dir(**无 alpha deadlock**, ad-hoc 签名)
6. ✅ cp -R → /Applications/ + desktop-sign-nested-binaries.sh(multica --help exit 0)
7. ✅ 冷启动三查:5432/8090 LISTEN、/health ok、row parity(workspace=1 / issue=251 / comment=1517 / agent=96)

## 风险 vs 结果
- 行数漂移: issue 84→251(0.5.2 以来 self-opt 每周报告 issue + 用户新增)、agent 38→96(多次 install 幂等 upsert 的历史残留)—— workspace=1 恒定,无数据丢失
- electron-builder alpha.13 deadlock 未触发,走 canonical `--dir` 路径
- 232/233 冷启动时本地库已应用(日志 skip),app 内确认无迁移错误

## 已知
- 无已知回归;handler/service/experimental 全量 Go 测试 + 2 个新集成测试全绿
- 该项目本地化 fork,不推云端(用户明确)
