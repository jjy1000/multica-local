# 0.5.89 — labs 对话化插件管理 + 回收账本 + 技术债清算

**Ship date**: 2026-08-30（02:06 冷启动验证 PASS）
**Branch**: `epic/0.5.72-followups`
**Baseline**: `7134918fe`（0.5.89 dev 收尾 + mock 扩展）
**Target**: `/Applications/Multica.app` = **0.5.89** ✅（`defaults read` 与 `package.json` 一致；冷启动 ~6 s，server PID 78459）

## What shipped

Full inventory in [`.omc/release-notes-0.5.89.md`](release-notes-0.5.89.md)：

- **0.5.89 主 cycle**（commits `3b26277fe`/`5ebfa0ab2`/`87e60463a`/`d32b9ba81`/`7134918fe`）：对话式插件创建/管理（claim briefing `## Lab Plugin Management` + `multica lab` 五动词 + never-disagree 五动词 contract test）、mig 284 回收账本（inline 代建/声明 + 删除即回收 + 409 护栏 + reclaim-plan/retry 端点）、`skills_visibility` lab_scoped、`created_by_issue/task` 来源盖章。
- **打包前技术债清算**（本周期追加）：daemonless 实验室安装 NULL runtime_id 修复（五安装位统一走 `resolveOrSynthesizeLabRuntime` 合成 stub + rebind 花名册 3→11 人）、`.trash/` 30 天 GC 接入 `RuntimeGC.sweep`、reclaim 两端点测试补齐、不可见 LabPicker 触发器、`useActorName` 按 id 兜底解析（"Unknown Agent" 销账）、`title_en` 查证销账（HEAD 上不存在失配）、DB 孤儿 lock/visibility 行清理（92+53 行）。

## Gates（quiet env，uncached，serial）

- `go test -count=1 -p 1 ./internal/... ./pkg/agent/... ./cmd/...`，`DATABASE_URL` 从 repo-root `.env` 导出，运行时验证服务器未启动：**exit 0，42 包 ok，0 FAIL**（含新增 daemonless/reclaim/GC 回归 pin）。
- `pnpm typecheck`（full turbo）：**6/6**。
- `pnpm test`（full turbo）：**零新增失败**——core 3 失败（auth/store ×2 + api/schemas ×1）与 views 42 失败全部经 `git stash` 基线判别为存量；desktop 47 文件 370 用例全过。
- `node scripts/check-agents-docs-sync.mjs`：✓（受 guard 段落一致；头部散文在本 ship 中刷新）。

## 打包链（ship-mac.sh 7 步，`--yes`）

1. **pre-update snapshot**：`/Applications/Multica.app.0.5.88.pre-update-20260830-020451.bak`（815M，保留上一正式版 0.5.88 作为回滚点；旧 0.5.86 bak 按策略剪除）+ `pg_dump -Fc` 32M（`~/.multica/backups/pre-update-20260830-020451`）+ config/profiles 93 files + KB vault rsync（保留策略：bak×1、pre-update×3、KB×2）。
2. **migrate up**：284 个 migration 全部 already-applied（mig 284 已在 dev 期前向应用）——零 schema 变更随本包首次落地。
3-5. **bundle-cli → electron-vite build → electron-builder --dir**：index.html 798 bytes 合规；asar 191 处 rawRequest（renderer 确认新打包）。
6. **install + re-sign**：cp → /Applications，嵌套 Go 二进制逐一 ad-hoc 重签，`multica --help` 自验通过（非 137）。
6b. **本地仓库备份**：`.omc/backups/2026-08-29-1806/0.5.89-ship/`（manifest + diff.patch + status；超额归档 0.5.45 → `_archive`）。
7. **cold-start**：Info.plist = 0.5.89 = package.json；冷启动 ~6 s；server PID 78459；`[verify] PASS: cold start verified, server is 0.5.89`。

## Row parity（手动补测；验证器内 psql 不在 PATH，0.5.88 同款）

`PGPASSWORD=multica psql -U multica -d multica -h 127.0.0.1 -p 5432`：
**workspace=8 / issue=415 / comment=2591 / agent=136**（0.5.88 金基线 7/408/2588/132 之后的自然累积）；新表 **user_plugin=21（5 active + 16 soft-deleted）**、**user_plugin_resource=3（全部 origin=provisioned、reclaim_status=linked、归属两个存活调试插件——账本健康，无孤儿）**。

## Risk vs outcome

- 本包携带 mig 284（`user_plugin_resource` 表 + `user_plugin.created_by_issue/task` 列）——纯增量、forward-only，老 daemon 二进制不受影响。
- daemonless stub 修复只改安装路径的 runtime 绑定来源：在线工作区行为与之前完全一致（优先复用在线 runtime）。
- rebind 花名册扩展只在 install/rebind 调用时生效，mythos 花名册刻意未纳入。
- 删除插件 API 204→200 属 0.5.89 主 cycle 已宣告的契约变化，客户端同步更新。
- 已知余留（0.5.90+）：CompleteTask dispatched no-op（机制已查明，留 WS4）、TestInstallTimesfm workspace 作用域化、mythos reaper boot 竞争；WS4/WS5 见设计文档分期。
