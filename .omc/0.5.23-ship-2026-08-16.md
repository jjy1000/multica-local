# 0.5.23 Ship Log — 2026-08-16

## Header

**Version 0.5.23** — committed 2026-08-16, awaiting packaging. Full port of **MUL-3963 + MUL-4525** onto the 0.5.22 baseline (commit `5d3cacd31`, the `MUL-3963 + MUL-4525 port plan from 11-commit audit` plan doc). The batch surfaced blocked-@mention `trigger_outcomes`, threaded the comment originator through the enqueue + merge path so consecutive comments by different originators actually re-enqueue, re-stamped the originator on agent-created issues, woke private-leader squads via the parent leader on child-done, threaded `permission_mode` / `invocation_targets` through agent template creation, and rolled out the new `agent.invocation_permissions` schema (migration 245) end-to-end. Phase 4 closes the loop by registering the new `swarm` locale namespace so the SwarmWorkbench + related strings are picked up by the type checker and the language picker. **Test skip closure**: `TestConsecutiveCommentsDifferentOriginatorsFullEnqueuePath` (the one deliberately-skipped gate from 0.5.21) now passes — the originator threading through both enqueue + merge path was the gap.

## Commit list (16 atomic commits on top of 0.5.22 baseline)

### MUL-3963 — invocation-permission schema + helpers + view gate (3 commits)

1. `86a51e417` — `feat(agents): MUL-3963 invocation-permission schema (migration 245)` — `agent.invocation_permissions` JSONB column + `agent_template_invocation_permissions` projection table; sqlc queries wired up.
2. `1543dda98` — `feat(agents): MUL-3963 invocation-permission core helpers + wire` — `packInvocationPermissions` / `unpackInvocationPermissions` helpers + template engine gate enforcement.
3. `5dd927517` — `fix(handlers): MUL-3963 call-site wiring — view gate keys on permission_mode` — handler-layer call-site wiring so the view-layer gate keys on `permission_mode` correctly.

### MUL-4525 — comment @mention trigger_outcomes (5 commits, §2 fan-out)

4. `98ab672d4` — `feat(comments): surface blocked @mention trigger_outcomes` (MUL-4525 §2; upstream 51325eeab) — render the blocked fan-out in the comment timeline.
5. `f0d7a1b91` — `fix(comments): one outcome per explicit mention` (MUL-4525 §2 round-2; upstream 7b03baa37) — dedup multiple explicit mentions to a single outcome row.
6. `7ae5f6f2f` — `fix(comments): honest role + status in trigger_outcomes fan-out` — role/status now reflect the actual gate decision, not the requested mention.
7. `a0e8af0eb` — `fix(comments): fail-closed active-task check, never fake deferred` — if there's an active task for the agent on the issue, fail closed (don't pretend it was deferred).
8. `de1adfc5c` — `fix(comments): honest merge outcome — refused merge is blocked, not fake coalesced` — the merge path now refuses coalesced writes instead of silently faking one.

### MUL-4525 §2 — merge-with-originator-re-stamp (2 commits + 2 test commits)

9. `b749ee8ef` — `fix(comment): thread originator through enqueue + merge path` (MUL-4525 sect 2) — `originatorUserID` argument threaded through `enqueueCommentAgentTriggers` + `triggerTasksForComment` (internal plumbing, no API change).
10. `c72a7453a` — `fix(comment): un-skip + close merge-with-originator-re-stamp test gap` — closes the test gap; the merge path now re-stamps the originator on the merged row.
11. `1ec401a9e` — `test(comment): un-skip TestConsecutiveCommentsDifferentOriginatorsFullEnqueuePath` — the 0.5.21-deliberately-skipped test now passes; this commit was the proof.
12. `d8d73a9f5` — `test(comment): clean up t.Skip comment block on the now-passing test` — small cleanup; the test no longer needs the skip comment.

### Pre-session follows — MUL-4063 / MUL-4010 / MUL-4305 (3 commits)

13. `7a28ea3bb` — `fix(agents): thread permission_mode/invocation_targets through template create` (MUL-4010) — agent template create now carries the two new fields.
14. `49de9c47e` — `fix(squad): wake private-leader squad parent leader on child-done` (MUL-4063) — when a private-leader squad's child task completes, the parent leader is woken to acknowledge.
15. `50dbf7d9b` — `fix(issue): keep originator on agent-created issues` (MUL-4305) — issues created by an agent on behalf of a user now stamp the user as the originator, not the agent.

### Phase 4 — i18n cleanup (1 commit)

16. `84be8440f` — `chore(i18n): register swarm locale keys in resources-types + locales index` — `swarm.json` imported for all 4 locales (en / zh-Hans / ja / ko) in `packages/views/locales/index.ts`; type-side `I18nResources` registry in `packages/views/i18n/resources-types.ts` declares `swarm: typeof swarm`. Pattern mirrors the existing `mythos` namespace.

## Verification

- `pnpm typecheck --force` — **6/6 tasks successful** (33.7s, cache bypassed). All packages compile clean with the new `swarm` namespace registered.
- `cd server && go test -count=1 -timeout 600s ./internal/handler/ ./internal/experimental/ ./internal/service/ ./cmd/multica/` — **all green** (handler 12.9s + experimental 0.7s + service 1.3s + cmd/multica 1.3s).
- `pnpm typecheck` (cached, full turbo) — 6/6.
- The 0.5.21 test skip `TestConsecutiveCommentsDifferentOriginatorsFullEnqueuePath` is now a passing test — closes the MUL-4304 follow-up.

## Risks and follow-ups

- **No packaging yet.** Per the standing rule ("先无需打包" — don't ship until asked), `apps/desktop/package.json` is untouched at 0.5.22. When the user says "ship", run `make ship-mac` exactly as documented in root `CLAUDE.md` → "Ship chain".
- **Three MUL-4063 / MUL-4010 / MUL-4305 commits landed in this session but were authored in pre-session sessions** — the work was already merged but uncommitted on this branch. The 0.5.23 ship folds them into one baseline so the lineage is clean.
- **Other uncommitted modifications in the working tree** (per `git status`): 10 files modified under `packages/core/{api,permissions,types}` and `packages/views/agents/{components,languages}` plus a new file `packages/views/agents/components/inspector/access-picker.tsx`. These are *not* part of 0.5.23 — they are leftover from earlier MUL-3963/MUL-4010 agent-detail-instrumentation work that was inspected but not committed. Recommend a follow-up session to audit and commit them (or revert if the experiment didn't pan out).
- **MUL-4305 nuance**: the originator stays the user on agent-created issues, but the agent is still the comment-creator of the first comment that auto-fires. The audit window is open — if a user reports "I created an issue but the row says my agent authored it", double-check the originator stamp, not the comment creator.
- **MUL-4063 nuance**: the private-leader squad wake path runs on `child-done`; the parent leader acknowledgement is async (mirrors the mythos-enhancer supervise pattern from 0.3.31). If the parent leader is offline, the wake times out at 24h and the run row flips to `degraded` — not a regression, but worth noting in the changelog.
- **MUL-4010 + MUL-3963 pair**: the `permission_mode` / `invocation_targets` thread is now end-to-end (schema → helpers → handler → agent template create). The 9 atomic commits close the open audit gap from 0.5.22.

## Next

- 0.5.23 packaging (optional, on user request).
- Audit + commit the 10 uncommitted working-tree files (`packages/core/{api,permissions,types}` + `packages/views/agents/*` + new `access-picker.tsx`) — these are MUL-3963/MUL-4010 agent-detail-instrumentation work that did not land in 0.5.23.
- Possible next ship: 0.5.24 fork-local cleanup, or the next semantica × Multica Phase 2 follow-up.
