# 0.5.92 Ship Log — 2026-08-31

> **Shipped**: `/Applications/Multica.app` = **0.5.92**（MCP 同步镜像 + 用量页 MCP 调用量——Claude Code 的 MCP 只读复刻进 Multica）。Chain: `bash scripts/ship-mac.sh --yes` exit 0。Branch: `epic/0.5.72-followups`，commits `3e4241501`（0.5.91 dirty-tree 残留对齐，10 files）+ `bffb023e8`（MCP sync feature，46 files +2 342）+ `e93d53c9b`（bump 0.5.91 → 0.5.92）。

## 打包前状态

- 0.5.91 遗留的并发会话 WIP 已按其 ship-log 要求**先落 commit**（`3e4241501`：mythos 文案四语言、WS 重连 invalidate 谓词、`networkMode: "always"`、status-picker 防陈旧闭包、timesfm sidebar + causal_graph installable manifest、图标注册）——本周期产物与 git 状态完全对齐，无未提交代码进包。
- 门禁：**全量 `go test -p 1 ./...` 未缓存串行 + `DATABASE_URL` 导出、30 包 0 FAIL**（handler 24.7s 为真跑 DB 的时长，非静默跳过）；typecheck 6/6；改动文件 eslint 0 错误（views 存量 8 个 `i18next/no-literal-string` error 来自已提交的 WIP 对齐 commit，非本周期引入）。
- **教训（一次中止换来的）**：第一次 ship 在 step 2 中止——为门禁 pkill 了 app，而打包版 app 自带 Postgres.app（:5432 随 app 存活），DB 一停 migrate 连不上。**正确姿势是带 app 运行发版**（DB 由在装 app 供着）。重启 app 后重跑了全量门禁（此前停库期间跑的那轮对 DB-backed 包不算数），然后才二次 ship。

## 七步链结果

1. **pre-update snapshot**：`/Applications/Multica.app.0.5.91.pre-update-20260831-153409.bak`（整包回滚点）+ `~/.multica/backups/pre-update-20260831-153409`（pg_dump + config/KB）。**ship 后已按用户偏好把 .bak 移至 `~/.multica/backups/2026-08-31-1537/`，/Applications 只留正式版**。
2. **migrate**：`285_mcp_sync` 已在特性开发期应用到共享 :5432 库，本步全 skip（SQL 语法已在真实库上验证过）。
3. **bundle CLI** → 4. **vite build**（index.html 校验 OK）→ 5. **electron-builder**（ad-hoc 签名）→ 5a. asar 203× rawRequest。
6. **install** + 6a. **re-sign**（嵌套 multica/server/migrate 逐个签名，`multica --help` exit 0 非 137）。
6b. **repo backup**：`.omc/backups/2026-08-31-0736/0.5.92-ship/`。
7. **cold-start verify**：`PASS: cold start verified, server is 0.5.92`（冷启动 ~6s；行平价 `psql-unavailable` 为既有已知项，pg_dump 快照兜底）。

## 安装后核验（特性三重确认）

- server.log：`15:36:19.707 INF mcpsync: worker started source=/Users/jiangjianyan/.claude.json` —— **安装版自己的同步 worker 在跑**。
- DB 实况（打包版 worker 于 15:37:19 完成一轮同步）：`mcp_sync_server` 15 行全部 `status=synced`，`mcp_sync_state.last_error` 为空，`last_synced_at` 实时。
- 产物存在性：renderer asar 内 `mcp-sync` ×23、`mcp_calls` ×25；server 二进制含 `mcpsync: worker started` 字面量。
- `CFBundleShortVersionString` = 0.5.92。

## 本周期交付摘要

见 [`.omc/release-notes-0.5.92.md`](release-notes-0.5.92.md)。要点：migration 285（`mcp_sync_server` + `mcp_sync_state` singleton + `agent_task_queue.mcp_calls`）；`internal/service/mcpsync` worker（canonical 子树 hash 检测、源缺失保旧镜像）；claim 合并（仅 claude provider，手动胜出）；`GET/POST /api/mcp-sync[/refresh]`（env/headers 值服务端打码）；用量页第 5 个 KPI"MCP 调用"（daemon 按 `mcp__` 前缀计数走 /usage 通道，completed_at 锚定日切片，不进 hourly rollup）。

## 遗留

- v1 边界：仅 claude provider 生效；仅 user-scope（`~/.claude.json::mcpServers`）；**无 per-agent 排除开关**（agent 级"继承 Claude Code MCP"toggle 为最自然的下一步）；项目级 `.mcp.json` 与 tombstone 保留语义未做（跟源走）。
- MCP 工具定义会进入每个 claude agent 的每次 run 上下文（token 开销），无排除手段时这是常驻成本。
- 存量：views lint 8 个 `i18next/no-literal-string` error（WIP 对齐 commit 带入的中文硬编码文案）；上游移植账本（MUL-6749/MUL-6835/#7209/MUL-6632）不变；origin remote 404 不变。
