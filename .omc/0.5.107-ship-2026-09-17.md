# 0.5.107 ship record — 2026-09-17

**发版内容 = 两批合一次安装**：

- **A. labs 环境 + `user_*` 插件链路修复批**（开发期记录：`.omc/0.5.107-dev-2026-09-13.md`，
  含 3 条被撤销的审计结论 + 9 条已知残留）。核心：实验室沙盒 `cmd.Env` 显式白名单
  （堵实测确证的密钥泄漏面）、Labs 插件开关绑真实源、`RunUserPlugin` flag 闸、
  batch 层 lab↔assignee 锁单一事实源、产物摄入 32 MiB 上限 + 下载头加固、
  `SetPanicFlagContext` 接线、死路径清理、server/CLAUDE.md §Labs 规则改写。
- **B. 上游 v0.4.38→v0.4.44 价值移植批**（分诊+执行账本：`.omc/upstream-sync-2026-09-16.md`）。

## 提交链（epic/0.5.72-followups）

| 提交 | 内容 |
| --- | --- |
| `30f47d478` | feat(labs): 0.5.107 dev 批次（A 批本体） |
| `5d3cf031f` | feat(agent): MUL-7355 claude fallback 用量去重 + MUL-6943 Fable 5.1 定价 |
| `145999a23` | feat(daemon): MUL-7423 worktree git 身份隔离 + UTF-8 安全预览 + 全 ID 日志 |
| `d01dc6053` | feat(server): MUL-7326 入队折叠 + GetTaskStatus 瘦身 + MUL-3428 头部剥离 + runtime-gone 推送 |
| `19a3fb251` | feat(auth): MUL-7028 会话过期终结 + MUL-7436 滑动续期（sid/CloudFront 有意不移植） |
| `768b22e51` | docs: 上游同步账本（分诊 + 移植结果 + 顺延清单） |

## 代码门禁（提交点上逐项跑过）

- `go test -count=1 -timeout 540s ./internal/... ./pkg/agent/...` 全绿
  （首轮一次 handler FAIL 未复现 = 仓内已记录的 TestQuickCreateIssueParentTrustBoundary
  共享行竞态 flake；另一次真 FAIL 为 A3 竞态测试暴露的存量 `parseUUID("")` panic，
  已按 mergeCommentIntoPendingTask 同款守卫加固后全绿）
- `pnpm typecheck` / `pnpm lint` / 全量 `pnpm test` 全绿
  （desktop 47 文件 386 测试、core 208、views 定点 70）

## 执行代理记录

3 路研究代理（claude 运行时 / claim+repocache / 会话+autopilot 鉴权）产出逐符号
锚点移植方案；Phase A 3 执行代理（包边界切分并行）+ Phase B 1 执行代理（会话对
串行）+ 补缺代理（731af7ccd）。中央门禁修复循环 3 项：
① `parseUUID(actorID)` 空串 panic 加固；② A3 中断遗留缺 `time` import；
③ UTF-8 测试暴露 executeAndDrain 完成竞态（测试侧错峰，生产侧残留见下）。

## 有意不移植（分歧裁决）

- MUL-7436 的 sid 双 cookie CSRF 层 + CloudFront/`cfSigner`/`mcn_`/disabled-user 分支：
  fork CSRF 绑原始 token 值（cookie.go HMAC），sid 要关的轮换竞态窗口不存在；
  FE retry-on-CSRF-403 评估后也不需要。
- 上游 mobile 会话续期文件（fork mobile 自主构建面）。
- MUL-7240/7365 存储类目（架构替换，fork 是 handler 层即时填充）、MUL-6986
  platform-skill 合并（顶撞 SKILL.md+source-map 仓规）、MUL-7236 self-host 遥测
  （STRIP）、计费/集成族/replica/法语 locale（SKIP）。

## 已知残留（本批引入或放大的）

1. `executeAndDrain` 完成竞态：Result 落地即 defer cancel，drain 协程 select 输了
   会丢缓冲中的尾消息（生产 stream-json 下 result 是末事件，窗口极窄）。
2. handler 侧 runtime-gone 目前 hub-local 投递；接 Redis loopback 需在 handler.go
   加 `DaemonRuntimeGone` 槽位 + router 断言（一行接线，RelayNotifier 已就绪）。
3. `mergeLegacyRuntimes`（daemon.go:605）与 `DeleteRuntimeProfile` 删除路径未接
   runtime-gone 推送（文件当时被并行代理持有）。

## 顺延下批（方案已写进账本，勿从零再查）

autopilot 鉴权族 MUL-6951→MUL-7090→MUL-7108-B（mig 290 `created_by` +
originator 落柱 + webhook token 广播脱敏）；MUL-7344 claim 快照增量
（~1000 行，需 MUL-6984-final 对齐）；571e61128 WS 心跳 lease 化；
a075e58b8/d1c7ec25b 模型目录发现对（PR-A/B 拆分）；MUL-7120 ACP ghost session。

## ship 链结果（装后回填）

- [ ] 快照 / 迁移 / bundle-cli / 构建 / 打包 / asar 校验
- [ ] 安装 + 嵌套二进制签名
- [ ] 冷启动验证 + :8090 PID + /health
- [ ] 装后数据完整性（workspace 计数）+ 安装版本号
