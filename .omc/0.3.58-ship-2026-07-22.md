---
name: 0.3.58-ship-2026-07-22
created: 2026-07-22T11:00:00Z
updated: 2026-07-22T11:00:00Z
status: complete
---

# Multica 0.3.58 ship — 2026-07-22T19:00+0800

`/Applications/Multica.app` 升级到 0.3.58 (上游 0.4.0 cherry-pick: UI 3 处)。
替代 0.3.57 安装版。

## 起点

接续 0.3.57 ship (`.omc/0.3.57-ship-2026-07-22.md`),`epic/0.3.58-cherry-pick`
分支 detached from `033b036`,51 文件未提交改动 (0.3.57 留下的工作树)。

新需求: 集成上游 0.4.0 修复,**只挑不引入 cloud / composio / PostHog /
workspace-member / invitations / auto-update / Google OAuth 的改动**。

3 个调研报告已就位:
- `.omc/cherry-pick-ui-0.4.0.html` (23 候选)
- `.omc/cherry-pick-bug-0.4.0.html` (16 候选)
- `.omc/cherry-pick-perf-0.4.0.html` (0 候选,已同步)

## 实施

3 PR 实际改动 (PR-D + PR-E 已由 0.3.56 commit 覆盖,无需新代码):

| PR | 文件 | 改动 |
|----|------|------|
| A | `packages/ui/styles/tokens.css` | +24 行 (5 surface-system tokens × light/dark + 注释) |
| B | `packages/ui/styles/base.css` | +44 行 (sidebar resize cursor / reduced-motion 8 个 .animate-* / in-page find highlight) |
| C | `packages/ui/hooks/use-scroll-fade.ts` | +60 行 (axis: "horizontal" 参数 + start/end 状态 + linear-gradient 横向) |

总改动: **3 文件 +128 行**。**纯 CSS + hook,无 schema 改动,无新依赖**。

## 风险点 vs 实际发生

| 风险 | 实际情况 |
|------|----------|
| UI #1 CSS token mythos-boost-pulse 冲突 | ✓ 0 冲突 (fork 0.3.56 mythos 块只 1 行,token 添加 24 行在 light/dark 段) |
| UI #2 i18n 漂移 (拉 en.ts 需补其它 3 语言) | ✓ 0 漂移 (本次不涉及 i18n) |
| 架构反向回归 (切勿 `git checkout -- file`) | ✓ 严格逐文件 Edit,没碰 `index.ts` / `bundle-cli.mjs` / `updater.ts` |
| PR-D triggerRestart test 重 | ✓ 跳过 — agent 报告错,现有 0.3.56 已有 3 个 TestTriggerRestart_* |
| PR-E IsBlockedEnvKey + freeze-breadcrumb | ✓ 跳过 — agent 报告错,现有 TestIsBlockedEnvKey (11/11 keys 覆盖) + freeze-breadcrumb.test.ts 已存在 |

## Ship 链

| 步骤 | 结果 |
|------|------|
| 1. pre-update-snapshot (reused) | `pre-update-20260722-174244` (5h 前) |
| 2. migrate up | ✓ 无 pending (0.3.58 无 schema 改动) |
| 3. bundle-cli + electron-vite build | ✓ 2.03s |
| 4. electron-builder --mac --dir | ✓ Multica.app 786M,ad-hoc signed |
| 5. pkill + cp -R 替换 | ✓ /Applications/Multica.app → 0.3.58 |

## Cold-start 三检查 + row parity

启动后 10s 内:

| 检查 | 结果 |
|------|------|
| lsof -nP -iTCP:5432 -sTCP:LISTEN | ✓ `postgres 97921` LISTEN |
| lsof -nP -iTCP:8090 -sTCP:LISTEN | ✓ `server 98004` LISTEN |
| curl /health | ✓ `{"status":"ok"}` |

Row parity:

```
workspace=1            (严格匹配 0.3.57 baseline)
issue=220              (一致)
comment=1346           (一致)
agent=92               (一致,纯 UI 改动,DB 无影响)
```

## 与 0.3.57 baseline 对比

| | 0.3.57 安装版 | 0.3.58 新安装版 |
|---|---|---|
| 版本号 | 0.3.57 | 0.3.58 |
| `--app-shell` / `--page-canvas` / `--surface-raised` tokens | ❌ | ✅ |
| `--menu-shadow` / `--floating-shadow` | ❌ | ✅ |
| 侧边栏拖拽 ew-resize cursor 跨边界 | ❌ | ✅ |
| 8 个 .animate-* reduced-motion opt-out | 部分 | 完整 |
| In-page find highlight (Cmd/Ctrl+F) | ❌ | ✅ |
| `useScrollFade(ref, 32, "horizontal")` 横向 fade | 静默回退 vertical | ✅ |

## 验证清单

- `pnpm --filter @multica/ui typecheck` → 0 errors
- `pnpm --filter @multica/desktop typecheck:web` → 0 errors
- `go run ./cmd/migrate up` → no new migration
- 启动 10s 内 5432+8090 LISTEN, /health 200 OK
- Row parity 与 0.3.57 完全一致 (UI-only 改动)

## 后续会话起点

- 想 ship 0.3.59: 需先 commit 当前 51 文件改动到 epic/0.3.58-cherry-pick 分支
- 想 cherry-pick 更多上游修复: 看 3 个 HTML 报告
  - **已应用 3 个 UI 候选 (PR-A/B/C),剩余 20 个 UI 候选 + 16 bug 候选 + 0 perf 候选**
  - 高价值剩余: `tab-bar.tsx` 上游简化版 (fork 反向回归警告) / `desktop-layout.tsx` / `tab-content.tsx` / `useActorName` 修复 / 9 个 P0 bug test
- 0.4.0 上游半合并改动 (670 文件) 仍在 `stash@{0}` (epic/upstream-0.4.0 分支)