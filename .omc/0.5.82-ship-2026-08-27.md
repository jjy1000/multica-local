# 0.5.82 — TimesFM Forecasting Lab (Work Line 2 of the 0.5.81–0.5.83 roadmap)

**Ship date**: 2026-08-27
**Branch**: `epic/0.5.82-timesfm` → merged `epic/0.5.72-followups`
**Baseline**: 2a4ea5b09 (0.5.81 + audit-batch closure)
**Merge**: 510225f1b · **Bump**: 5e3872c3e
**Worktree**: `/Users/jiangjianyan/jjy/multica-exploration-dev-0582`
**Result**: `/Applications/Multica.app` = 0.5.82, ship-mac 7-step chain exit 0, cold-start PASS ~6s

## What shipped

Full inventory in [`.omc/release-notes-0.5.82.md`](release-notes-0.5.82.md). Commit chain on the branch:

| # | Commit | Content |
| --- | --- | --- |
| 1 | `8b06c8119` | vendor: timesfm 2.5 torch-stack source + offline runner |
| 2 | `a36a8a148` | desktop: run_loopback FastAPI wrapper |
| 3 | `16dca8f1c` | build: bundle timesfm resources + electron-builder entry |
| 4 | `4aa32beb7` | server: catalog flag + manifest + dispatch-contract pin |
| 5 | `795f1f0f8` | server: mig 275 + sqlc |
| 6 | `5bf31a0ac` | server: migration static pin test addendum |
| 7 | `e7e3cde9e` | server: handlers + leader install |
| 8 | `9145199de` | views: route suffix + core forecast-runs hook (D1) |
| 9 | `cb0cb13ef`+`410475839` | desktop: timesfm-lab route + records-only view (D2; addendum after an atomic `git add` pathspec failure) |
| 10 | `57e9308ae` | views: LabOutputPanel compact timesfm reader (D3) |
| 11 | `233f2c1fc` | server: multica-timesfm builtin skill + contract pin (D4) |
| 12 | `24ad6412a` | typecheck fixes: i18n registry + noUncheckedIndexedAccess + i18next `count` |

## Gates (all run in the 0582 worktree, pre-merge)

- `pnpm typecheck` 6/6
- core vitest 816/819 — 3 failures = ledgered baseline (api/schemas ×1, auth refreshMe ×2)
- views vitest 1746/1788 — 42 failures = ledgered baseline, exact 9-file match (inbox ×29, issue-detail ×3, description-preview ×2, math ×2, chat-input ×2, markdown-paste/highlight/agents-tabs ×2)
- desktop vitest **370/370** (47 files)
- `go build ./...` + full `go test -count=1 -p 1` with `DATABASE_URL` — all packages ok (DB-backed runs verified real)

## Data safety

- Pre-migration backup: `~/.multica/backups/db-pre-wl2-mig-20260827T140339Z.sql` (105MB, pg_dump 17 via `/opt/homebrew/opt/postgresql@17/bin/pg_dump`) + backup.sh 22:03 — migration 275 applied only after both.
- ship-mac step 1 pre-update snapshot + step 6b local backup (`.omc/backups/<TS>/0.5.82-ship/`).

## Ops notes

- Cold-start verifier resolved expected version 0.5.82 from the primary checkout WITHOUT `EXPECTED_VER` (merge-back landed first — the 0.5.81 staleness caveat is retired).
- `pocketbase` restart policy left at `no` (revert: `docker update --restart=unless-stopped pocketbase`); it did not hold :8090 during the verify window.
- Wheelhouse + model weights remain machine-local prerequisites (gitignored); first-class `model` provenance needs `~/.multica/models/timesfm/model.safetensors` seeded and `scripts/build-timesfm-wheelhouse.sh` run once on an arm64 build machine.
- Mid-ship finding: the client-half agent previously reported "cancelled" had actually landed D1 and left D2 files uncommitted/unverified — resumed by verification-and-completion instead of re-delegation.

## Post-ship docs

CLAUDE.md / AGENTS.md headers refreshed to 0.5.82 (docs-sync check green); roadmap v3 addendum gains the WL2 closure row.
