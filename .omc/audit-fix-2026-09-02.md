# 审计修复 2026-09-02(P0 三件套 + CI 首跑战果)

外部审计报告(epic @ `ad225c8d8`)三点 P0 逐条独立复核**全部属实**后,当日修复完毕。
逐条复核证据见对话记录;此处只记修复与 CI 首跑新战果。

## P0-1 迁移 167 空库必挂 → 修复(`625646948` + `841c7c166`)

- 空库 `migrate up` 在 167 死于 42704(约束 `agent_task_queue_accountable_`
  `matches_originator` 只在 167 自身 DROP+ADD,列 240 才有)。
- **审计的修法不完整**:`DROP IF EXISTS` 之后同文件 `ADD CONSTRAINT` 会以
  42703(列不存在)再炸;必须门控整个 agent_task_queue 块。就地改 167 是
  安全的:`schema_migrations` 只有 (version, applied_at),无校验和,存量库
  按版本跳过。
- 新增 **288**:宽松 CHECK 的真正落地迁移(定义比对的 DO 块:缺→建、已是
  宽松版→no-op、异版→换)。线上库实测已是宽松版(240 注释"constraint is
  absent today"对该谱系不成立),0 违规 vs 严格口径 236 违规(永远不能回严)。
- 空库全链 1→288 冒烟过、复跑幂等;288 已应用到线上库(纯 no-op)。

## P0-3 门禁口径依赖 → 修复(`ca5fd2811`)

- `TestLLMCallHandler/502_when_no_provider_CLI_on_PATH` 在 `check.sh` 全量
  source `.env`(`MULTICA_CODEX_PATH=codex`)下 FAIL——PATH 隔离被
  pickProviderCLI 的 env 优先级绕过。
- 两个 provider-discovery 子测试(502 路径 + happy path)统一经
  `clearProviderPathOverrides` 清空 `MULTICA_<PROVIDER>_PATH`(空值=未设置)。
- 双口径实测绿:仅 DATABASE_URL(AGENTS.md 记载口径)与全量 .env(更严)。

## P0-2 fork 无 CI → 修复(`5b9a1a07f`)

- `ci.yml` 触发加 `epic/**` push + `workflow_dispatch`;installer 矩阵
  (macOS 10× 计费)限定 PR/main。
- 首跑 run 33602759939 即抓到**审计都不知道的 8 个 fresh-schema 失败**——
  本地全绿的根源是对着增量升级的线上库跑测试。

## CI 首跑战果(全新库暴露的三类问题)

1. **真 bug:originator 写入 agent id**(`1b1681327`)。`enqueueSingle`
   `CommentTrigger` 用 `parseUUID(actorID)`(评论作者)当任务 originator——
   agent 评论时写入的是 agent 表 id,违反 240 声明的 `→ user(id)` FK。线上
   库因 240 的 `ADD COLUMN IF NOT EXISTS` 在列已 out-of-band 存在时整体跳过
   (FK 从未建立)而静默吞了 **153 行脏数据**。修复:switch 改用调用方已解析
   的 originator(member=actor、agent=链顶人类、""→NULL——MUL-4525 §2 注释
   本来就写着这个意图);reconcile 与 mention 扫描路径把解析值穿透到入队。
2. **迁移 289**(`841c7c166`):NULL 掉 153 行悬挂引用 + 给线上库补齐两个
   FK(幂等 DO 块,新库 no-op)。已应用:脏数据清零、FK 落位。
3. **宿主状态依赖 ×2**:`TestLoadConfig` 要求 PATH 有 agent CLI(裸 runner
   无)→ 假 `claude` 桩(`72ce44065`);desktop `pg-bootstrap` 写 sentinel 不
   保证 `~/.multica` 存在 → `mkdirSync recursive`(`34811224f`,真实新机器
   首跑会崩的同款路径,空 HOME 复现→修复→10/10 绿)。

## 验证

本地双口径全量 Go 套件:全新库@289(CI 同款,无 env 变量)与线上库@289
(全量 .env 变量)均 0 FAIL;desktop 370/370(空 HOME)。docs-sync /
migrations-mirror 同步(551 文件)。CI run 33605565476:backend ✓(9m35s,
含空库迁移冒烟)、docs-sync ✓、installer 按门正确跳过。

## 遗留(审计其余发现,未列入本轮)

- 审计 4 个隔离库待用户确认后 DROP(含数据副本,勿擅自删)。
- git 子进程无 context(#4)、RLS 缺失(#5)、memory 47/55 失效引用的深扫
  (#6 只修了锚点路径)、AGENTS.md 头部膨胀(#7)、e2e 覆盖(#9)、11 条 lint
  warning(#10)、零字节备份清理(#11)。
