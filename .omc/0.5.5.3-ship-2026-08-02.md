# 0.5.5.3 Ship Log — 2026-08-02

**Version**: 0.5.5.3 (`apps/desktop/package.json`, Info.plist verified 0.5.5.3)
**Branch**: epic/0.3.58-cherry-pick (local project, no cloud push)
**Type**: 纯 FE 修复(自优化 view + issue-labs-section 链接去实验语义)

## 内容

### 修复:0.5.5.1 + 0.5.5.2 漏改的 self-opt 页面入口

0.5.5.1 已经把 self-opt 的**控制面**搬出实验域(catalog DefaultVal=true, flag gate stub, 9 个 HTTP handler 解 gating),0.5.5.2 把 **Labs tab** 隐藏了产品级 flag,但**遗漏**了 2 处:

1. **`/experimental/agent-self-optimization` view 仍以"实验"口吻展示**:bread crumb `实验性功能 / 智能体自优化循环`,FlagOffPlaceholder 文案"请先在「设置 → 试验性功能」中打开..."
2. **Issue detail 的"open panel"链接**仍能跳到 self-opt view(FLAG_ROUTE_SUFFIX 仍有 `agent_self_optimization: "agent-self-optimization"` 映射)

### 改动

- `packages/views/issues/components/issue-labs-section.tsx`:
  - `FLAG_ROUTE_SUFFIX` 删 `agent_self_optimization` 条目(同 0.5.5 阶段删 `agent_creation_studio` 一致)
  - 注释:issue detail "open panel" 链接对 self-opt issue 不再渲染
- `apps/desktop/src/renderer/src/pages/self-opt-view.tsx`:
  - 头部 doc 改 0.5.5.3 语义(view 是产品级 read-only,不是 opt-in lab)
  - 加 `<ProductLevelBanner />` 在 Intro 后:Sparkles 图标 + 绿底,明确"本功能已升格为产品内置",链接到 `/autopilots`(自动化工程页)改对应 autopilot 的 enabled 字段
  - `useExperimentalFlag("agent_self_optimization", true)` default 改 true
  - `FlagOffPlaceholder` 文案重写(从"请在 Labs 启用"改为"检查 FF_AGENT_SELF_OPTIMIZATION env 或 catalog 配置"—— 防御性)
- `apps/desktop/package.json`: 0.5.5.2 → 0.5.5.3

## 验证

- `pnpm typecheck`: 6/6 子包过
- 冷启动三查: Info.plist 0.5.5.3 = package.json 0.5.5.3、5432/8090 LISTEN、/health ok
- 纯 FE 修复,Go binary 未动(无 bundle-cli / sign step)
- self-opt 2 个 autopilot 仍 status=active,leader agent 仍在

## 风险 vs 结果

- **第三处遗漏修正**:与 0.5.5 阶段删 `agent_creation_studio` 路由映射对称,补完 self-opt 的入口语义
- **ProductLevelBanner 显眼**(Sparkles 图标 + 绿底 + 加粗"已升格为产品内置"):用户进 self-opt view 第一眼就看到,不会再以为这是个实验
- **FlagOffPlaceholder 文案**改防御性(原"请去 Labs 启用"是错误引导,0.5.5.1 后 Labs tab 已无此 flag)
- **route 仍保留**:`/experimental/agent-self-optimization` 路由没删,view 仍可访问(read-only overview);只是 issue detail 的"open panel" 链接不再指向它,sidebar 也没有入口

## 已知

- 0.5.5.3 是 0.5.5.2 的 UI 补完(同系列 patch)
- view 顶部 ProductLevelBanner 是 0.5.5.1 stub 行为的产品级入口解释,用户从 bookmark / 旧 URL 进来都能看到
- 0.5.6 待办(用户未要求,留作记录):删除 self-opt view 整个页面 + 删路由 / 删除 RecentLabsPanel / 删除 install handler / 删除 HIDDEN_LAB_KEYS 集合 / 删除 ListOptedInUsers query
