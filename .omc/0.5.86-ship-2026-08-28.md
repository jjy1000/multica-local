# 0.5.86 — Interaction-model law + assignee lock + report writeback + swarm consolidation + causal-graph readability

**Ship date**: 2026-08-28
**Branch**: `epic/0.5.72-followups`
**Baseline**: `fc01afd76` (0.5.86 dev record)
**Bump**: `f1647903a` (0.5.85 → 0.5.86)
**Target**: `/Applications/Multica.app` = 0.5.86 ✅ (cold start ~4 s, server PID 99786, server version 0.5.86)

## What shipped

Full inventory in [`.omc/release-notes-0.5.86.md`](release-notes-0.5.86.md).
Commit set `70a20d84f..f1647903a` (10 commits):

| # | Commit | Content |
| --- | --- | --- |
| 1 | `70a20d84f` | feat(views): causal graph readability overhaul — depth-banded ring layout, node drag, zoom/pan, animations |
| 2 | `8edc53089` | feat(views): issue lab progress card — per-lab run state + click-through, `lab-run-heuristics.ts` |
| 3 | `94f693f7e` | feat(server,views): interaction model + assignee lock (独立工作型 vs 辅助协作型) |
| 4 | `46c27cf51` | feat(server): lab report writeback — Pythia/TimesFM run reports land in the issue (mig 282) |
| 5 | `4bf578f89` | feat(server): swarm consolidation — single 蜂群 lab (mythos_swarm), swarm_topology frozen (mig 283) |
| 6 | `5e4d6a49a` | chore(desktop): track bundled migration copies 281-283 |
| 7 | `602281f4f` | test(server): DB-backed pins for the assignee-lock gate |
| 8 | `a3a78bacb` | fix(server): swarm_topology mutex preserved — lab-flip bypass requires the leader agent row |
| 9 | `fc01afd76` | docs: 0.5.86 dev record |
| 10 | `f1647903a` | chore(release): bump 0.5.85 → 0.5.86 |

## Gates (run pre-bump on `fc01afd76`-tree, serial)

- `pnpm typecheck` (full turbo): **6/6** (18.752 s, 5 cached).
- `go test -count=1 -timeout 600s ./internal/... ./pkg/agent/...` with `DATABASE_URL`
  exported from repo-root `.env`: **all packages ok, 0 FAIL**. Note: `server/.env`
  does not exist — the CLAUDE.md one-liner must run from the repo root; without
  the export the suite silently skips DB tests (0.5.79 lesson, re-confirmed).
- DB-backed suite confirmed live: `TestAssigneeLabLockGate` 8/8 subtests PASS
  (0.04 s) — not a silent skip.

## Ship chain (`bash scripts/ship-mac.sh --yes`)

1. **Snapshot** ✓ — app → `/Applications/Multica.app.0.5.85.pre-update-20260828-233948.bak` (814M), pg_dump 32M, git tag `pre-update-20260828-233948`, old backups pruned.
2. **migrate up** ✓ — all 541 files verified; 282/283 already applied in dev, no new SQL errors.
3. **bundle-cli** ✓ — 3 Go binaries built at version 0.5.86, migrations bundled (541 files intact).
4. **electron-vite build** ✓ (renderer integrity check passed).
5. **electron-builder --mac --dir** ✓ — no deadlock this run.
6. **Install + re-sign** ✓ — nested binaries signed, `multica --help` exits 0.
7. **Cold-start verify** ✓ — ~4 s, server PID 99786, version 0.5.86.
   `Row parity: psql-unavailable` is the known benign sub-check (verify script
   subshell lacks psql on PATH; manual check below).

## Row parity (manual psql, `PGPASSWORD=multica psql -h 127.0.0.1`)

| Table | Count | vs baseline |
| --- | --- | --- |
| workspace | 7 | unchanged (0.5.60 ship log recorded `workspace=7(未动)`; gold-baseline `1` in CLAUDE.md is a 0.3.x-era number, superseded) |
| issue | 404 | natural growth from ≈350 (0.5.60) — dev-stage lab issues expected |
| comment | 2583 | natural growth |
| agent | 132 (90 live + 42 archived_at) | archived 42 are historical user agents (中文名: 一线客服/交互设计师/…), NOT mig-283 swarm archives; no `^swarm_` rows remain — swarm consolidation cleanup already consumed |

Migrations 282/283 caused no data movement beyond their design (report_comment_id
column add + orphan swarm cleanup). Risk vs outcome: **low / shipped clean**.

## Known notes

- `psql-unavailable` in cold-start verifier = benign (0.5.43/0.5.78/0.5.80/0.5.83 precedent).
- `server/.env` does not exist in this checkout; `DATABASE_URL` lives in repo-root
  `.env` — ship-mac.sh step 2/7 relies on the caller exporting it (this ship did).
