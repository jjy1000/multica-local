# 0.5.90 Ship Log — 2026-08-30

> **Shipped**: `/Applications/Multica.app` = **0.5.90**（OpenMythos enhancer-only 外循环）。Chain: `bash scripts/ship-mac.sh --yes` exit 0（后台运行，全日志 `/tmp/ship-090.log`）。Release: `epic/0.5.72-followups`，commits `30d58b851`（code）+ `39597bb25`（docs）+ `9705557b5`（bump 0.5.89 → 0.5.90）。

## 打包前状态

- 工作树干净（bump 提交后）；dev 验证服务器已停（:8091 进程确认杀死）。
- 门禁（dev 周期内已跑，见 [`.omc/0.5.90-dev-2026-08-30.md`](0.5.90-dev-2026-08-30.md)）：Go 38 包 0 FAIL（`-count=1 -p 1` + DATABASE_URL + 服务器停机）；typecheck 6/6；views vitest 42 失败 = 既有基线原数；desktop 370/370；live API 全环 :8091 **15 pass / 0 fail**。

## 七步链结果

1. **pre-update snapshot**：`/Applications/Multica.app.0.5.89.pre-update-20260830-203106.bak`（整包回滚点）+ `~/.multica/backups/pre-update-20260830-203106`（pg_dump 32M + config/KB），旧备份按保留策略剪枝（keep 3）。
2. **migrate**：`go run ./cmd/migrate up` — 280 条全 skip（本周期零新迁移，符合预期）。
3. **bundle CLI** → 4. **vite build** → 5. **electron-builder**（ad-hoc 签名）→ 6. **install + re-sign** → 全部 PASS。
6b. **repo backup**：`.omc/backups/2026-08-30-1232/0.5.90-ship/`（保留 30 份活跃，旧的移入 `_archive/`）。
7. **cold-start verify**：`PASS: cold start verified, server is 0.5.90`。

## 安装后核验

- `CFBundleShortVersionString` = **0.5.90**；`:8090/health` → `{"status":"ok"}`。
- **asar 品牌检查**：安装包 `app.asar` 内 80 处 "OpenMythos" 串——新 UI（header 图标/popover 文案）与品牌化locale 确认进包（替代本周期顺延的 Electron UI audit 的冒烟部分；完整 Playwright audit 仍可按需补跑）。
- 回滚点：整包 `.bak`（0.5.89）+ pg_dump；亦可用 git `9705557b5^` 走 ship 链重建。

## 遗留

- 0.5.91 账本照旧（见 dev record）：嵌入收敛 + 自适应早退、子 issue `hidden_at`（mig 285）、self-opt 深接线、roster 复用、WS4/WS5、CompleteTask dispatched no-op（随 WS4）。
