# 0.5.86 dev cycle — 2026-08-28 (unshipped)

**Branch**: `epic/0.5.72-followups` · **Commits**: `70a20d84f..a3a78bacb` (8) · **NOT packaged / NOT shipped** — user directive 「你不需要打包，只需要完成和验证代码这些」+ safety constraint (no packaging/version-replacement until code changes are tested; incremental backups honored — working tree committed atomically, no amend, no push).

## Trigger

User review of the packaged 0.5.85 app: ① runtime audit of enabled plugins; ② causal graph (popup + lab page) too crowded, nodes not draggable, animations need polish; ③ two swarm sidebar entries (Mythos 蜂群 v1 + 蜂群拓扑) must consolidate into one deepened swarm lab (OpenMythos-style: plan → spawn sub-agents → self-track → swarm-complete → auto-reclaim agents, no multica pollution); ④ Pythia on an issue must not offer manual assignee choice, must surface run progress + click-through to the lab view, and must deliver its report as a text comment on the issue; ⑤ overall polish + a crisp two-class plugin taxonomy.

## ① Packaged-app runtime audit (finding, 19:52–19:56)

| Lab (enabled) | State |
|---|---|
| causal_graph | ✅ healthy — 21 nodes / 16 edges real, write side active |
| pythia | ✅ healthy — 9 runs, latest 10-round mixed source run today 19:56; no issue writeback (fixed this cycle, ③) |
| mythos_swarm | 🟡 3 completed with coda comments + final_issue_id; 3 stuck `running` since 08-24 (async-engine follow-up) |
| swarm_topology | ❌ structurally dead — 3/3 stuck preparing/research; probe agent stuck; `topology_spec` had zero writers (consolidated away this cycle) |
| timesfm / claude_science_lab / semantica | enabled, 0 usage (no error) |
| llm_wiki_bridge | proxy-only, by design |

Boot-provisioned per-workspace `swarm_coordinator` + `agent_creation_expert` pairs are legitimate (not duplicates). Orphan signature: `swarm\_%\_%` (≥2 underscore segments) with no `swarm_role` row.

## ② Implementation

### Interaction-model law (⑤ taxonomy)
- `server/internal/experimental/catalog.go`: `Flag.InteractionModel` + consts + `InteractionModelOf` / `IsAssigneeModelLab` / `IsAuxiliaryModelLab`.
  - `assignee` 独立工作型: claude_science_lab, pythia_oracle, mythos_swarm, swarm_topology, semantica, timesfm
  - `auxiliary` 辅助协作型: causal_graph, llm_wiki_bridge
  - `""` legacy (chat_pin_ui, code_canvas, user plugins) keeps pre-0.5.86 behavior
- `handler/experimental_flags.go`: `ExperimentalFlagResponse.interaction_model` stamped in built-in + user-plugin loops. `packages/core/types/experimental.ts`: optional field.

### Assignee-lock hard gate (④ + ③ picker law)
- `handler/issue.go::assigneeLabLockError` — assignee-model labs lock the assignee slot to the lab leader. Empty assignee always allowed (0.3.46 leader-rewrite fills); non-leader → 400 naming the leader; leader row missing → install-first 400; mythos (leader-less roster) keeps the 0.3.33 strict no-manual-assignee mutex through the same path.
- Create gate: general lock. Update gate scoped by touched fields: assignee-touched → full post-state check; lab-only flip → allowed ONLY when the leader AGENT ROW exists in the workspace (so the rewrite can land). Regression caught by re-running the legacy pins at HEAD: `swarm_topology`'s `swarm_coordinator` is bootstrap-provisioned, so name-level resolution alone 200'd and stranded the human assignee, breaking the 0.5.22/0.5.60 mutex pins — fixed in `a3a78bacb` (row-presence requirement; row-missing labs get install-first 400; legacy/auxiliary labs keep tag-and-keep-assignee freedom).
- `defaultLabLeaderForKey` + service `defaultLeaderAgentForLab`: `timesfm → timesfm_oracle` added (was missing since 0.5.82 — leader-rewrite silently no-op'd).
- FE: `packages/views/issues/components/pickers/assignee-lab-lock.ts` (flag-driven `isAssigneeLabLocked`, replaces hardcoded ASSIGNEE_MUTEX_LABS) + 6 unit tests; wired into lab-picker, issue-detail, create-issue (ManualCreatePanel now loads flags).

### Lab report writeback (④)
- New `handler/lab_report_writeback.go::postLabRunReportComment` — text report comment authored by the leader agent (AuthorType=`agent`, EventCommentCreated, mirrors TaskService.createAgentComment); system-author fallback (all-zero UUID, mig 107).
- `forecast_issue.go` (Pythia): persist captures run row + writeback block (6 s ctx), `pythiaIssueReportContent` renders sources/notes. `timesfm_forecast.go`: writeback after persist success (`timesfmIssueReportContent` + provenance label).
- Mig **282** `report_comment_id UUID` on `pythia_forecast_run` + `timesfm_forecast_run`; queries `Set{Pythia,Timesfm}ForecastRunReportComment` with `SET report_comment_id = COALESCE(report_comment_id, $2)` — exactly-once, always returns row.

### Swarm consolidation (③)
- ONE 蜂群 lab: `mythos_swarm` is the entry. `swarm_topology` frozen: catalog `HideFromIssueLabPicker:true` + freeze comments, manifest `entry_points.sidebar → []`, amber consolidation banner on desktop + web views (i18n key `consolidation_notice` ×4 locales).
- Mig **283**: stuck non-terminal `swarm_run` rows → `failed` (`orphaned pre-consolidation run`); orphan `swarm\_%\_%` agents archived. GC: `ListStalledSwarmRunsForGC` (2× max_runtime_hours reap) + `ArchiveSwarmRoleAgentRows` wired into `swarm_gc.sweep`/`archiveOne`.
- Deepened swarm semantics stay on `mythos_swarm`'s roster runner; mythos async-engine unification (porting swarm orchestrator patterns) recorded as fast-follow, not attempted this cycle.

### Causal-graph readability (②)
- `causal-minimap.tsx` rewritten: depth-banded rings — ≤3 center seeds (was: all issue-bound nodes at ring 0 → 18px chords at 21 nodes), per-ring radius `r_k = max(maxR·k/(maxRing+1), L_k·34/2π)` (34px node spacing floor), parent-angle ordering; `clientToViewBox` pure helper with injectable matrix (jsdom-testable); node drag (`positionOverrides` prop, 5px threshold + pointer capture pattern), `onBackgroundPointerDown`, reduced-motion-gated motion animations, dash-flow only on dashed ACTIVE edges.
- New `causal-graph-canvas.tsx`: wheel zoom at cursor + pan + zoom buttons + reset. Desktop lab view: per-view position override Maps, `pollPaused` while dragging, 重置布局 button.
- `issue-causal-icon.tsx` popup: default depth 1; depth 2 → depth-1 map + "+N 节点 · M 边" summary row (440×280).
- `packages/core/experimental/causal-graph-queries.ts`: optional `{pollPaused}` on both hooks.

### Issue lab progress card (④ visibility)
- New `packages/views/issues/components/lab-progress-card.tsx`: per-lab run state on issue detail + click-through to the lab view (runs while active too, not only after delivery); `lab-run-heuristics.ts` (pythia sessionStorage heuristic extracted + `isTimesfmRunRecent` 120 s) shared with `lab-output-panel.tsx`; 12 `progress_*` i18n keys ×4 locales.

### Migrations
- **282** `lab_report_writeback` (ADD COLUMN IF NOT EXISTS report_comment_id ×2 tables), **283** `swarm_consolidation_cleanup`. Both forward-only, bundled copies under `apps/desktop/resources/server/migrations/` tracked + cmp-verified.

## ③ Verification

- Go: full `internal/handler` package uncached with `DATABASE_URL` exported → PASS 14.7 s (after the a3a78bacb fix; the interim run exposed the swarm_topology pin break). `internal/experimental` + `internal/service/...` PASS. New tests: `TestAssigneeLabLockGate` (8 subtests), `TestLabReportWriteback` (4), `TestCatalogInteractionModelContract`, FE `assignee-lab-lock.test.ts` (6) + heuristics tests; views causal/progress suites 19+35, core 8.
- TS: `pnpm typecheck` 6/6 workspaces. Views full suite 1806 pass / 42 pre-existing failures across 9 files (issue-detail×3 + core×3 baseline-confirmed identical at HEAD via `git stash push -u`; remaining ~36 in untouched domains).
- DB used for tests: the packaged app's bundled Postgres started standalone (`pg_ctl -D "~/Library/Application Support/Multica/pgdata" -o "-p 5432 -k /tmp"`) after the packaged server was quit; stopped after the cycle.

## ④ Not done / next

- NOT packaged (user directive). When shipping: `bash scripts/ship-mac.sh --yes` from the PRIMARY checkout only.
- Next cycle (0.5.87+): DB-backed handler integration tests for `ClaimTaskByRuntime`; Tier C Pythia hypothesis→evidence closure; historian/verifier automation; evolver window bound; mythos async-engine unification (swarm orchestrator port) fast-follow; ~39 pre-existing TS test failures in untouched domains.
