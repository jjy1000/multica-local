# 0.5.46 Ship Log — 2026-08-20 (pending ship)

**Version**: 0.5.45 → 0.5.46 (apps/desktop/package.json only)
**Branch**: epic/0.5.13-integration
**Status**: pre-ship — version bumped + release notes + headers updated, ship chain NOT yet run
**Goal**: infra batch — smallest pieces that unblock 0.5.45's re-cherry-pick attempts

## Commits (2 infra + 1 chore release + 1 post-ship docs pending)

```
fc52836cd feat(0.5.46): port AutopilotQuotaExceededError sentinel type (32 LOC)
5f0f1f592 feat(0.5.46): port upstream SanitizeTextForPostgres + SanitizeJSONForPostgres (literal alignment)
256919a72 docs(server/CLAUDE.md): document 0.5.45-0.5.46 audit-batch abstractions (/init improvement)
```

## Re-cherry-pick findings (0.5.45 batch blocked 6 SKIPs)

| Skip | 0.5.45 progress | 0.5.46 progress | Still blocked |
|---|---|---|---|
| MUL-6472 dispatch leak | `entitlement` ✅ | `AutopilotQuotaExceededError` ✅ | `clientErrorMessage` + handler sqlc queries fork lacks |
| MUL-6310 NUL bytes | 4 abstracted pkgs ✅ | sanitization aligned ✅ | caller integration (autopilot/skill/task.go) — sqlc queries fork lacks |
| CJK markdown | `cjk-emphasis.ts` ✅ | — | fork's `readonly-content.tsx` integration |
| MUL-6417 follow-ups | — | — | daemon workflow refactor + skill prompt |

**Conclusion**: 4 of 6 SKIPs still blocked because the upstream code paths they
touch require sqlc queries / DB fields the fork's schema doesn't have
(migrations 261-374 not landed). Next unlock wave = schema migration sweep +
sqlc regen + handler refactor.

## Deferred to 0.5.47+

- **CJK markdown integration** — `cjk-emphasis.ts` into fork's `readonly-content.tsx`.
- **MUL-6472 / MUL-6310 re-attempts** — need schema migration sweep first.
- **Schema migration sweep** (migrations 261-374) — the dominant blocker.
- **MUL-6417 follow-ups** — daemon workflow refactor.

## Gates

| Gate | Result |
|---|---|
| `pnpm typecheck` (full turbo) | 6/6 ✅ |
| `go test -count=1` `./internal/util/...` | PASS (sanitization) |
| `go build ./...` | PASS |
| Pre-existing mythos panic | `TestTickSupervision_CompletionByFinalIssueStatus` (CLAUDE.md documented) |
| `scripts/check-agents-docs-sync.mjs` | PASS (5 guarded categories) |
