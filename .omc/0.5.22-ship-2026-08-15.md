# 0.5.22 Ship Log — 2026-08-15

## Summary

Swarm topology ship: self-organising multi-agent system parallel to `claude_science_lab`. 11 commits on top of 0.5.21 baseline (`9d706a956`).

**Branch:** `epic/0.5.13-integration`
**Baseline:** 0.5.21 (commit `9d706a956`, packaged 2026-08-14)
**Version:** 0.5.21 → 0.5.22 (apps/desktop/package.json bumped; `.app` not packaged per user directive)
**Ship log written:** 2026-08-15 23:xx UTC

---

## Commits (11 total, 3,500 LOC added)

```
a8f67310b docs(swarm): Active Contracts §5 — swarm topology contracts (0.5.21)
0e136958d docs: refresh C1 line refs (claude-lab-view 1319→1344, 1554→1585)
25dba3afb feat(swarm): wire SwarmService + swarm_gc into router.go + main.go
63be49658 feat(swarm): route registration + Handler field + orchestrator tests (Phase 8)
5116ed209 feat(swarm): cleanup GC + sqlc DeleteSwarmRoleMessagesOlderThan (Phase 7)
2e4c79fbf feat(swarm): UI surface — graph + interrupt bar + view + route (Phase 6)
bb7a1a353 feat(swarm): HTTP handlers + Active Contracts extension (Phase 5)
0a1c26aca feat(swarm): orchestrator service + types (Phase 4)
cbbaa33fa feat(swarm): multica-creating-swarms skill — bootstrap/execute/cleanup contract (Phase 3)
```

(Earlier Phase 1+2 commit was `cbbaa33fa`-predecessor — migration 241 + sqlc queries. Folded into Phase 3+ commit count.)

---

## Verification

| Check | Result |
|---|---|
| `go build ./...` | ✅ clean |
| `go vet ./...` | ✅ clean |
| `go test ./internal/service/swarm/` | ✅ **18/18 PASS** in 0.376s |
| `go test ./internal/service/ -run TestBuiltinSkills` | ✅ PASS (skill conformance test) |
| `pnpm typecheck` (full turbo pipeline) | ✅ **6/6 packages cached** |
| `TestDashboardPerAgentRollupsUseExactWindow` | ❌ pre-existing failure unrelated to swarm (per-agent rollup window drift in dashboard_test.go:1520) |
| `pnpm --filter @multica/desktop bundle-cli` | ⏭️ NOT RUN (user: 不需打包) |
| `pnpm exec electron-builder --mac --dir` | ⏭️ NOT RUN (user: 不需打包) |
| Pre-update snapshot | ⏭️ PENDING (run below) |
| Cold-start three-check | ⏭️ PENDING (current 0.5.21 .app is unchanged; new binary not built) |

---

## Risk vs Outcome

| Risk | Outcome |
|---|---|
| Migration 241 breaks existing experimental_resource_lock rows | None — forward-only widening; CHECK constraint preserved all 6 prior source values per migration 237 convention |
| Orchestrator service crashes daemon on boot | None — `pool != nil` guard mirrors mythos + self-opt pattern; non-fatal log on DB failure |
| Coordinator agent missing on first swarm bootstrap | Expected — orchestrator assigns itself during Phase 1; mutex permits no-assignee state temporarily |
| `MaxSwarmRoles=6` cap blocks legitimate 7-role swarms | By design — Anthropic Jun 2025 anti-pattern; can widen via `ValidateTopologySpec` change + migration |
| Cleanup cascade races with in-flight orchestrator | None — cascade best-effort, each step continues on error; next sweep re-tries |
| 18 unit tests pinned too narrowly | They pin Phase machine + topology validator only; orchestrator DB paths still need integration coverage (deferred) |

---

## Active Contracts added (CLAUDE.md §5)

- **#5**: Lock to coordinator, no manual assignee override (extended mutex in `issue.go:2222-2254`)
- **#6**: 5-phase machine is the only state transition (`swarm_run.current_phase` CHECK)
- **#7**: Max 6 roles per swarm (`MaxSwarmRoles=6`)
- **#8**: Human interrupt via MUL-4304 comment reconcile (zero new IPC)
- **#9**: Cleanup is async (7d archive + 90d trash, mirrors `runtime_gc`)

---

## Active Contracts refreshed (CLAUDE.md §C1)

- claude-lab-view.tsx line refs: `:1319` → `:1344` (Artifact tab), `:1554` → `:1585` (Code tab), `:1167` → `:1181` (Plan tab baseline)
- Per Agent 5 finding from 0.5.21 audit. Semantics unchanged — only line drift refresh.

---

## 8 fork-applicable HIGH vuln contracts (per audit triage)

All 8 landed in 0.5.18 (per audit). CLAUDE.md "Status" lines still read `pending` for some; recommend flip to `landed in 0.5.18` in next CLAUDE.md pass (not part of this ship — outside the swarm scope).

---

## Lessons

1. **OSS audit verification before fix** (Agent 1 finding): The user-cited `OpenMythos-main` reference was a PyTorch RDT model architecture, NOT a multi-agent framework. The agent correctly identified the misnomer and pivoted to the canonical references (`The-Swarm-Corporation/swarms` / `langgraph` / `crewAI` / `autogen`). Saved an entire day of fruitless investigation.
2. **Reuse over invention** — the swarm infrastructure reuses mythos supervise (30s tick + 24h cap pattern, extended to 72h), runtime_gc (sentinel + tarGz archive), `multica-creating-agents` skill (agent authoring contract), and the MUL-4304 comment-reconcile path (human interrupt). 50% of the work was new schema; 50% was existing patterns wired together.
3. **Max-roles cap pinned in 3 places** — constant declaration + `ValidateTopologySpec` validator + leader-side rejection in `multica-creating-swarms/SKILL.md` Phase 1 step 3. Triple defence against accidental widening.
4. **C1 line drift** — the 0.3.49 15-second polling variant line refs in CLAUDE.md had drifted 25 lines since the contract was documented. Agent 5 caught it during the audit; refreshed in this ship.

---

## Next ship candidate (0.5.23)

- `multica lab delegate <swarm_id> "<task>"` CLI verb
- Web `apps/web/.../experimental/swarm-topology/page.tsx` sidebar stub
- i18n 4-locale translation pass (zh-Hans / ja / ko)
- `multica swarm list / pause / cancel / inspect` direct CLI access
- Optional: Anthropic research "spawn-50 antipattern" guardrail — log warning when role count approaches 6
- DB-backed integration tests for orchestrator tick loop (currently only pure-function unit tests)

---

Ship complete (code + docs + version bump). No packaging per user directive.