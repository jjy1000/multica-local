---
name: 0.5.18-ship-2026-08-13
created: 2026-08-13
type: ship-log
status: dev (阶段 0-3 committed; 阶段 4/5 in progress)
---

# 0.5.18 — Lab environment batch (dev, committed 2026-08-13)

## 概述

Lab environment plan (`.omc/plans/lab-environment-0.5.18-plan.md`) 阶段 0–3 落地, 13 atomic commits.
目标: 建立让 Multica 任务能自建实验室插件的环境 + 内置插件如期运行。

## 阶段 0 — reliability/security P1 (4 commits)

- Pythia proxy allowlist +`/agent/events`+`/scorecard/resolve` (DT-P1-2)
- IPC `ensure-up` gated on safety-net blacklist (DT-P1-1)
- `user_` flag-key prefix deduped via `experimental.UserPluginPrefix` (BE-P1-2)
- `lab_managed` stamped on single-fetch `GetAgent`/`GetSquad` (SEC-P1-7, was list-only)

## 阶段 1 — G1 core (1 commit)

- user-plugin `runtime_kind: subprocess` now executes `manifest.runtime.command`+`args` (argv, no shell) — was 501

## 阶段 2 — G1 verify (4 commits, subagent)

- `multica lab delegate` normalization + 4 poll-path tests
- `multica-lab-builder` SKILL updated (6 built-ins, subprocess contract, self-test checklist)
- `scripts/lab-plugin-smoke.sh`

## 阶段 3 — G2 code_canvas (3 commits, subagent)

- real stdlib-only `/render` service (vendor + resources run.sh)
- render view + i18n (4 locales)
- manifest + `multica-code-canvas` SKILL reflect `/render` contract

## 验证

- `pnpm typecheck` 6/6
- `go build ./...` OK; `go test` green (2 pre-existing/env failures unrelated: TestQuickCreateIssueParentTrustBoundary flake + TestSmoke_LLMWikiBridge_LiveDesktop sandbox denial)
- run.sh: bash -n + py_compile + curl /health 200 + /render 200 + XSS escaping + stdin=/dev/null survive
- locale 13 keys × 4 languages parity

## Zero migrations

## Out-of-scope / next

- 阶段 4: LabOutputPanel + iframe auth proxy — spec in progress
- 阶段 5: 6 HIGH vulns (F-002/005/006/008/013/027) — in progress
- ship 前: `pnpm --filter @multica/desktop bundle-cli` 让 vendor code-canvas/run.sh 进资源包
