---
name: 0.4.0-integration-proposal-2026-07-31
created: 2026-07-31
updated: 2026-07-31T12:55:01Z
 type: project
status: shipped
---

# Multica 0.4.0 Fork 集成 — 收口 proposal

> 给 jyf 审。基于 2026-07-31 现状:
> - fork HEAD `epic/0.3.58-cherry-pick` @ `91811e2`(0.3.67 ship 文档)
> - `epic/upstream-0.4.0` worktree @ `4d20572`(migrations 218-227 + v7 constitution + cloud 中和清单 + wave 2 Go-port 评估)
> - `epic/0.4.0-integration` worktree @ `91811e2`(占位)
> - Downloads `multica-main 2` 已 `git init`,fetch 失败(代理 127.0.0.1:7890 不通),但内容确认 = 07-30 snapshot 副本(240 migrations,同 fork upstream-040)
> - 根工作树 uncommitted:31 M + 12 untracked = 0.3.68/0.3.69 release batch

## 目标

把 `epic/upstream-0.4.0` 推到 **Phase 1 完成态**(迁移 + cloud 中和 + 0.3.68/0.3.69 release batch 落地),作为 0.4.0 fork release 候选;不触 Phase 2(中等冲突功能)和 Phase 3(大重构)。

不重新 pull 上游(网络受限,代理 7890 不通),基于已有 07-30 snapshot 工作。

## 范围 / 非范围

### IN(本次做)
1. 提交 fork 根工作树 0.3.68/0.3.69 release batch(31 M + 12 untracked)
2. 收口 `epic/upstream-0.4.0` 上未完成的 cloud 中和项(stub / 疑似完整残留)
3. 应用层 6 个云端排除文件剥离(Composio / Slack / GitHub multi-workspace)
4. 验证 `36c18b7` 10 个 wave 2 迁移(218-227)仍能 `migrate up` 干净应用到生产 DB
5. v7 constitution 收口(a190b7f / e3c04af retire 后还有什么没清)
6. `epic/0.4.0-integration` worktree 收口(目前 0 commit,直接重置到 upstream-040 完成态)
7. release notes:`.omc/release-notes-0.4.0-fork.md` + ship log `.omc/0.4.0-fork-ship-2026-07-31.md`
8. bump `apps/desktop/package.json` version 到 `0.4.0`

### OUT(本次不做)
- Phase 2(chat 独立化 / settings 11-tab / cron / agent 创建工作室+MCP / 表格 / 全局快捷键)
- Phase 3(daemon execenv / WS RPC / webhook 队列 / desktop 多窗口)
- Go port(决策已记录:schema-first)
- 重新拉上游 / 跨多日上游 cherry-pick(网络受限,代理需用户处理)
- 任何 P0 destructive ship(走 release batch,不打 DMG)

## 实施步骤(分 6 个 commit / 1 个 release)

### Commit 1: `chore(release): 0.3.68 release batch`
- 在 fork 根工作树(`epic/0.3.58-cherry-pick` branch)
- 提交 31 M + 12 untracked 中**首批**:labs runtime 改动(`server/internal/experimental/runtime_gc.go` `registry.go` `handler/experimental_*.go` `service/mythos/supervise.go` `cmd/server/main.go`) + `server/handler/skill.go` `squad.go` + `packages/core/api/client.ts` + 4 个 settings.json locale
- 验证:`pnpm typecheck` + `cd server && go test -count=1 -timeout 120s ./internal/...`
- 风险:可能与 labs runtime GC 的 2026-07-28 audit 改动冲突,先看 diff

### Commit 2: `chore(release): 0.3.69 release batch`
- 同一 branch,atop commit 1
- 提交剩余:`packages/views/issues/components/{issue-detail,issue-labs-section,create-issue}.tsx` + `app-sidebar.tsx` + `labs-tab.tsx` + `scripts/check-agents-docs-sync.mjs` + `scripts/check.sh` + AGENTS.md / CLAUDE.md 同步 + `packages/eslint-config/boundaries.js` 新文件
- `.omc/{0.3.68,0.3.69}-ship-2026-07-30.md` + `.omc/release-notes-0.3.{68,69}.md` 一起 commit
- 新 AGENTS.md(`apps/{desktop,mobile,web}/AGENTS.md` 等 6 个)一起 commit
- 验证:`pnpm typecheck` + `pnpm lint` + `pnpm test` + `node scripts/check-agents-docs-sync.mjs`(新增的同步校验必须过)

### Commit 3: `fix(cloud): neutralize 0.4.0 cloud-boundary stub + suspect-residual items`
- 切到 `epic/upstream-0.4.0` worktree
- 读 `.omc/upstream-0.4.0-cloud-neutralization.md` §"⚠️ stub/noop 占位" 和 §"🚨 疑似完整残留"
- 逐项确认:Composio / Slack / GitHub multi-workspace 应用层是否已 stub
- 不在的 → 加 stub / noop(参考 fork 已有 `analytics.NewFromEnv()` `loginWithGoogle` removed 模式)
- 验证:`grep -rE 'posthog|electron-updater|google|composio|slack' server/ apps/desktop/src/ packages/views/ packages/core/` 应无新残留

### Commit 4: `chore(upstream): wave 2 migration verification (218-227)`
- 在 `epic/upstream-0.4.0` worktree
- 跑 `cd server && go run ./cmd/migrate up` 在**备份** dev DB 上(不是生产)
- 验证 `indisvalid = t` + 列已加(`SELECT attname FROM pg_attribute WHERE ...`)
- 写 `.omc/0.4.0-fork-migration-verify-2026-07-31.md`
- 失败 → 修 `IF NOT EXISTS` / 列类型 / 索引,再验证

### Commit 5: `docs(upstream): finalize 0.4.0 fork release notes + ship log`
- 写 `.omc/release-notes-0.4.0-fork.md`(含:已完成迁移、cloud 中和、v7 constitution retire 情况、Phase 1 vs 2/3 边界)
- 写 `.omc/0.4.0-fork-ship-2026-07-31.md`(1 行 header + ship steps + 风险 vs outcome)
- bump `apps/desktop/package.json` version `0.3.69` → `0.4.0`
- bump root `package.json` version `0.2.0` → `0.4.0`(同步)

### Commit 6: `chore(merge): 0.4.0-integration consolidation`
- 切到 `epic/0.4.0-integration` worktree
- 拉取 `epic/upstream-0.4.0` 全部 commit 作为 base
- 在其上添加 0.3.68/0.3.69 release batch 内容(如果根工作树 commit 1+2 是直接推到 `epic/0.3.58-cherry-pick` 分支,这里要 cherry-pick)
- 推一个 `0.4.0` 标记 commit(空内容,纯标记)

## 风险评估

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 0.3.68/0.3.69 commit 1 改动与 labs runtime GC 2026-07-28 audit 冲突 | 中 | 高(labs 回归) | commit 1 前先 `git diff server/internal/experimental/runtime_gc.go` 自查,跑 `go test ./internal/experimental/...` |
| wave 2 迁移(218-227)在升级 DB 上冲突(已有数据) | 中 | 中(需要更多 `IF NOT EXISTS`) | 先在备份 DB 试跑,不直接动生产 |
| v7 constitution retire 后还有 daemon prepend / catalog 残留 | 中 | 中 | 跑 `grep -r 'charter\|v7\|constitution_agent' server/ apps/desktop/src/` 扫尾 |
| Downloads 同步冲突 | 低 | 低 | 已确认 Downloads = 07-30 snapshot,不动它的 .git |
| 网络拉上游 fetch 失败 | 高(已知) | 低(本期不依赖) | 不拉;已有 07-30 snapshot 够用 |
| 6 个云端排除文件应用层剥离漏一项 | 中 | 中(云端调用可能在 log 报错) | grep 5 类云端关键词 + 跑 typecheck + 试跑 daemon |

## 验证闸门(每个 commit 后必跑)

- `pnpm typecheck`(根目录)
- `cd server && go test -count=1 -timeout 120s ./internal/...`
- `pnpm lint`(根目录)
- `node scripts/check-agents-docs-sync.mjs`(commit 2 后必过)
- `git grep -E 'posthog|electron-updater|google|composio|slack|cloud-billing'` → 应空
- `cd server && go run ./cmd/migrate status` → 干净
- `bash ~/.multica/scripts/pre-update-snapshot.sh`(commit 4 前必跑)

## 不做的事(防止 scope creep)

- 不打 DMG(用户没要 0.4.0 桌面发布)
- 不重写 Phase 2 中任何功能
- 不重新拉上游 multica-ai/multica(网络不通)
- 不改 `runtime_gc.go` 业务逻辑(仅 commit 1 收口)
- 不碰 mobile(本期未做 mobile 集成)

## Ship 实际 commit 列表

按实际集成顺序收口如下：

1. `d7d99cf` — 0.3.68 release batch。
2. `991b71e` — 0.3.69 release batch。
3. `36d09ea` — cloud audit（upstream-040）。
4. `3b5d3b1` — migration verify（upstream-040）。
5. `0e31df2` — 0.4.0 release + version bump（integration-040）。
6. `tag 0.4.0` — integration-040 release tag。

## 下一步(等用户批准)

1. 用户审本 proposal
2. 批准 → 进入实施(按 commit 顺序)
3. 任何 commit 失败 → 立即停 + 报失败输出 + 不继续
4. 全部 commit 成功 → 标 0.4.0 fork release ready,出 ship log
