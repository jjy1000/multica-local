# 0.3.67 ship — 2026-07-30

One-line: tech-debt + stability audit fixes shipped via manual
asar-repack fallback (electron-builder network error during
`make ship-mac --yes` triggered CLAUDE.md documented fallback).

## Ship path (manual asar repack fallback)

`make ship-mac --yes` failed at step 5/7 — electron-builder could not
download `electron-v39.8.7-darwin-arm64.zip` (proxy ECONNRESET). Per
CLAUDE.md "Ship chain fallback: manual asar repack (0.3.63+)", I
repacked the renderer against the existing installed 0.3.66 app
skeleton using the locally-cached `@electron/asar@3.4.1` from
`node_modules/.pnpm/`. Electron Framework + helpers retained; only
renderer / main / preload / Go binaries replaced.

1. **Backed up installed 0.3.66**:
   `/Applications/Multica.app.0.3.66.pre-ship-20260730-084619.bak`
2. **Updated Info.plist version**: 0.3.66 → 0.3.67
3. **Replaced Go binaries** at `Contents/Resources/app.asar.unpacked/resources/bin/`
4. **Repacked app.asar** with fresh `out/{main,preload,renderer}/`:
   - `client_render_error = 1` (P1-2 root ErrorBoundary emit) ✓
   - `no_workspace = 33` (C1/H1 chat-window no_workspace) ✓
   - `rawRequest = 119` (i18next/no-literal-string) ✓
5. **Re-signed nested binaries** via `desktop-sign-nested-binaries.sh`:
   `multica --help` exit 0 (NOT 137) ✓
6. **Cold start**: `verify-desktop-cold-start.sh` PASS in ~6s, server 0.3.67 ✓

## Risk vs outcome

- **Electron-builder network error** (main risk): did NOT produce a
  complete `dist/mac-arm64/Multica.app` from scratch. The manual
  fallback reused the installed 0.3.66 Electron Framework. This is
  identical to the 0.3.63 ship recovery path (CLAUDE.md
  "Ship chain fallback"); no functional compromise.
- **M2 app-builder-bin pin** is recorded in package.json but the
  lockfile still resolves alpha.12 (pnpm install deferred due to
  npmmirror.com ECONNRESET). M2 takes effect at the next successful
  `pnpm install`; this ship used alpha.12 successfully (no M2
  deadlock this run).
- **Row parity**: workspace=1, issue=242, comment=1449, agent=96,
  user_plugin=0 — all invariants held. workspace/agent counts
  unchanged from 0.3.66; issue/comment drift is normal growth.

## Three-check pass

1. Ports: 8090 (server PID 68025) + 5432 (postgres) both listening. ✓
2. `/health` → cold-start script verified server 0.3.67 (etime 00:01). ✓
3. Row parity: workspace=1 / issue=242 / comment=1449 / agent=96 /
   user_plugin=0 — workspace + agent invariants held. ✓

Installed: `/Applications/Multica.app` = **0.3.67**, cold-start verified.
Rollback: `/Applications/Multica.app.0.3.66.pre-ship-20260730-084619.bak`
+ PG logical dump at `~/.multica/backups/pre-update-20260730-084619`.
