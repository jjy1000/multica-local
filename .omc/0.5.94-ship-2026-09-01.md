# 0.5.94 Ship Log — 2026-09-01

**结果:ship 7/7,cold-start PASS,`/Applications/Multica.app` = 0.5.94,health ok。**
提交范围:`692a77663..02509d0e1`(审计修复批 6 commit + bump)。

## 内容

技术债务独立审计修复批(详见 `.omc/release-notes-0.5.94.md`):
MUL-6749 移植(中文名状态 key 派生回退)、MUL-6835 收尾(inbox 优先级 i18n)、
thinking 缓存 flaky 修复、check.sh TS 三步 --force、5 处失效 eslint-disable 清理、
`TestAutopilotTickFlagOffShortCircuits` 退役、账本销账与补记、origin 重建私有镜像。

## 发版曲折(记入教训)

**ship-1(12:42)与 ship-2(13:0x)失败,ship-3(13:1x)被新门拦截,ship-4(13:2x)通过。**

1. **ship-1 冷启动 FAIL(app 静默秒退)**:排查链 `open` 无进程 → 直跑主二进制 rc=1 零输出 →
   无崩溃报告、无 AMFI 拒绝 → 与 .bak(0.5.93)对照启动正常 → 逐文件对比两包:
   main/preload/Electron 版本(39.8.7)完全一致,唯一差异是 asar 内容 →
   **根因:electron-builder 写出的 asar 数据区相对头部整体偏移 ~5 字节,全部 488 个文件损坏**
   (package.json 首部缺 `{\n  "name"`,尾部混入邻文件 `#!/usr/bi`,JSON.parse 失败 →
   Electron main 启动 ~150ms 静默 exit(1))。诱因高度可疑:打包进行时后台还在推 193MiB git pack(重 I/O 竞争)。
   排查中试错过的非原因(留档防复发):codesign 封条破损(ad-hoc 无隔离属性时不阻断启动)、
   ElectronAsarIntegrity 值(该校验未启用,是惰性元数据)、Gatekeeper spctl reject(ad-hoc 包本就 reject)。
2. **新门(5b/7)**:`scripts/ship-mac.sh` 新增 asar 数据区完整性校验——零依赖 Node 内联读取
   asar 头部文件表,取出金丝雀 `out/main/index.js` 与源**逐字节比对**(grep 对位移字节照样命中,
   只有内容比对能抓到);另对 asar 内 package.json 做 JSON 有效性校验
   (注意:electron-builder 会重写 asar 内 package.json 裁掉 devDeps,不能与源做相等比较——
   ship-3 的门首版就是栽在这上面误报,已修正)。
3. **ship-2 失败于步骤 2 migrate**:重复 0.5.92 教训——诊断期间 app 全被杀,bundled Postgres(:5432)随之下线。
   修法:`pg_ctl` 手动拉起 `~/Library/Application Support/Multica/pgdata`(手动实例命令行不匹配
   验证器的 pkill 模式,反而让冷启动时 Postgres 常驻,server 秒绑定)。
4. **.bak 迁移**:ship-1 的回滚快照(坏 asar 的 0.5.94)已按偏好移至
   `~/.multica/backups/2026-09-01-1330/`(命名含 CORRUPT-ASAR 标记);0.5.93 的 .bak 被快照轮换消费,
   0.5.93 完整拷贝仍在 `.omc` 无存,需要回滚 0.5.93 时用 git 重建(代码在 692a77663)。

## 验证

- 门禁(发版前):`pnpm typecheck --force` 6/6;`pnpm test --force` 8/8(views 全绿含 33 停车);
  `pnpm lint --force` 0 错误(desktop 3 + views 8 个有意保留的 exhaustive-deps warning);
  `go test -count=1 -p 1 ./...` 全量 0 FAIL(DATABASE_URL 真实在线)。
- ship 7/7:cold-start PASS,`{"status":"ok"}`,server 0.5.94。
- MUL-6749:8 个 DB-backed handler 测试全绿(含中文状态创建、slug 冲突消歧、
  目录锁停等、并发派生创建零 409、HTTP/事件负载一致性)。
