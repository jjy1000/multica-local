# 0.5.91 Ship Log — 2026-08-31

> **Shipped**: `/Applications/Multica.app` = **0.5.91**（issue 右键菜单单例重构——根治应用级卡死）。Chain: `bash scripts/ship-mac.sh --yes` exit 0（后台运行，全日志 `~/.zcode/cli/exec/sess_*/call_0e8d02a4bc8e45e6a3ca54d7-stdout.log`）。Branch: `epic/0.5.72-followups`，commits `4fc93130b`（code，8 files）+ `8c4720533`（docs）+ `d43b1881d`（bump 0.5.90 → 0.5.91）。

## 打包前状态

- ⚠️ **工作树不干净（有并发在途修改）**：打包时工作树包含另一会话的未提交改动——`packages/core/issues/mutations.ts`（useUpdateIssue 加 `networkMode: "always"`）、`packages/core/realtime/use-realtime-sync.ts`（WS 重连 invalidate 谓词跳过 issue detail keys）、`status-picker.tsx`、`app-sidebar.tsx`、四语言 `experimental.json`、`causal_graph/timesfm` 两个 manifest。打包前以**当前工作树**重跑根 typecheck 6/6 + views vitest 42 失败 = 既有基线原数（编译干净、测试干净）；这些改动**未提交**即被带进 0.5.91 产物，等其会话完成后应尽快落 commit 对齐已发布产物。
- 本周期门禁（见 [`.omc/0.5.91-dev-2026-08-31.md`](0.5.91-dev-2026-08-31.md)）：typecheck 6/6；issue-actions-menu 8/8（含行卸载回归 + 无 Provider 抛错钉子）；swimlane 43/43；views 基线原数；eslint 0 错误。Go 门不适用（零服务端改动）。

## 七步链结果

1. **pre-update snapshot**：`/Applications/Multica.app.0.5.90.pre-update-20260831-104759.bak`（整包回滚点）+ `~/.multica/backups/pre-update-20260831-104759`（pg_dump 32M + config/KB 93 files），旧备份按保留策略剪枝（keep 3）。
2. **migrate**：`go run ./cmd/migrate up` — **277 条全 skip**（本周期零新迁移，符合预期）。
3. **bundle CLI** → 4. **vite build**（index.html 798B 校验 OK）→ 5. **electron-builder**（asar 200× rawRequest 检查通过）→ 6. **install + re-sign**（嵌套 multica/server/migrate 逐个签名，`multica --help` exit 0 非 137）→ 全部 PASS。
6b. **repo backup**：`.omc/backups/2026-08-31-0250/0.5.91-ship/`（活跃超 30 份，`0.5.47-ship` 移入 `_archive/`）。
7. **cold-start verify**：`PASS: cold start verified, server is 0.5.91`（冷启动 ~4s；行平价检查本机 psql 版本不匹配报 `psql-unavailable`，以既有 pg_dump 快照兜底）。

## 安装后核验

- `CFBundleShortVersionString` = **0.5.91**；cold-start verifier PASS。
- **asar 修复检查**：安装包 `app.asar` 内 `node_modules/@multica/views/issues/actions/issue-actions-context-menu.tsx` = 新版 6 717 B（含 `IssueContextMenuProvider` ×3；旧版为 2.8 KB per-row 实现）——修复确认进包。注：asar 二进制上直接 grep 组件名会因 minify 误报 0，提取包内源文件或 grep 错误文案字面量才是可靠判据。
- 回滚点：整包 `.bak`（0.5.90）+ pg_dump；亦可用 git `d43b1881d^` 走 ship 链重建。

## 遗留

- 并发会话在途改动的 commit 对齐（见上"打包前状态"）。
- 0.5.9x 账本照旧：上游 MUL-6749 / MUL-6835 / #7209 移植评估、MUL-6632 inbox 族（决策门未开）、路由切换时菜单打开的残余边界（上游同样接受）、origin remote 404 修复。
