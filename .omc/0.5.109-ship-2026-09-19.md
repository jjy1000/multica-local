# 0.5.109 ship record — 2026-09-19

**发版内容 = 上游价值移植批**(分诊+执行账本:
[upstream-sync-2026-09-19.md](upstream-sync-2026-09-19.md),22 提交 12 port /
10 skip;用户可见说明:[release-notes-0.5.109.md](release-notes-0.5.109.md))。

## 提交链(epic/0.5.72-followups,基于 0.5.108 的 `af28ccec1`)

| 提交 | 内容 |
| --- | --- |
| `39ea27761` | test(cli): port 7112606ae — 捕获 stdout 排空(macOS 管道死锁) |
| `0674e4e0d` | fix(taskfailure): port afedc6f76 cursor 半 — 网络错误分类 |
| `586094382` | fix(agent): port MUL-5241 — hermes 关闭有界化(修 fork 现存楔死) |
| `042ef8609` | fix(handler): port MUL-7449+7485 — stage 进度区分 cancelled |
| `f75c133ee` | fix(editor): port MUL-7456 — mention picker 任意 token 边界(CJK) |
| `68002ba51` | fix(agent): port MUL-7465 — Codex delta 端到端流式(Go+FE) |
| `153c8d931` | fix(autopilots): port MUL-7478 half-1 — 排程编辑锁收窄 |
| `cb111ecf0` | fix(comments): port MUL-7447 — @all 仅成员广播说明 |
| `631fac94d` | fix(agent): port MUL-7467 — Pi 静默错误显式化(pi.go 半) |
| `6a0a4798e` | fix(daemon): port MUL-7471 — 终态回调持久化重放(outbox) |
| `3be60bdd8` | style(taskfailure): gofmt 对齐(审查 P2) |

## 代码门禁(逐项实测)

- `go test -count=1 -timeout 600s ./internal/... ./pkg/agent/...`
  (DATABASE_URL 已导出):40 包 ok / 0 FAIL,DB-set 证据在案。
- `pnpm typecheck` 6/6;`pnpm lint` 8/8;`pnpm test` 8/8
  (views 1883 passed / 33 parked skip = MUL-6632 家族)。
- 代码审查(独立只读 agent):P0/P1 零 finding,verdict SHIP;唯一可执行项
  (classify.go gofmt)已修。

## ship 链结果(装后回填)

- (待回填)
