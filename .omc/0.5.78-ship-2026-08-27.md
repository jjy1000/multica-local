---
name: 0.5.78-ship-2026-08-27
created: 2026-08-27T11:20:00+08:00
---

# 0.5.78 Ship Log — 2026-08-27

`bash scripts/ship-mac.sh --yes` → **7/7 PASS**, `/Applications/Multica.app` = 0.5.78.

- Pre-update snapshot taken by chain (rollback point against running 0.5.77)
- `migrate up` clean, no pending migrations
- electron-builder --dir + asar verification OK
- Nested binaries re-signed; Info.plist verified 0.5.78 == package.json
- Cold start ~6s; server PID up (`psql-unavailable` row-parity note = verify subshell lacks DATABASE_URL env; backend itself connected via profile .env and served API during boot)
- Backup retention sweep archived `.omc/backups/2026-08-17-1339/`

## Gate evidence

| Gate | Result |
|---|---|
| `pnpm typecheck` (turbo full) | 6/6 |
| `go test ./internal/handler/` | ok (14.1s incl. dashboard trio + plugin suites) |
| `go test ./internal/experimental/ ./internal/cli/ ./pkg/agent/` | all ok |
| Dashboard trio re-runs | rollups ×2 ok · endpoints ok · quick-create `-race` ×3 ok |
| Desktop `scripts/package.test.mjs` | 22/22 |
| `pythia-smoke.sh` | 9/9 |
| vendor↔resources diffs (pythia engine / semantica builds) | clean |
| `node scripts/check-agents-docs-sync.mjs` | ✓ |

Known non-blocking: `issue-detail.test.tsx > highlightCommentId` ×2 fail at baseline too (stash-verified pre-existing; queued for 0.5.79).
