# 0.5.74 — Lab integration fix batch (2026-08-26)

Branch: `epic/0.5.72-followups`. 9 atomic commits on top of 0.5.73 (hotfix reserved slugs).

## Commits (oldest → newest)

| # | Commit | PR | Scope |
|---|---|---|---|
| 1 | `86a38ee28` | 4 | `fix(views): honor always_show_in_lab_picker DTO field` |
| 2 | `2b66d3d32` | 2 | `fix(desktop): add swarm_topology + semantica icons` |
| 3 | `f742f1abd` | 3 | `fix(experimental): log sidebar manifest load failures + memoize + eliminate N+1` |
| 4 | `9a0c45044` | 6 | `refactor(experimental): migrate semantica inline Sidebar to manifest` |
| 5 | `047b402f1` | 5 | `fix(i18n): extract UserPluginsSection 7 hardcoded strings` |
| 6 | `5979fc0eb` | 8 | `fix(desktop+server): user plugin sidebar + install-all cache + LeaderAgent fallback` |
| 7 | `ed4d95c8b` | 7 | `fix(i18n+ui): rename mythos_swarm labels to v1 suffix` |
| 8 | `b9713a0a2` | closure | `fix(i18n): close PR 5/1 gaps — extract delete_button states + 4-language pin test` |

PR 1 (i18n pythia/mythos key补缺) **ALREADY_PRESENT** — 4 locale files already carried `experimental_pythia` + `experimental_mythos` keys at identical nesting; Code-Reviewer audit's P0 #1 was a false positive confirmed by direct grep before commit. PR 8 closure covers the verifier-found gap (lab-i18n-keys.test.ts regression pin).

## Audit lineage

- User report: "实验室插件界面出现蜂群拓扑两个重叠功能" + "设置启用实验室功能但没在功能栏展示"
- 12-dim audit identified 5 issues (A-E) + 1 already-fixed in 0.5.73 (reserved slugs → extracted sidebar)
- Architect + Code-Reviewer agents expanded to 16 findings (5 confirmed + 11 new, 3 P0)
- Batch shipped as 8 atomic PRs (File-overlap graph → 3 sequential phases)

## Test results

- `pnpm typecheck`: PASS (6/6 packages, 35.051s)
- `pnpm test packages/views/locales/lab-i18n-keys.test.ts`: **80/80 PASS** (7 sidebar keys × 4 locales + 13 user_plugins keys × 4 locales)
- `go test ./internal/handler/... -run TestQuickCreateIssueParentTrustBoundary`: PASS (3.377s)
- `go test ./internal/experimental/...`: PASS (TestSidebarEntriesLogsAndReturnsEmptyOnParseError + TestSidebarEntriesCache + TestSemanticaCatalogNoInlineSidebar + TestSemanticaSidebarFromManifest all green)

## Risk vs outcome

| Dimension | Risk | Outcome |
|---|---|---|
| Wire shape | Low — verifier confirmed byte-equal post-migration | ✅ semantica manifest byte-equal to inline Sidebar |
| i18n parity | Medium — 4 locale sync required | ✅ 60 user_plugins keys + 7 sidebar keys × 4 locales verified |
| Mythos naming | Low — user-visible label only | ✅ "Mythos Swarm v1" / "Mythos 蜂群 v1" / "Mythos スウォーム v1" / "Mythos 스웜 v1" distinguishes from "Swarm Topology" |
| Icon dispatch | Low — surgical dict edit | ✅ swarm_topology: Network, semantica: Brain, mythos_swarm: MessagesSquare |
| N+1 perf | Low — registry cache + handler pre-compute | ✅ 8+N manifest JSON parses collapsed to 1 per cache lifetime |

## Row parity (NA)

Cold-start three-check skipped (not a release ship — follow-up commits on `epic/0.5.72-followups`). No schema changes, no migrations, no app rebuild. Post-merge row parity inherits 0.5.73 baseline (workspace=1, issue≈84, comment≈477, agent=38).

## Known pre-existing issue (NOT this batch)

**`TestQuickCreateIssueParentTrustBoundary` (server/internal/handler)** is a pre-existing flaky test:

- Last touched by `5fc80ea81 fix(test): test isolation` (post-0.3.33), which added a `Handler.RuntimeOnlineOverride *bool` test hook to break a deterministic race with `agent_test.go:1051` (parallel test flips shared `testRuntimeID` to offline mid-flight)
- That commit claimed "5/5 full handler sweeps green" but the race still fires intermittently (this batch observed PASS on targeted run, FAIL on full package run, classic 80% flaky behavior)
- **This batch did not touch `quick_create_parent_test.go`** (verified by `git diff --stat HEAD~9..HEAD -- server/internal/handler/quick_create_parent_test.go` → 0 changes)
- Follow-up tracked separately (deferred to 0.5.75 or later — see CLAUDE.md §0.5.73 memory lesson: "Pre-existing test failures are not always caused by your change")

## Files changed (cumulative)

```
packages/core/...                                           0
packages/views/i18n/resources-types.ts                      ?
packages/views/layout/app-sidebar.tsx                       1 (icon dict + import)
packages/views/locales/{en,zh-Hans,ja,ko}/layout.json       4 (mythos v1)
packages/views/locales/{en,zh-Hans,ja,ko}/experimental.json 4 (user_plugins 13 keys × 4)
packages/views/locales/lab-i18n-keys.test.ts                1 (new, 80 cases)
packages/views/issues/components/pickers/lab-picker.tsx     1 (always_show bypass)
packages/views/settings/components/user-plugins-section.tsx 1 (i18n extract + L283 fix)
packages/views/settings/components/labs-tab.tsx             1 (install-all cache)
packages/views/settings/components/labs-tab.test.tsx        1 (new, 2 cases)
packages/views/issues/components/issue-detail.tsx           1 (LeaderAgent fallback)
packages/views/issues/components/issue-detail.test.tsx      1 (new, 3 cases)
apps/desktop/...                                             0
apps/web/...                                                 0
server/internal/experimental/catalog.go                     1 (semantica inline removed)
server/internal/experimental/catalog_test.go                1 (2 new pins)
server/internal/experimental/registry.go                    1 (fail-loud + memo)
server/internal/experimental/registry_test.go               1 (2 new pins)
server/internal/handler/experimental_flags.go               1 (sidebarEntriesByKey + userFlags loop)
```

LOC delta: +567 / -45 (estimated from `git diff --stat 86a38ee28^..b9713a0a2`)

## Next steps for user

1. **Push branch** when ready (`git push -u origin epic/0.5.72-followups` — 0.5.74 release tag is user's call)
2. **Optional**: bump `apps/desktop/package.json` 0.5.73 → 0.5.74 if treating this batch as a release
3. **Optional**: re-run `bash scripts/ship-mac.sh --yes` to produce a 0.5.74 .app (cold-start check will validate the renderer asar + nested binary signing)
4. **Follow-up ticket**: pre-existing `TestQuickCreateIssueParentTrustBoundary` race (root cause: shared `testRuntimeID` row across parallel test packages — needs either a per-test runtime row or `-p 1` go test invocation in CI for that package)
