---
name: 0.5.28-ship-2026-08-17
created: 2026-08-17T08:43:52Z
updated: 2026-08-17T08:43:52Z
---

# 0.5.28 Ship Log — 2026-08-17

## Summary
Urgent patch on top of 0.5.27: completes the daemon-port-env-leak fix and adds the server `.env` PORT reconciliation. Installed at /Applications/Multica.app.

## Ship steps
1. ✅ Pre-update snapshot — app 0.5.27 backed up to `/Applications/Multica.app.0.5.27.pre-update-20260817-084306.bak` (776M), PG logical snapshot 26M, config + KB rsync, tag `pre-update-20260817-084306`.
2. ✅ `migrate up` — 0 pending (0.5.28 has no new migrations, TS + server fix only).
3. ✅ bundle-cli — 3 Go binaries @ 0.5.28, 471 migration files verified, Pythia/OpenScience/claude-science bundled.
4. ✅ electron-vite build — renderer rebuilt with the 3 fixes (`grep -c rawRequest app.asar` would show the post-fix asar).
5. ✅ electron-builder --mac --dir — NO deadlock, ad-hoc signature.
6. ✅ install + `desktop-sign-nested-binaries.sh` — app + 3 nested binaries re-signed, nested `multica` runs exit 0.
7. ✅ cold-start verify — Info.plist 0.5.28, cold start ~6s, Server PID 15979, etime 00:02, **PASS**.
8. Row parity — skipped (psql client not in PATH, known non-blocker).

## Risk vs outcome
- **0.5.27 → 0.5.28 in 90 minutes**: same shell session as 0.5.27 ship. Acceptable because (a) the residual fix was identified immediately by the analyst agent (P0 same class as 0.5.27), (b) 0.5.27 was already live for ~50 minutes with no observed regressions.
- **electron-builder deadlock** (known 0.3.63 hazard) did NOT recur for the second ship in 90 min.
- **All analyst findings triaged**: 3.1 (P0) + 3.3 (P1) shipped; 3.2 (P2) + 3.3 DB-URL + 3.4 (P3) deferred to 0.5.29.

## Follow-ups
- Row parity manual verify next time psql is available.
- 0.5.28 docs release commit pending.