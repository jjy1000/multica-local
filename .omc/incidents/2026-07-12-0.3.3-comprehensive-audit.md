# Multica 0.3.3 Comprehensive Audit Report (2026-07-12)

> Auditors: 4 parallel agents (schema / handler / deploy / scope) + 1 main session verification.
> Scope: Verify 0.3.3 ship 完成度, 发现 bug, 检查历史 memory 遵循.
> 总体评级: **条件性 ship (功能 schema prep, 非功能完整)**.

---

## 1. 总体评级矩阵

| 维度 | 评级 | 理由 |
|---|---|---|
| Schema 集成 | **A-** | 10 个 forward-only migration 全部 apply, schema_migrations 完整 (181/181), 但 8/10 新列无 handler 引用 |
| Handler 集成 | **C** | mcp_overlay.go 4 函数全孤立 (0 caller), 无 unit test |
| 本地化策略 | **A** | 未违反 CLAUDE.md 任何规则 |
| 部署完整性 | **B-** | version=0.3.3, schema 落地, 但 last-packaged.json 漂移到 0.3.0, dmg 未生成 |
| 数据安全 | **A** | forward-only + pre-update snapshot + row parity 保留 |
| 可回滚性 | **A** | 10 个 migration 有 down, handler 只 +1 个文件, 备份锚点都在 |
| 历史经验遵循 | **B-** | 0.3.2 已 apply 的 4 个 migration schema_migrations 缺失记录 (audit 中已修复), dmg-builder 下载卡死 |

**综合**: 78/100 — 技术上 ship OK, 但用户感知上 0.3.3 vs 0.3.2 完全无变化 (schema 列是 dead column, mcp_overlay 是 dead code).

---

## 2. 关键发现 (P0→P3)

### P0 (紧急)
**无**. 0.3.3 app 是好的, PG 数据完好, 三-check pass.

### P1 (必须修)

#### P1-1: `mcp_overlay.go` 无 unit test (handler audit 发现)
- 本地有 `mcp_overlay.go`, **无** `mcp_overlay_test.go`
- 官方版有 9 个测试覆盖: 双方为空 / agent-only / overlay-only / 双侧合并 / overlay collision wins / 保留 agent 顶层字段 / malformed overlay fallback / malformed agent fallback / 非 object server 拒绝
- **修复**: 移植官方 9 测试到 `mcp_overlay_test.go`

#### P1-2: `mergeMCPOverlay` 0 caller — dead code
- 唯一引用在 `mcp_overlay.go` 内部
- `daemon.go:1303-1321` 仍直接 `agent.McpConfig` 入 claim, 无 merge 调用
- 文件头注释声称"handler layer calls"但代码无 caller — 文档与代码不符
- **修复二选一**:
  1. 移植官方 daemon claim caller (读 `task.RuntimeMcpOverlay` + merge)
  2. 删除 mcp_overlay.go (本地策略仍排除 Composio, 无 overlay writer)

#### P1-3: `~/.multica/last-packaged.json` 漂移到 0.3.0 (deploy audit)
- 当前内容: 2026-07-02 写入的 0.3.0, 完全没记录 0.3.1/0.3.2/0.3.3
- 违反 `.omc/version-anchor.md` "SOURCE_VER == INSTALLED_VER == last-packaged.version"
- **修复**:
  ```bash
  cat > ~/.multica/last-packaged.json <<EOF
  {
    "version": "0.3.3",
    "packaged_at": "2026-07-12T20:32:30Z",
    "previous_version": "0.3.2",
    "smoke": { "health": "ok", "boot_under_8s": true }
  }
  EOF
  ```

#### P1-4: dmg-builder 下载卡死未解决 (deploy audit)
- `electron-builder package` 在 `downloading release=dmg-builder@1.2.0 file=dmgbuild-bundle-arm64-75c8a6c.tar.gz` 阶段 hang 1h+
- dmg-builder 在 node_modules 装了 (`dmg-builder@26.8.1_electron-builder-squirrel-windows@26.8.1`), 但 dmgbuild-bundle tarball 下载卡
- 旧的 dmgbuild tarball 在 `.Trash/electron-builder/dmg-builder@1.2.0/dmgbuild-bundle-arm64-75c8a6c-uwh7j` — **已在垃圾箱**
- **0.3.4 必做**: pre-package 脚本先 `curl` dmgbuild tarball 到 electron-builder cache, 或写 fallback 走 ZIP

#### P1-5: 8090 server 仍是 0.3.2 旧进程 (deploy audit)
- PID 52246 etime = **4天6小时** = 启动时间 2026-07-08 ≈ 0.3.2 ship 时间
- 当前 server 进程读 `app.asar.unpacked/resources/bin/server` inode — inode 已被 cp -R 替换为 0.3.3 binary, 但**进程本身仍是 0.3.2 启动的 fork**
- 0.3.3 binary **未被实际加载**
- 用户必须 `pkill -f Multica.app && open /Applications/Multica.app` 才能切换到 0.3.3 server
- **当前 symptom**: /health ok 是因为 0.3.2 server 还在跑, 不是 0.3.3 在跑

### P2 (应该修)

#### P2-1: overlay-only 路径绕过 server-entry 校验 (handler audit)
- `mcp_overlay.go:59-71` 在 agent config 为空时仅顶层 unmarshal/marshal, 不调 `unmarshalServerMap`
- 输入 `{"mcpServers":{"bad":"not-an-object"}}` 可能成功而非报错
- **修复**: overlay-only 分支也调 `unmarshalServerMap(oCfg["mcpServers"])`

#### P2-2: 3 个 chat_session 新列是 dead column (handler audit)
- `last_read_at` / `is_agent_intro` / `pinned_at` 在所有 Go handler 中 0 引用
- 0 个 chat_unread.go / chat_session_pin.go / daemon intro claim 分支
- 用户**感知上 0.3.3 vs 0.3.2 完全相同** — IM unread 红点没变数字, agent intro 不显示, session pin UI 不存在
- **修复二选一**:
  1. 0.3.4 PR-2 实装 3 个 handler
  2. release-notes 显式标注 "schema prep, 功能未接线"

#### P2-3: 8/10 新列死列 (schema audit)
- 全表 0 引用: 135 last_read_at, 141 delivered_comment_ids, 142 chat_input_task_id, 143 message_kind, 138 is_agent_intro, 139 pinned_at
- 144 idx_chat_message_input_owner 在 `chat_message.task_id IS NULL` 的 user message 行上 fallback seq-scan — plan 未声明

#### P2-4: plan §拒绝项与现实不符 (schema audit)
- plan §拒绝项写 "128/129-133 不 apply"
- 实际: 3 个 128 文件 + 120 autopilot_subscriber + 120 github_pending_installation + 122 lark + 124 channel_generalization **已 apply**
- 解释: Lark/Slack/GitHub 代码存在但运行时被 env var gate (`MULTICA_LARK_SECRET_KEY not set` → disabled)
- **修复**: 更新 plan 拒绝项措辞, 区分"代码删除" vs "schema 列存在但 runtime disabled"

#### P2-5: owner/group 是 jiangjianyan:admin 不是 :staff (deploy audit)
- `cp -R` 保留源 dir owner, 未踩 chown 沙箱坑
- 但 CLAUDE.md "Reinstalling the DMG" 段落写的 `chown -R $USER:admin` 已过时
- **修复**: 更新 CLAUDE.md 文档

#### P2-6: 没有 last-packaged-${prev_ver}.json 历史归档
- `last-packaged.json` 单一文件原地覆盖, 0.3.1/0.3.2 smoke data 永久丢失
- **修复**: bundle-cli 脚本 `mv` 而非覆盖

### P3 (可选)

#### P3-1: down.sql 141/142/143 down 丢运行时数据 (schema audit)
- down 仅 `DROP COLUMN`, 141 期间活跃任务的 delivered_comment_ids 永久丢失
- 142 期间写入的 chat_input_task_id 永久丢失
- 143 用户可见的 no_response 终端态被擦除

#### P3-2: 136 末尾 DROP CONSTRAINT 是 dead code (schema audit)
- 036 up 里建表根本没 FK (no-FK 规则), DROP 是无意义的反向防御

#### P3-3: mcp_overlay.go 文件头注释与代码不符 (handler audit)
- 注释: "handler layer calls mergeMCPOverlay" — 但代码无 caller

#### P3-4: `~/.multica/.data-snapshot.json` 还是 6/26 (deploy audit)
- 与 0.3.3 无关, 列表备忘

---

## 3. 历史 memory 遵循检查

| Memory | 0.3.3 状态 |
|---|---|
| `multica-fork-vs-upstream-divergence-map.md` | ✅ 未违反 — Slack/Lark/GitHub/Composio runtime 全部 disabled |
| `multica-0.3.0-standalone-2026-07-02.md` (destructive-guard) | ✅ 135-144 全部 forward-only, 不触发 029/046/103 历史破坏性 |
| `multica-ipc-registration-order.md` | ✅ 未新增 IPC channel |
| `0.2.97-daemon-autostart-regression.md` | ✅ App.tsx useEffect [user] 未触动 |
| `multica-dmg-replace-chown-pitfall.md` | ✅ 未踩坑 (cp -R 保留 owner) |
| `0.2.89.5.1-path-d-modulepreload-fix.md` | ✅ Path D modulePreload 仍在生效, 0.3.3 启动没复现 NSAlert |
| `multica-0.3.2-stability-fix-2026-07-06.md` (P0 task:completed) | ✅ release-notes 明确标注 mcp_overlay 不是该 P0 的修复, 是 schema-only |
| `multica-version-backward-compat.md` | ✅ 10/10 up 都用 IF NOT EXISTS / IF EXISTS 幂等 |

**违反 1 处**: `.omc/version-anchor.md` 强制同步检查 — last-packaged.json 没更新 (P1-3)

---

## 4. 是否解决了历史经验错误

| 历史错误 | 0.3.3 修复? |
|---|---|
| 0.3.0 destructive-migration (029/046/103) | ✅ 135-144 全 forward-only |
| 0.3.0 JWT recovery (签名密钥轮换) | ✅ 未触动 JWT 路径 |
| 0.3.1 sentinel 原子性 | ✅ pre-update-snapshot.sh 已含 `.pg-migrating-v1` 检查 |
| 0.2.89.5.1 asar unpack 失败 | ✅ asarUnpack 配置未改, 0.3.3 .app 验证 mcp_overlay 在 asar.unpacked |
| 0.2.90 Electron 39 NSAlert | ✅ Renderer PID 53531 正常启动, NSAlert 未复现 |
| 0.2.97 daemon auto-start 回归 | ✅ App.tsx 未触动 |
| 0.3.2 P0 task:completed issue 缓存 | ❌ 没解决 — chat_input_task_id 已落库但无 handler 写入 (P2-3) |
| **新错误**: schema_migrations 4 行 0.3.2 缺失 | ❌ 0.3.2 ship 时漏记录, audit 中已 backfill |
| **新错误**: dmg-builder 下载卡死 | ❌ 没解决, 推到 0.3.4 (P1-4) |
| **新错误**: last-packaged.json 漂移 | ❌ 没解决, 推到 0.3.4 (P1-3) |
| **新错误**: 8090 server 是 0.3.2 旧进程 | ❌ 没解决, 需要用户手动重启 |

---

## 5. 0.3.4 必须做的清单

### P0 (无)

### P1 (必须)
1. **修 mcp_overlay.go**:
   - 选项 A: 删除 dead code 文件 (本地无 Composio, 无 overlay writer)
   - 选项 B: 移植官方 daemon claim caller, 补 9 个 unit test
2. **修 last-packaged.json**: 写 0.3.3 实际值 + 加 prev_ver 历史归档机制
3. **修 dmg-builder 下载**: pre-package 脚本预下载 tarball 到 electron-builder cache
4. **修 verification 流程**: 必须 `pkill + cold restart` 后才读 server PID etime
5. **PR-2 (MUL-4351)**: chat_input_task_id claim handler + message_kind 渲染 (schema 落库后必须配套)

### P2 (应该)
6. 补 3 个 chat_session 新列的 handler (chat_unread.go / chat_session_pin.go / daemon intro claim)
7. 修 plan §拒绝项措辞, 区分"代码删除" vs "runtime disabled"
8. 修 CLAUDE.md "Reinstalling the DMG" 段落 — 删过时 chown 命令
9. 修 mcp_overlay.go overlay-only 路径 server-entry 校验
10. bundle-cli.mjs 加 last-packaged.json 写钩子 (无论 cp -R 还是 DMG)

### P3 (可选)
11. schema 141/142/143 down.sql 加注释警告 down 丢运行时数据
12. mcp_overlay.go 文件头注释同步代码真实状态

---

## 6. 关键证据文件路径

- `/Applications/Multica.app` — version 0.3.3, 内嵌 0.3.3 binary
- `/Applications/Multica.app.0.3.2.pre-update-20260711-103903.bak` — 回滚锚点
- `/Users/jiangjianyan/.multica/backups/pre-update-20260711-103903/` — PG 备份
- `/Users/jiangjianyan/.multica/last-packaged.json` — **stale 0.3.0** (P1-3)
- `/Users/jiangjianyan/jjy/multica-main/.omc/release-notes-0.3.3.md` — ship 记录
- `/Users/jiangjianyan/jjy/multica-main/.omc/plan-0.3.3-upstream-integration.md` — plan
- `/Users/jiangjianyan/jjy/multica-main/server/migrations/135_*.sql` ~ `144_*.sql` — 10 个新 migration
- `/Users/jiangjianyan/jjy/multica-main/server/internal/handler/mcp_overlay.go` — 唯一新增 handler (4 函数, 0 caller, 0 test)
- `/Users/jiangjianyan/jjy/multica-main/apps/desktop/dist/multica-desktop-0.3.3-mac-arm64.zip` — 220MB ZIP
- `/Users/jiangjianyan/jjy/multica-main/apps/desktop/dist/mac-arm64/Multica.app` — 719MB 已部署 .app

---

## 7. 一句话总结

**0.3.3 ship 了 "schema-only foundation + dead mcp_overlay tool"**。功能上用户感知 = 0.3.2, 数据安全 = A, 部署完整性 = B- (last-packaged 漂移 + dmg 卡死 + 旧 server 进程未重启)。3 个 P1 修复 + 5 个 P2 修复必须在 0.3.4 解决, 否则继续累积。

**Audit 完成时间**: 2026-07-12 20:42
**审计方式**: 4 个并行 agent + 1 个 main session 独立验证 + 1 个 incident P0 backfill (4 个 0.3.2 缺失 migration 记录)