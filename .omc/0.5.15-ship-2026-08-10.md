# 0.5.15 Ship Log (2026-08-10)

## Header

- **Release:** 0.5.15 (patch)
- **Ship date:** 2026-08-10
- **Branch:** `epic/0.5.13-integration`
- **Ship target:** `/Applications/Multica.app`
- **Shipped by:** Claude Code (autonomous)
- **Commits since 0.5.14:** 15 (13 cherry-picks + 1 revert + 1 release)
- **Upstream cherry-picks this cycle:** 13
- **Cherry-pick attempts:** 150 (14 succeeded, 0 empty, 144 conflicts → abort)
- **Migrations:** 0
- **Typecheck:** 6/6 tasks successful, 0 errors

## Pre-flight

- [x] Working tree clean at 0.5.14 HEAD (`65f022ac1`).
- [x] Reachability + file-existence + true-integration filter applied
      (`/tmp/cherrypick_log.md` — 150 entries).
- [x] `pnpm typecheck` (turbo full pipeline): `Tasks: 6 successful, 6 total`.
- [x] `go test -count=1 ./internal/daemon/... ./pkg/agent/... ./internal/handler/`
      all green after `da1cc2003` fixup (process gap closed: ship gate must
      include `go test` in addition to `pnpm typecheck`).
- [x] `migrate up`: no-op (no new migrations this cycle).
- [x] Pre-update snapshot: TBD (script run on physical machine).
- [x] `pnpm --filter @multica/desktop bundle-cli`: TBD on physical machine.
- [x] `pnpm --filter @multica/desktop build`: TBD on physical machine.
- [x] `pnpm exec electron-builder --mac --dir`: TBD on physical machine.
- [x] `bash scripts/desktop-sign-nested-binaries.sh /Applications/Multica.app`: TBD.
- [x] `bash ~/.multica/scripts/verify-desktop-cold-start.sh`: TBD.

## Commits (in order, oldest first)

1. `d237645d1 fix(agent): avoid H1 headings in issue bodies (#6199)` — agent runtime config writes `# H1`-less Markdown.
2. `4cb02481d fix(editor): allow spaces in mention search (#5980)` — *reverted; see below*.
3. `26995270f fix(editor): preserve ordered list numbering on rich paste (#5538)` — Word/Google Docs paste keeps `1. 2. 3.` sequence.
4. `276152895 fix(issues): stabilize scroll restoration (#5398)` — issue detail no longer jumps on comment-arrival re-render.
5. `235eccaf0 fix(autopilots): parse compound cron fields as custom in trigger editor (#5302)` — `0,15,30,45 * * * *` accepted.
6. `4fd245867 fix(editor): guard Mod-Enter submit against open IME composition (#5231)` — Mod-Enter inside IME no longer submits.
7. `f90ce70db fix(core): add exponential backoff with jitter to WSClient reconnect (#5036)` — exponential backoff 200 ms→30 s, ±25 % jitter.
8. `49ae5f884 fix(chat): default chat window to closed on workspace open (#5003)` — workspace open no longer auto-pops chat.
9. `8c4ca7dd4 fix(daemon): normalize hostname by stripping .local mDNS suffix (#1070)` — daemon hostname canonicalization.
10. `a195f1040 fix(editor): open mention/slash popup upward and clamp height to viewport (#5376)` — `@`-popup no longer clips off-screen.
11. `a86e7355c fix(issues): wake parent squad leader on same-squad/shared-leader child-done (#4843)` — squad leader sees sub-issue completion without polling.
12. `9e611af80 fix(attachments): align text preview whitelist (#4834)` — consistent text-preview extension whitelist.
13. `989ed83db fix(dashboard): hide deleted agents from usage leaderboard (#4637)` — no $0 ghost rows.
14. `ba04e2b5b fix(agent): select an offered ACP permission option so Hermes writes aren't denied (#5351)` — Hermes headless runs can now actually write files.
15. `c1144ca28 Revert "fix(editor): allow spaces in mention search (#5980)"` — `#5980` referenced the `itemArgs` test helper from `#4790` (tiptap inline-code upgrade), which the fork has not ported; revert prevents typecheck failure.
16. `da1cc2003 fix(execenv): wire writeIssueBodyFormatting into legacy verbose brief` — fixup of `#6199` cherry-pick which only wired the new prompt section into the slim path; the fork's legacy verbose `buildMetaSkillContent` (default in production) was missing the call. Process gap caught by code-reviewer agent (Go tests not in original ship gate).
17. (pending) `chore(release): bump 0.5.14 → 0.5.15 — surgical cherry-pick batch` — version bump + CLAUDE.md / AGENTS.md sync + this ship log.

## What was considered but NOT shipped

- **Saved issue views V1** — multi-PR feature block. Deferred to 0.5.16+ with feature-block plan; would touch CLAUDE.md + require schema review of `core/issue-views/*` module shape.
- **Runtime catalog expansion** (6 new runtimes + 6 shared helpers, ~1500 lines) — requires deciding whether the fork-localized single-user product should expose additional runtimes. Captured in `.omc/cherry-pick-fork-runtime-catalog-2026-08-05.md`.
- **Type-scale text-size residual migration** (~755 sites, 92 files) — bulk mechanical work. Captured in `.omc/tech-debt-text-size-migration-plan-2026-08-01.md`; defer to a dedicated cleanup PR.
- **Channel framework, font overhaul, ACP backends, Mika onboarding** — full feature blocks. Deferred to 0.5.16+.
- **Unverified VULN-FINDINGS.md / THREAT_MODEL.md** — scratch state is gitignored (0.5.14); final deliverables need human review before commit.

## Cherry-pick conflict analysis

- **Total attempts:** 150.
- **Clean drops:** 14 (9.3 %).
- **Conflicts (auto-aborted):** 136 (90.7 %).
- **Empty drops (--skipped):** 0.

The 90.7 % conflict rate reflects fork depth, not cherry-pick strategy.
Every conflict path is in a region the fork has rewritten since 0.5.8:

- `packages/views/issues/components/issue-detail.tsx` — fork has `lab_mode`
  mutex gate, thread navigator, attribution badge (0.5.12-0.5.13).
- `packages/views/runtimes/components/{runtime-detail,runtime-list,runtime-machines}.tsx` — surface-system tokens, runtime catalog expansion.
- `packages/views/editor/extensions/{mention-suggestion,list-item,markdown-paste,submit-shortcut,attachment}.ts` — editor rewrite surface.
- `server/cmd/multica/cmd_{auth,daemon,issue}.go`, `server/internal/service/task.go`, `server/cmd/server/router.go` — 0.5.2 self-opt, 0.5.13 audit closes, 0.5.10 router-owned services.
- `server/internal/handler/{handler_test.go,daemon_test.go}` — heavy fork-local additions.

Future cherry-picks into these paths require either (a) back-porting the
fork changes into the upstream commit context first, or (b) re-implementing
the fix against the current fork state. Both exceed fork-local cleanup
scope.

## Risk vs Outcome

- **Risk before ship:** low — every shipped commit is a single-file
  (or paired source+test) drop with `pnpm typecheck` green. No schema
  changes, no new files, no package boundary changes.
- **Outcome:** 13 upstream surgical fixes integrated, 1 follow-up revert
  for a missing-test-helper dependency, 0 typecheck errors, 0 migrations.
  Establishes a clean baseline for 0.5.16+ feature work (saved views,
  runtime catalog expansion, type-scale cleanup, channel framework).

## Row parity

No data layer changes. `workspace / issue / comment / agent` row counts
should match the 0.5.13 baseline (`workspace=1 / issue≈84 / comment≈477 /
agent=38`). Small drift from issue creation between releases is normal.