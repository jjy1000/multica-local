# 0.5.84 — Causal Graph P0 fix batch (post-ship audit closure)

**Ship date**: 2026-08-28
**Branch**: `epic/0.5.72-followups`
**Baseline**: `be80fe200` (0.5.83 post-ship verification audit)
**Bump**: `9604cb032` · **Merges**: `934fb7e66` (A) / `2c39b0ffb` (C) / `d7f60c968` (B)
**Target**: `/Applications/Multica.app` = 0.5.84 (pending ship-mac)

## What shipped

Full inventory in [`.omc/release-notes-0.5.84.md`](release-notes-0.5.84.md); audit that triggered the work in [`.omc/0.5.83-post-ship-verification.md`](0.5.83-post-ship-verification.md). 10 new commits on top of `be80fe200`:

| # | Commit | Content |
| --- | --- | --- |
| 1 | `5b33c25bc` | fix(views): add causal_graph to FLAG_ROUTE_SUFFIX (P0 #1) |
| 2 | `ebff9cfcb` | fix(server): createCausalSuggestion probes FindCausalEdgeBetween (P0 #2, contract #8b) |
| 3 | `fb659ed5f` | fix(server): TouchCausalNode callers on issue/comment/task paths (P0 #3) |
| 4 | `9e7d00967` | fix(server): maintenance ticker DB-anchored against restart (P0 #4) |
| 5 | `cc4c761ec` | fix(views): ring layout angle divisor pre-compute (P0 #6) |
| 6 | `ebfa68f77` | fix(views): split edge rendering by status (P0 #5) |
| — | `934fb7e66` | merge: batch A (standing laws #1 + #2) |
| — | `2c39b0ffb` | merge: batch C (ticker + minimap #4 + #6 + #5) |
| — | `d7f60c968` | merge: batch B (TouchCausalNode callers #3; maintenance.go auto-merged) |
| — | `9604cb032` | chore(release): bump 0.5.83 → 0.5.84 |

## Gates (ship gate run on `d7f60c968` pre-bump)

- `pnpm typecheck`: **6/6** (37.9 s, 3 cached)
- `cd server && go test -count=1 -timeout 600s ./internal/handler/... ./internal/service/causal_graph/... ./internal/experimental/... ./pkg/agent/...` — **5 packages PASS, 0 FAIL**:
  - `internal/handler` 16.701 s
  - `internal/service/causal_graph` 2.109 s
  - `internal/experimental` 2.697 s
  - `internal/experimental/helpers` 2.684 s
  - `pkg/agent` 14.990 s
- Pre-existing flakes (`TestInstallCausalGraphSeedsHiddenTeam` + `TestInstallTimesfm_SeedsVisibilityAndIdempotent` — DB lock count global, not workspace-scoped) confirmed via `git stash` baseline run on `be80fe200`: not caused by these fixes. Same flake class as CLAUDE.md "Known Stability Surfaces".

## Process notes

- 3 parallel fix agents via `isolation: "worktree"` on isolated branches
  `worktree-wf_b31a5dc5-058-{1,2,3}`. No file-set overlap except
  `service/causal_graph/maintenance.go` (Batch B 13-line package doc update +
  Batch C 80-line boot-anchor code) — git auto-merged cleanly with no manual
  conflict resolution.
- All 3 agents honoured hard rules: stayed within assigned bug scope, no
  version bump, no `git push`, no amending previous commits, surgical
  style-matched fixes.
- Merge order: A → C → B (C first because its `maintenance.go` change is the
  canonical new code; B's doc update wraps it cleanly after).

## Data safety

- ship-mac step 1 pre-update snapshot required before `/Applications`
  overwrite (pending).
- Migration 281 covered by the existing
  `~/.multica/backups/db-pre-wl3-migs-20260827T150543Z.sql` (0.5.83 backup is
  still the pre-mig floor; 281 is purely additive — singleton row + 2
  indices).

## What changed in cooperation (post-fix projection)

The 6 fixes close the structural write-side bugs but do NOT unlock the read
side. Cooperation trajectory stays ~40-45% until daemon claim-response
subgraph injection lands in 0.5.85 (P1). After this ship:

- ✅ Every `labSourceRouteSuffix('causal_graph')` returns the correct path
  (no more silent links from a missing dict row)
- ✅ No duplicate `suggested` rows from LLM retries (never-nag on proposer
  path; mirror of the manual path's probe)
- ✅ Tier A chains no longer go stale after 30 days
  (`RefreshCausalNodesForIssue` on every hot path)
- ✅ Maintenance GC survives daily desktop restarts
  (DB-anchored boot sweep via mig 281 singleton)
- ✅ UI distinguishes active vs suggested vs rejected edges at a glance
  (split tones via `resolveEdgeTone(type, status)`)
- ✅ Ring layout is evenly spaced as designed
  (pre-compute `ringLengths` before insertion loop)

## Deferred to 0.5.85

- **P1**: daemon claim-response subgraph injection
  (`handler/daemon.go::ClaimTaskByRuntime` — the "agent reads the graph" step).
- **P2**: Tier C Pythia hypothesis→evidence closure (the last tier of the
  trust ladder); historian / verifier automation; evolver window bound;
  mention-flow doc notes.
- **Open decision gate**: MUL-6632 upstream inbox architecture (adopt vs
  fork-local filtering). Until decided, inbox-family upstream commits stay
  SKIP-DIVERGENCE.
