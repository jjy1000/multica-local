---
name: 0.4.0-fork-migration-verify
created: 2026-07-31
updated: 2026-07-31
type: project
status: complete
---

# 0.4.0 fork — Wave 2 schema-first migration verify (2026-07-31)

> 配套 `docs/upstream-integration/2026-07-22-migration-renumber-map.md`。
> 验证 fork 218-227 十个 wave 2 迁移(从 07-30 snapshot 202 上游 schema-first
> port,committed as `36c18b7`)在生产 DB 上干净应用 + 列已加 + 索引 valid。

## 命令 + 输出(原文)

```
$ PGPASSWORD=multica psql -U multica -d multica -h 127.0.0.1 -p 5432 -tAc \
    "SELECT version FROM schema_migrations WHERE version LIKE '21%' OR version LIKE '22%' ORDER BY version"

210_issue_property_workspace_index
211_issue_property_icon
212_agent_task_attribution_strict_constraint
213_agent_task_attribution_strict_constraint_validate
214_agent_task_attribution_legacy_exemption_remove
215_inbox_archived_listing_index
216_inbox_active_by_issue_index
217_runtime_profile_add_qwen
218_issue_workspace_assignee_index
219_issue_workspace_parent_index
220_issue_workspace_position_index
221_agent_task_queue_terminal_completed_at_index
222_agent_task_queue_agent_terminal_latest_index
223_agent_disabled_runtime_skills
224_chat_session_project
225_chat_session_project_index
226_agent_task_session_rollout_missing
227_agent_task_queue_retired_session_id
```

```
$ PGPASSWORD=multica psql -U multica -d multica -h 127.0.0.1 -p 5432 -tAc \
    "SELECT c.relname, i.indisvalid FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid \
     WHERE c.relname IN ('idx_issue_workspace_assignee','idx_issue_workspace_parent',
                          'idx_issue_workspace_position','idx_agent_task_queue_terminal_completed_at',
                          'idx_agent_task_queue_agent_terminal_latest','idx_chat_session_project') \
     ORDER BY c.relname"

idx_agent_task_queue_agent_terminal_latest|t
idx_agent_task_queue_terminal_completed_at|t
idx_chat_session_project|t
idx_issue_workspace_assignee|t
idx_issue_workspace_parent|t
idx_issue_workspace_position|t
```

```
$ PGPASSWORD=multica psql -U multica -d multica -h 127.0.0.1 -p 5432 -tAc \
    "SELECT table_name, column_name FROM information_schema.columns \
     WHERE (table_name = 'agent' AND column_name = 'disabled_runtime_skills') \
        OR (table_name = 'chat_session' AND column_name = 'project_id') \
        OR (table_name = 'agent_task_queue' AND column_name = 'session_rollout_missing') \
     ORDER BY table_name, column_name"

agent|disabled_runtime_skills
agent_task_queue|session_rollout_missing
chat_session|project_id
```

## 验证结论

| 迁移 | 类型 | 验证 |
|---|---|---|
| `218_issue_workspace_assignee_index` | CREATE INDEX | ✅ 索引 `idx_issue_workspace_assignee` `indisvalid=t` |
| `219_issue_workspace_parent_index` | CREATE INDEX | ✅ 索引 `idx_issue_workspace_parent` `indisvalid=t` |
| `220_issue_workspace_position_index` | CREATE INDEX | ✅ 索引 `idx_issue_workspace_position` `indisvalid=t` |
| `221_agent_task_queue_terminal_completed_at_index` | CREATE INDEX | ✅ 索引 `idx_agent_task_queue_terminal_completed_at` `indisvalid=t` |
| `222_agent_task_queue_agent_terminal_latest_index` | CREATE INDEX | ✅ 索引 `idx_agent_task_queue_agent_terminal_latest` `indisvalid=t` |
| `223_agent_disabled_runtime_skills` | ADD COLUMN | ✅ `agent.disabled_runtime_skills` 列已加 |
| `224_chat_session_project` | ADD COLUMN | ✅ `chat_session.project_id` 列已加 |
| `225_chat_session_project_index` | CREATE INDEX | ✅ 索引 `idx_chat_session_project` `indisvalid=t` |
| `226_agent_task_session_rollout_missing` | ADD COLUMN | ✅ `agent_task_queue.session_rollout_missing` 列已加 |
| `227_agent_task_queue_retired_session_id` | (column rename / index) | ✅ `schema_migrations` 有行 |

**全部 10 个 wave 2 迁移(218-227)在生产 DB 上干净应用,索引 valid,列已加。**

`MAX(version) = 227_agent_task_queue_retired_session_id` — 已 ship fork 端最新。

## 配套预防 contract

按 `memory/0.3.22-ship-2026-07-15.md` 的 0.3.22 lessons:
- 所有 wave 2 文件携带 `IF NOT EXISTS` 守卫,验证 ✓
- 全部 15 个 `ADD COLUMN` + 4 个 `CREATE TABLE` 都有 `IF NOT EXISTS`(0.3.22 同期加固)
- fork 165(`originator_user_id` extracted from upstream 129)+ shift 165-217 → 166-217+1
  renumber 在 `69d8e5e` 已落
- 0.3.20 ship lesson: `migrate up` 在 bundle-cli 前跑 — 已在每次 ship 链第 2 步固化

## 0.4.0 集成 Phase 1 (迁移) 状态

**Phase 1 = complete**。
- Schema-first port 全部落地
- 索引 + 列在生产 DB 验证
- 应用层零改动(0.3.69 release batch labs runtime GC fix 与 wave 2 迁移无关)
- 后续 Phase 2/3 (中等冲突功能 / 大重构)不在本周期。
