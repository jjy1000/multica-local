# Upstream Port Roadmap — 0.5.101 + beyond

**Date**: 2026-09-06 (just after 0.5.100 ship)
**Head**: `eefb3a722` (0.5.100)
**Last upstream HEAD**: `7a438bd5b` (no new commits since 0.5.100)

## 0.5.101 — three-piece perf/security + replica + ws claim poll

**User-confirmed scope (3 PRs, ~1,500 LOC delta, all Go server, no FE):**

1. **PR-1: MUL-7050 (perf search remove counts)** — LOW complexity, +50/-50 LOC, 9 files pure deletion + test additions.
2. **PR-2: MUL-7016 (read replica infrastructure + daemon route)** — HIGH complexity, ~1,200 LOC, paired (B.4 infra first, B.5 route second). New `server/internal/dbreader/` package + `Handler.ReadSelector` field + `Business` constants + 6 metric getters + 4 docs files + `docker-compose.selfhost.yml`.
3. **PR-3: MUL-7008 (WS claim poll 3m)** — MED complexity, +250/-15 LOC, config key + capability flag + scheduler branch. NO wsrpc.go (0.5.100 memory was wrong about this).

## 0.5.102 — UX batch

**Pre-flight**: Verify daemon PAT rotation invariant (per user C.1 decision) before starting C.1 port.
- Read `apps/desktop/src/main/index.ts` for `daemon:sync-token` handler
- Confirm PAT mints fresh on user id change
- If absent: STOP and surface to user

**User-confirmed scope (3 PRs, ~2,700 LOC delta):**
1. **PR-A.1+A.2 (MUL-7002 heartbeat + invalidation ordered pair)** — HIGH, ~1,200 LOC. A.2 hard depends on A.1's `RuntimeLease` populating `Hub.byRuntime`. New `RuntimeLease` struct + `heartbeatLivenessState` + sqlc CTE `GetAgentRuntimeHeartbeatLeases`. Test `daemon_test.go:889` BREAKS (assertion change to "no ack on heartbeat gone").
2. **PR-C.1 (MUL-7028 session expire → login)** — HIGH, 8-phase surgical, ~850-1,100 LOC. New `apps/desktop/src/renderer/src/platform/session-teardown.ts` + `packages/core/drafts/cleanup-registry.ts` + `packages/core/platform/session-cleanup.ts`. Adopt upstream's named-options `initCore` signature. Replace fork's inline `handleDaemonLogout` in App.tsx with `SessionTeardown` binding.
3. **PR-C.2 (MUL-6833 error page i18n scaffolding)** — MED, ~400-500 LOC. Skip server + callback + e2e (OAuth dead code, ~700 LOC). Port 24 generic i18n keys (common.not_found.* + ui.error_boundary.*). Extract `apps/web/lib/html-lang.ts` from layout.tsx. Add `emergency-locale.ts` + `user-locale-sync.ts`. Localize global-error.tsx + not-found.tsx + error-boundary.tsx.

## 0.5.103 — high-risk backend batch

**User-confirmed B.7 split into 3 sub-batches:**

1. **B.7a (MUL-6986 Source field + protocol)** — transport layer. ~50 LOC, + 5 files. Adds `Source` field to skill definition + `protocol/messages.go`. Includes `daemon_legacy_skill_redirect_test.go` for forward-compat. **First sub-batch — can land standalone**.
2. **B.7b (MUL-6986 content move)** — content layer. −2,000 LOC net, +12 files. Creates `multica-platform/` skill (NEW SKILL.md + 8 reference files). Deletes 8 source maps + 8 source skill dirs (preserves 12 fork-only skills: causal-graph-curator, claude-science, claude-science-runtime, code-canvas, creating-swarms, lab-builder, llm-wiki, mythos, pythia, semantica, semantica-decision-advisor, timesfm). **Note**: fork has NO `multica-onboarding` skill (retired 0.3.57) — upstream scope-to-Mika sub-commit has no fork surface.
3. **B.7c (MUL-6986 builtin_skills_test rewrite)** — test layer. +1,000 LOC, ~2 files. Port rewritten `builtin_skills_test.go` (must read fork's current state first) + `daemon_skill_resolve_scope_test.go`.

**Plus standalone high-risk PRs:**

4. **B.2 (MUL-7048/6891 runtime presence)** — HIGH, 4 platforms (server + packages + mobile + web), ~300-450 LOC. sqlc regen + new migration. 11 sub-commits tightly coupled.
5. **B.3 (MUL-6904/6684 OpenClaw MCP isolation)** — HIGH, ~700-1,000 LOC. 7 files. CI workflow files (openclaw-config-smoke.yml NEW) — fork does not run GitHub Actions; either drop or keep for parity. `agentintegration` build tag.
7. **B.6 (MUL-6665 task → runs rename)** — MED by LOC, LOW by nature. 188 files but ~47 unique × 4 locales. Vocabulary rename NOT schema. **Recommend chunk into 3 sub-commits** (CLI → locales → docs) to manage reviewer fatigue.

## SKIP — dormant / fork-absent

- **A.3 (MUL-7051 armed autopilot subscribers)** — SKIP per user decision B. `DelegatedSubscriber` is dormant on fork (zero callers outside its own test file). MUL-5483 prerequisite never ported. Document SKIP rationale in `release-notes-0.5.103.md`. Revisit only when upstream PR adds the autopilot dispatch-side caller.
- **MUL-6665 schema migration (if upstream has one in a future commit)** — fork's `agent_task_queue` has `delegated_from_task_id` per mig 165 era; pure column ops are OK, but the rename to "runs" is presentation-layer only.

## Key reversals from 0.5.100 memory

| Memory said | Deep-dive said | Impact |
|---|---|---|
| MUL-7050 "dormant" | REACHABLE LOW | Ship as PR-1 warm-up |
| MUL-7016 "fork-absent infra" | REACHABLE HIGH | Ship as PR-2 paired |
| MUL-7051 "standalone MED" | DORMANT, 3 options | SKIP per user B |
| MUL-7008 "wsrpc.go doesn't exist" | NO wsrpc.go in this commit | Reclassify to MED |
| MUL-7048 "dormant" | REACHABLE HIGH | Standalone 0.5.103 |
| MUL-6665 "dormant" | REACHABLE MED (vocabulary not schema) | Standalone 0.5.103 |
| MUL-6986 "dormant" | REACHABLE VERY HIGH | Split into 3 sub-batches |

## Localization conflict scan (full roadmap)

**Zero hits across all 11 PRs** (verified by Agent B's grep):
- posthog / PostHog — fork's `analytics.NewFromEnv()` returns `NoopClient{}`; no PostHog integration touched
- electron-updater / autoUpdate — fork's updater is no-op; nothing in this batch
- SendCode / VerifyCode / GoogleLogin — fork returns 410 Gone; OAuth flows untouched (PR-C.2 explicitly skips the OAuth callback blocks)
- CloudFront / cloudfront — no cloud runtime touched
- workspace_invitation / invitations — retired 0.5.36; no resurrection
- billing / subscription — no billing surface touched
- contact-sales / contact_sales — retired; no resurrection

The only `subscription` hit was a `vi.hoisted` test variable named `subscriptionSetups` in MUL-7048 — not billing-related.

## Next actions (this session)
- 0.5.101 PR-1: MUL-7050 (LOW, warm-up)
- 0.5.101 PR-2: MUL-7016 (HIGH, paired B.4+B.5)
- 0.5.101 PR-3: MUL-7008 (MED, capability flag)
- 0.5.102 pre-flight: verify daemon PAT rotation before C.1

## Memory pointers
- `~/.claude/projects/.../memory/0.5.100-upstream-port-batch-with-ship-chain-verify-fatal-2026-09-06.md` — read for port strategy + 0.5.100 lessons
- `~/.claude/projects/.../memory/0.5.99-h9-import-fix-ship-chain-verify-2026-09-06.md` — the H9 import-vs-re-export pattern (must `import { x }; export { x }`, not bare `export { x } from "..."`)
- Per-port stats: fork-side per-file diff ≤ 5× upstream's per-file stat (wholesale-adoption trap from 0.5.36)