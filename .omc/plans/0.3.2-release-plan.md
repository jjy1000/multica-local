---
name: 0.3.2-release-plan
created: 2026-07-05T15:05:00Z
updated: 2026-07-05T15:05:00Z
status: backlog
version_target: 0.3.2
target_dmg: apps/desktop/dist/multica-desktop-0.3.2-mac-arm64.dmg
predecessor: .omc/plans/0.3.2-backlog.md (audit findings) + memory/0.3.2-backlog.md
---

# Multica 0.3.2 — Release Plan

## 范围 (In / Out)

### IN

- **Schema**: 上游 migration `134_runtime_profile_add_qoder`（fork 当前在 127；123/124/125 schema 已 ship，handler `issue_child_done_stage.go` 已就位）
- **Schema**: 上游 migration `130_xxx` / `131_xxx` / `132_xxx` / `133_xxx` 之间的非云端 schema（按 Explore agent 输出追加；不在本计划假设中）
- **Schema-only upstream cherry-picks**: 仅 DDL/索引文件，不含 handler 业务逻辑（防止 cloud 漏入）
- **Path D modulePreload**: `apps/desktop/electron.vite.config.ts` 加 `build: { modulePreload: false }`
- **electron-builder.yml asarUnpack**: 加 `resources/bin/` + 任何 native PG 路径（防 0.2.89.5.1 hot-patch 复发）
- **Tier 0 必修 ship-blocker (audit 0.3.2-backlog)**: M12 bundle-cli ldflag 拆分 + F7 before-quit 合并
- **Tier 1 ship-blocker (audit 0.3.2-backlog)**: M9 pre-update snapshot TABLES drift
- **Agent 连续运行**: autoStart 已 ship（0.3.0 验证），squad leader 触发链 review + GUI 状态推送收尾
- **UI 关键路径微动效**: `motion/react` 已在 packages/views 用，扩展到 workspace switcher / issue status change / agent online pulse
- **审计**: code-reviewer agent（阶段 5 全程覆盖）
- **打包**: pre-update-snapshot → bundle-cli → electron-vite build → electron-builder package → hdiutil 安装 → smoke

### OUT

- Composio / GitHub / Slack / Lark / Cloud / PostHog / OAuth / invitations / email-verification / electron-updater publish
- 数据库 destructive migration（CLAUDE.md P0 守卫；migration 134 NOT VALID 是 additive，保留 134 进入）
- Hot-patch binary 替换（只走 DMG 路径；memory `0.2.89.5.1 asar unpack failure` 教训）
- Tier 3 / Tier 4 audit 项（推到 0.3.2.1 hotpatch + 用户决策 D1/D2）
- 跨语言 ipc / race condition 之外的非 ship-blocker P2 audit 项（推 0.3.3）

## 阶段 1 — Schema 迁移 (对应 task #6)

> 起点 `127_task_squad_id` applied。目标 `134_runtime_profile_add_qoder` applied。
> 唯一确认 gap = `128`、`129`、`130–133`（5 个 gap，由 Explore agent 列出每个文件后再追加）。

- [ ] **1.1** 跑 Explore agent 确认 fork 未应用的上游 migration 列表 + 每个文件 LOC Δ + 是否有 non-schema（handler / sqlc / runtime）联动。`memory/multica-fork-vs-upstream-divergence-map.md` 是 ground truth 起点
- [ ] **1.2** Dry-run: `cp server/migrations/134_runtime_profile_add_qoder.{up,down}.sql /tmp/` → `go test -run TestMigrate ./server/cmd/migrate/` 验证 forward/back（NOT VALID 约束允许 historical gemini row 不阻断升级）
- [ ] **1.3** Copy `134_runtime_profile_add_qoder.{up,down}.sql` 到 `server/migrations/`
- [ ] **1.4** 验证 sqlc 不爆（不动 schema → 不需要 regenerate，但保险起见跑 `cd server && make sqlc`）
- [ ] **1.5** **不重打** `runMigrate` P0 destructive-guard（已在 0.3.0 ship，audit 0.3.2-backlog 确认 intact）。仅 dry-run forward+back+forward 一次确认 134 idempotent
- [ ] **1.6** Row parity 检查（与 `multica-0.3.1-final-verification-2026-07-03.md` baseline 对齐）: `workspace=1 / issue≈84 / comment≈477 / agent=38`。任何漂移 > 5% → 立刻回滚
- **回滚**: `cp /tmp/134_*.{up,down}.sql /dev/null`（如未 copy）+ `cd server && ./bin/migrate down 1` → 134 NOT VALID 删除，CHECK 回到 13 个值

## 阶段 2 — 代码 cherry-pick (对应 task #7)

> 来源: Explore agent 的输出（阶段 1.1 跑出）。**不要等 agent 跑完，先按已知范围列**。下表是占位。

### 批次 A — Path D modulePreload (P0 修复，1 文件)

- [ ] **2.A.1** `apps/desktop/electron.vite.config.ts` — `main.build.modulePreload: false`（renderer 端 dynamic import 按需加载）
- [ ] **2.A.2** `pnpm typecheck` → 0 errors（仅 config 文件，不动逻辑）

### 批次 B — Electron builder asarUnpack (P0 修复，1 文件)

- [ ] **2.B.1** `apps/desktop/electron-builder.yml` — `asarUnpack:` 补 `resources/bin/**` + `resources/pg/**`（如果 0.3.0 standalone 路径） + `node_modules/embedded-postgres/**` + `node_modules/@embedded-postgres/darwin-arm64/**`
- [ ] **2.B.2** 读 `apps/desktop/scripts/bundle-cli.mjs:170-177`（T6）确认 codesign `--options runtime` 是否需要补 — 暂不改，记 0.3.2.1 评估

### 批次 C — Audit Tier 0 ship-blockers (2 文件)

- [ ] **2.C.1** `apps/desktop/scripts/bundle-cli.mjs:149` (M12) — 拆分 ldflag per binary: `ldflags: BUILDS[i].ldflags === false ? '' : fullLdflags`。`server/cmd/migrate/main.go` 没 `var version`，必须 skip `-X main.version`
- [ ] **2.C.2** `apps/desktop/src/main/index.ts:600` + `apps/desktop/src/main/daemon-manager.ts:1321` (F7) — 合并 before-quit 到 index.ts 单一 ordered handler (stopDaemon → stopServerManager → app.quit)，删 daemon-manager 侧
- [ ] **2.C.3** T1 mirror: `apps/desktop/scripts/bundle-cli.mjs:142` — 验证 `multica` ldflag 在 main.go 已有 `version+commit+date`，**通过**（已 grep 确认）；本条不需修，标 DONE

### 批次 D — Audit Tier 1 (1 shell 文件)

- [ ] **2.D.1** `~/.multica/scripts/pre-update-snapshot.sh:76` (M9) — 从 `\dt` 实时生成 TABLES，或直接 sync 到 `pg-bootstrap.ts:884` MIGRATION_TABLES 全部 8 表（comment / inbox_item / squad / skill 漏 4 个）
- [ ] **2.D.2** `M8` 同脚本 `:15` — 解析 `~/.multica/profiles/*/config.json` DB URL，custom-port profile 也能备份

### 批次 E — Audit Tier 2 (20+ 文件，按 P2 顺序)

> 单 commit per finding，每批 ≤ 3 文件，与 Tier 4 fork cleanup 解耦。

- [ ] **2.E.1** `server-manager.ts:519` (F1) — `inflightEnsure` keyed by `${profile}|${apiUrl}`
- [ ] **2.E.2** `server-manager.ts:392` (F2) — `runMigrate(backend: PgBackend)` 必填
- [ ] **2.E.3** `daemon-manager.ts:893` (F6) — `startDaemon` 走 `withGuard`
- [ ] **2.E.4** `pg-bootstrap.ts:312` (M5) — postgresql.conf regex skip 注释行
- [ ] **2.E.5** `pg-bootstrap.ts:242` (M7) — `flock(pgdata() + '/.lock')` 多实例防并发 initdb
- [ ] **2.E.6** `pg-bootstrap.ts:1067` (M11) — `pg_tables` 断言存在性
- [ ] **2.E.7** `pg-bootstrap.ts:884` (T2) — MIGRATION_TABLES 加 `user` + `schema_migrations`
- [ ] **2.E.8** `apps/desktop/scripts/bundle-cli.mjs:188` (M13) — inspect DMG 验证 packaged manifest 在 asarUnpack glob

### 批次 F — Upstream 上游非云端代码 (Explore agent 输出驱动)

> 仅在 Explore 确认 fork 缺且非 cloud / non-telemetry / non-OAuth 后纳入。

- [ ] **2.F.x** 每条 = 1 sub-task，按文件路径列；本计划**保留位置**，具体清单 Explore agent 跑完后填
- **回滚**: `git revert <commit>` per finding（fork 非 git 仓库 → 用 `cp` 备份 + 手动 patch-back）

## 阶段 3 — 智能体连续运行修复 (对应 task #8)

> autoStart 已 ship (0.3.0)；squad leader 触发链已 ship (0.2.95 / MUL-3724 / MUL-3744)。本阶段是补强，不是修 bug。

- [ ] **3.1** `cat /Users/jiangjianyan/jjy/multica-main/server/internal/handler/squad_briefing.go` 重读 — 验证 `shouldInjectSquadLeaderBriefing` 仍走 `task.IsLeaderTask + task.SquadID`，无 fallback 误触
- [ ] **3.2** 重读 `apps/desktop/src/renderer/src/App.tsx` — `tryAutoStartFromMain` + `maybeRecoverDaemon` + `useRef` fire-once 三处都在，验证
- [ ] **3.3** GUI 状态推送补强: `packages/views/agents/` 加 `agent-online-pulse` 组件（用 `motion/react` 已有依赖）— 用户点 issue / agent 时立即看到 leader 是否在线，而不是 30s polling
- [ ] **3.4** Watchdog `~/.multica/scripts/multica-daemon-watchdog.sh` 不动（已 load 进 launchd）；只验证 `~/.multica/daemon-needs-spawn.txt` 出现频率
- **回滚**: 3.1-3.2 验证无 → 不需回滚；3.3 动效可独立 revert

## 阶段 4 — UI 动画 (对应 task #9)

> `motion@^12.38.0` 已在 `packages/views` 和 `packages/ui` 装好（grep 确认）。

- [ ] **4.1** `packages/views/layout/workspace-switcher.tsx` — 工作区切换 `AnimatePresence` + 250ms fade（关键路径 1）
- [ ] **4.2** `packages/views/issues/components/issue-status-badge.tsx` — 状态变更 200ms scale 1 → 1.05 → 1（关键路径 2）
- [ ] **4.3** `packages/views/agents/components/agent-avatar.tsx` — 在线 agent 加 1.4s `pulse` 无限循环；离线 → 静态（关键路径 3）
- [ ] **4.4** `prefers-reduced-motion` 必须尊重 — `useReducedMotion()` hook 守门
- **回滚**: 单组件 revert；不破坏功能

## 阶段 5 — 审计 (对应 task #10)

> code-reviewer agent (Opus) — 一次性全 diff 审计。

- [ ] **5.1** 启动 code-reviewer agent，传入本计划文件 + `git diff main` (non-git 仓库 → 用 `find -newer .omc/plans/0.3.1-stability-fixes.md -type f`)
- [ ] **5.2** 重点关注: schema 134 NOT VALID 不漏触发器 / Path D 是否真关闭 modulePreload 而非隐藏警告 / F7 合并后 quit 是否真干净
- [ ] **5.3** 任何 P0 → 立即修；任何 P1 → 本阶段内修；P2+ → 推 0.3.2.1 backlog

## 阶段 6 — 打包 + 替换 (对应 task #11–14)

> 严格走 0.3.1 stability-fix memory 的 ship 顺序。

- [ ] **6.1** `bash ~/.multica/scripts/pre-update-snapshot.sh` → 必须 exit 0（M9 修好后 TABLES 完整）。失败立刻停
- [ ] **6.2** `cd apps/desktop && pnpm typecheck && pnpm test` → 0 errors / 275+ tests
- [ ] **6.3** `cd server && make test` → all green
- [ ] **6.4** `pnpm --filter @multica/desktop bundle-cli` → Go binaries + manifest 写到 `apps/desktop/resources/bin/`，**M12 修复后这里会真编译 migrate**，验证 `apps/desktop/resources/bin/migrate -version` 输出 `dev` 不是空
- [ ] **6.5** `pnpm --filter @multica/desktop build` → electron-vite，Path D 后 main bundle 单文件，renderer dynamic chunks 按需
- [ ] **6.6** `pnpm --filter @multica/desktop package` → `apps/desktop/dist/multica-desktop-0.3.2-mac-arm64.dmg`
- [ ] **6.7** 安装:
  ```bash
  hdiutil attach dist/multica-desktop-0.3.2-*.dmg -nobrowse -quiet
  cp -R /Volumes/Multica*/Multica.app /Applications/
  hdiutil detach /Volumes/Multica* -quiet
  ```
- [ ] **6.8** Smoke:
  - `pkill -f "multica daemon" ; pkill -f "Multica.app/Contents/MacOS/Multica"`
  - `open /Applications/Multica.app`
  - 6s 内 `lsof -nP -iTCP:5432 -sTCP:LISTEN` + `lsof -nP -iTCP:8090 -sTCP:LISTEN` 都有 listener
  - `curl -s http://localhost:8090/health` → `{"status":"ok"}`
  - Row parity: `PGPASSWORD=multica psql ... "SELECT COUNT(*) FROM workspace|issue|comment|agent"` → 1 / ≈84 / ≈477 / 38
- **回滚**: `cp -R /Applications/Multica.app.bak /Applications/Multica.app` → 回到 0.3.1 working app（pre-update 脚本已存 `.bak`）

## 风险与回滚总表

| 阶段 | 失败模式 | 回滚动作 | ETA |
|------|----------|----------|-----|
| 1 | 134 NOT VALID 触发器漏 / row parity 漂移 | `migrate down 1` → CHECK 回到 13 值 | 30s |
| 2.A | Path D 关闭 modulePreload 导致 preload import 失败 | revert electron.vite.config.ts | 5min |
| 2.B | asarUnpack 加错路径 | revert electron-builder.yml + 重打包 | 10min |
| 2.C | M12 拆分后 migrate 编译仍爆 | revert + 把 main.version 加进 migrate/main.go（不推荐，污染 fork） | 15min |
| 2.C | F7 合并后 quit 卡死 | revert daemon-manager.ts `app.on("before-quit")` 删除 | 10min |
| 2.D | M9 TABLES 改后 backup 多吐 / 漏吐 | revert ~/.multica/scripts/pre-update-snapshot.sh | 5min |
| 2.E | F1 keyed map 引入新 race | revert server-manager.ts:519 | 10min |
| 3.3 | motion 性能问题 | 单组件 revert | 5min |
| 4 | motion 违反 prefers-reduced-motion | useReducedMotion 守门 | 5min |
| 6 | DMG 启动崩 / port 不 listen | `cp -R /Applications/Multica.app.bak /Applications/Multica.app` | 2min |

## 验证检查表

- [ ] `make test` (server Go tests)
- [ ] `pnpm typecheck` (workspace wide)
- [ ] `pnpm test` (TS/Vitest 通过 Turborepo)
- [ ] Dry-run migrate forward & back (134 idempotent)
- [ ] Row parity: workspace=1 / issue≈84 / comment≈477 / agent=38
- [ ] `~/.multica/scripts/pre-update-snapshot.sh` exit 0
- [ ] DMG cold start < 10s (baseline 0.3.1 = 5.2s)
- [ ] `lsof 5432 / 8090` 双 listen < 6s
- [ ] `curl /health` 返回 `{"status":"ok"}`
- [ ] GUI 登录 → 选 workspace → 选 issue → 看到 agent online pulse
- [ ] code-reviewer agent P0/P1 全清
- [ ] `apps/desktop/package.json` `version` bump `0.3.1` → `0.3.2`

## Ship 顺序（最终）

```
1. Schema (1 文件)
2. Path D + asarUnpack (2 文件)
3. Audit Tier 0 (2 文件) — M12 + F7
4. Audit Tier 1 (1 文件) — M9
5. Audit Tier 2 选做（按 P2 优先级，每批 ≤ 3 文件）
6. Agent 连续运行验证 + UI 动效（独立 component，失败可局部回滚）
7. Code-reviewer 审计
8. pre-update-snapshot
9. bundle-cli → build → package
10. hdiutil 安装 + smoke
11. 更新 .omc/release-notes-0.3.2.md
12. 更新 memory/MEMORY.md
13. 写 0.3.2 stability-fix memory
```

## Memory 落点

- 新建 `~/.claude/projects/-Users-jiangjianyan-jjy-multica-main/memory/multica-0.3.2-stability-fix-2026-07-05.md` — 记录 audit fix ship + Path D 生效 + asarUnpack 完整
- 更新 `multica-fork-vs-upstream-divergence-map.md` — 标注 134 schema 已 ship
- 更新 `MEMORY.md` 索引 — 加 0.3.2 条目