# 0.5.75 — Upstream Integration Batch (2026-08-27)

Branch: `epic/0.5.72-followups`. 7 commits on top of 0.5.74.

## Commits (oldest → newest)

| # | Commit | Scope |
|---|---|---|
| 1 | `e31d004d3` | merge: B2 #7557 autopilot priority flag removal |
| 2 | `eb0f3e47c` | merge: B2 #7418 MULTICA_TASK_QUEUED_TTL |
| 3 | `038888e44` | merge: B3 triple (MUL-5928 + GH prefix + chat live) — REVERTED |
| 4 | `e13c339ba` | revert: B3 triple merge |
| 5 | `04d53dd87` | MUL-5928: bound board description previews (#6634) |
| 6 | `f5d7d4f51` | fix(github): support single-character issue prefixes (#7385) |
| 7 | `45724be3d` | chore(release): bump 0.5.74 → 0.5.75 |

B3 #7018 `fix(chat): keep following rapid live output` was part of the reverted triple merge — cherry-pick incompleteness (test file references 6 fork-absent props). Skipped for this batch; recoverable later with custom test aligned to fork's chat surface.

## Audit lineage

- Triage baseline: `.omc/upstream-integration-triage-2026-08-26.md` (post-triage re-grade `79a787f80`).
- Prior session (2026-08-26) launched Workflow `wf_eb8dcaa6-935` with 5 cherry-picks targeted for isolated worktree branches. Stopped mid-flight per user ("今天就先到这里").
- This session recovered: 5 cherry-picks landed on `worktree-wf_eb8dcaa6-935-{4,6,9}` branches. Merged 3 branches via `--no-ff`, then reverted the triple merge when `pnpm typecheck` surfaced the #7018 test-file incompatibility.

## Test results

- `pnpm typecheck`: PASS (6/6 packages, 53.262s)
- `go test -count=1 -timeout 600s ./internal/... ./pkg/agent/...`: all packages green except 2 pre-existing failures:
  - `TestDashboardPerAgentRollupsUseExactWindow` (locked this batch — fails on eb9d23704 baseline via `/tmp/wt-baseline` bisect)
  - `TestQuickCreateIssueParentTrustBoundary` (locked 0.5.74 batch — CLAUDE.md §Known Stability Surfaces)
- `pnpm test packages/views/issues/components/description-preview.test.ts`: PASS

## Risk vs outcome

| Dimension | Risk | Outcome |
|---|---|---|
| B2 #7557 autopilot flag removal | Low — surgical CLI change | ✅ zero conflict |
| B2 #7418 MULTICA_TASK_QUEUED_TTL | Medium — env pass-through + desktop fork override | ✅ 1-line fork adaptation in serializeEnvFile |
| B3 #6634 description preview | Low — UI bound | ✅ 3 new test cases |
| B3 #7385 GH single-char prefix | Low — regex + test | ✅ 1 test addition |
| B3 #7018 chat live output | HIGH — test file refs fork-absent surface | ⚠️ reverted, deferred |

## Row parity

NA — no schema changes, no migrations. Post-ship row parity inherits 0.5.74 baseline (workspace=1, issue≈84, comment≈477, agent=38).

## Known pre-existing issues (NOT this batch)

- **`TestDashboardPerAgentRollupsUseExactWindow`** — 900s delta leak in days=1 window. Bisected to `eb9d23704` baseline; not introduced by this batch. Follow-up: investigate `pkg/dashboard` rollup query.
- **`TestQuickCreateIssueParentTrustBoundary`** — pre-existing 80% flaky (CLAUDE.md §Known Stability Surfaces). This batch touched 0 files in `quick_create_parent_test.go` (verified via `git diff eb9d23704..HEAD -- server/internal/handler/quick_create_parent_test.go` → 0 lines).

## Files changed (cumulative)

```
M  CLAUDE.md                                                                   1 (release header)
M  apps/desktop/package.json                                                   1 (version 0.5.74 → 0.5.75)
A  .omc/release-notes-0.5.75.md                                                1 (new)
M  server/cmd/multica/cmd_autopilot.go                                         1 (B2 #7557)
M  server/cmd/multica/cmd_autopilot_test.go                                    1 (B2 #7557 test)
A  server/cmd/server/sweeper_task_timeouts_test.go                              1 (B2 #7418 new test)
M  server/cmd/server/runtime_sweeper.go                                        1 (B2 #7418)
M  server/cmd/server/main.go                                                   1 (B2 #7418)
M  apps/desktop/src/main/server-manager.ts                                     1 (B2 #7418 fork env)
M  packages/views/issues/components/description-preview.tsx                     1 (B3 #6634)
M  packages/views/issues/components/description-preview.test.ts                 1 (B3 #6634 new test)
M  server/internal/handler/github.go                                           1 (B3 #7385)
M  server/internal/handler/github_test.go                                      1 (B3 #7385 test)
```

## Next steps for user

1. **Ship the .app**: `bash scripts/ship-mac.sh --yes` (canonical 11-step ship chain).
2. **Optional cleanup**: remove the 11 unused `wt-batch*/` worktree dirs (`git worktree remove --force wt-batch2-* wt-batch3-* wt-batch4`).
3. **Optional**: rebase + merge `epic/0.5.72-followups` into `main` when ready.
4. **Follow-up tickets**:
   - #7018 chat live output: cherry-pick with custom test file aligned to fork's chat surface (deferred).
   - `TestDashboardPerAgentRollupsUseExactWindow` fix: investigate `pkg/dashboard` rollup aggregation (new pre-existing lock).
   - ~25 remaining triage commits + 41 docs-only changelog entries + 8 Batch 5 REVIEW-tier (user decision required).
